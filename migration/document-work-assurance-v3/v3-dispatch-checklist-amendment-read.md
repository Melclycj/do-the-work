# Dispatch — E10 independent read of the CONSTRUCTION-CHECKLIST amendment

**Subject: blob `f97b3483c4ac0ab22257ad8ce16fe62b9727e962`** —
`ResearchSystem/document-harness/CONSTRUCTION-CHECKLIST.md`, its state from `f054a08` onward.
Read it with `git cat-file -p`. The amendment itself is
`git diff 17e2b65..f054a08 -- ResearchSystem/document-harness/CONSTRUCTION-CHECKLIST.md`:
three clauses added by `af2905c`, two of them repaired by `f054a08`. The subject is a text,
not a range, so it is unaffected by any later commit on this branch.

Your standing instructions are
`ResearchSystem/migration/document-work-assurance-v3/v3-harness-review-contract.md`; read
it, and the counterpart it names, before anything else. They govern this read. This prompt
does not — it exists only to hand you a subject a `git log` cannot explain.

Three facts a blob cannot carry. They are pointers, not conclusions; verify each.

1. `E10` obliges an independent read of an amendment to this layer, and that obligation is
   undischarged.
2. Both reviews of the round that made these edits declined to be this read, in their own
   words — `v3-review-full-af2905c.md` §1 and `v3-review-verify-f054a08.md` §8.
3. The subject is the **post-repair** text: `f054a08` changed both `E2` and `E10` relative to
   the candidate `af2905c`.

Write `v3-checkpoint-read-f97b348.md` beside those records and stop there — the execution
side commits it, title `V3-REVIEW-RECORD-E10-E2-AMENDMENT-READ-f97b348-v1`.

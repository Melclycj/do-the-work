# Amendment checkpoint read — `8d03563` + `39e4136` (the 1df6245 cold-read F1–F4 fix batch)

**Status: review-side authored, relayed by the user, committed by the execution side.** The
rule-1 / §12 checkpoint read both amendment commits' messages declare they owe (accumulated
per rule 3; dispatch generated by `rsc v3 dispatch --range 9da20ef..39e4136` — the range form
covers exactly the two amendment commits, the record/issue commits excluded as provenance per
the 94a97f5-read precedent). Not a round in §3's sense: no plan-§8 budget, no verdict —
findings only. Delivered as the reviewer's final message and routed by the user per the
banked-unresolved record-channel issue (this read's own O5). Condensed reproduction;
substance, severities, quotes and minimum fixes preserved; the full relay lives in the
routing session.

**Headline: 2 findings (1 must-fix / 1 low) + 5 observations. All four cold-read fixes
verified correct, minimal, additive, zero signed bytes touched. The must-fix is not in the
fix content — it is that both operating contracts were modified with no provenance entry,
a recurrence of the class already paid for at `979a983`.**

## Subject, re-derived by the reviewer (no reported number accepted)

- `8d03563` parent = `9da20ef`, `39e4136` parent = `8d03563`; each the **unique** child of
  its parent (`git rev-list --children` over the full repo); HEAD == `39e4136`. Chain
  unambiguous; both messages declare this accumulated read — range-form subject compatible
  with the anti-renaming rule.
- Signed bytes untouched: all three blobs identical between tip and parent `9da20ef` — plan
  `8ad404b1…`, contract `b2dbdf75…`, supersession-1 `68031fa2…` (`git ls-tree` both sides).
- A4 `f91a7c4` not an ancestor ✓; A3 `7db177d` ancestor ✓; `ac1b383` in ancestry ✓.
- Changed paths hand-classified one by one: 9 sites all instruction layer (README /
  EXECUTION / REVIEW / both contracts / review.v2 description) + 1 tooling test (the F3
  mechanization, user-ruling authorized); none inside any `N*/` allowlist — the out-of-node
  batch form is correct.
- Worktree: amended files match HEAD; only ` M .goals/LEDGER.md` + untracked
  `ResearchSystem/docs/` (see O3).

## Findings

**F1 — must-fix — both operating contracts modified, provenance blocks carry zero entry.**

- Quote/locators: both files' provenance ends at the 2026-07-26 `b953b3c` entry; a grep for
  `2026-07-27` hits only the two new property-sentence parentheticals (operating
  contract:237, review contract:297). `8d03563` changed both contracts' property sentence,
  `39e4136` changed the review contract §2 row — none of the three modifications left an
  entry.
- Violates/risks: precedent already ruled this — the operating contract's own provenance
  records "2026-07-22, cold-read fix batch `979a983` (entry back-filled by the following
  batch, per that batch's verify-read finding F1)". The provenance entry is where an
  amendment's authorization attribution and reliance state ("may be relied on as it
  stands") live; without it a fresh session reading the contracts cannot tell whether the
  2026-07-27 text has passed its rule-1 read and may be relied on.
- Minimum fix: in this read's flag-closure batch (owed anyway), back-fill one 2026-07-27
  entry per contract (authorization: the user's cold-read F1–F4 dispositions; per
  convention, include the pointer to this read's result).

**F2 — low — `test_readme_enumeration.py`'s `stem in text` substring match is satisfiable
by unrelated prose; 2 of the 14 stems carry no actual constraint (live-mutation proven).**

- Evidence (all run by the reviewer): M1 delete README's review.v2 entry → RED
  (missing == ['review.v2'], binds the real defect shape ✓); M2 delete the [review] entry →
  GREEN; M3 additionally delete the [assurance] entry → GREEN ("review" satisfied by prose
  like "review round", "assurance" satisfied by the "document-work-assurance-v3" path
  string). README restored byte-exact (SHA-256 compared before/after, `5c6cca15…`).
- Violates/risks: exactly the named failure shape written into the execution contract's
  scope-discipline rule 2 one day earlier — "expectations weak enough that unrelated
  content satisfies them … never a substring other content can satisfy"; consequently both
  contracts' new parenthetical ("pins the README's schema enumeration") and the docstring's
  "closes the class" overclaim for those 2 stems (and any future stem that is a substring
  of existing text).
- Severity rationale (not inflated): the guarded decay class itself was graded low by the
  cold read; the found instance (review.v2) is bound; no byte is currently false about the
  current state. A guard's weakness ≤ what it guards.
- Minimum fix: narrow the match to delimited tokens within the enumeration table region
  (`` `stem` `` or `[stem]` — all 14 stems in the three README rows already appear in one
  of these two delimited forms, verified); after the change re-prove red in both the M1 and
  M2 directions.

## Observations (none owing a fix)

- **O1** — the "zero mechanical binding" headline clause read in isolation is now false,
  but the immediately following exception parenthetical + "otherwise" make the sentence
  true-as-qualified; an acceptable shape for an additive fix.
- **O2** — `39e4136`'s message says F2 was applied per "the reviewer's verbatim fix"; the
  landed text adds "(one home, N0-A6)". Content recomputed true (3 genuine $ref single
  homes; verdict/residual_uncertainty validation constraints byte-equal to v1 — enum, type,
  items, uniqueItems, allOf narrowing compared one by one; only description wording
  differs, and description is not a constraint). Commit messages are not instruction layer.
- **O3** — worktree isomorphic to the cold read's finding-8 state (` M .goals/LEDGER.md` +
  untracked `General-Harness-v2-Design.md`); the ledger's uncommitted live pointer still
  said "four findings await ruling" — stale relative to HEAD; the user's own WIP, outside
  subject.
- **O4** — the guard is one-directional (directory→README); a ghost README entry pointing
  at a deleted schema is not caught. The docstring claims only the one direction — no false
  claim; recorded for future reference.
- **O5** — the standing reads' record channel remains banked-unresolved
  (`issue-p3-corr-review-record-channel-unspecified`, ruled handled-before-Stage-2); this
  read accordingly delivered as final message, created no file in the repo, and was routed
  by the user.

## Negative results (checked, found nothing)

The F1-fix fact chain fully recomputed and holds: W2-record:3/323 signing entry
(2026-07-24, "签字", also signs the carrier), `ac1b383` in ancestry, the carrier's
top-of-file UNSIGNED line really exists inside the signed blob, all three added texts
semantically consistent with supersession-1 §3. No other package-bound steering remains in
the layer (grep hits all inside the two resolved stage markers). F4 row matches the actual
blob. F3 row mirrors the W1 sibling's form ✓. All added relative links resolve. The two
contracts' exception parentheticals agree with each other. All four fixes fall strictly
within the cold-read record's minimum-fix boundaries (the README row's UNSIGNED-residue
extra sentence is an additive extension, disclosed in the message, fact-verified). No
rewrite anywhere (all additive; rule 3 observed). Construction/product verdict vocabularies
unmixed. No new self-referential prose (the README row is the N0 §8 pointer-convention
shape).

## Coverage + recomputation

Read in full: both commits' complete diffs, both operating contracts (full text at HEAD),
the cold-read record `v3-cold-read-1df6245.md`, the new test, the README table region, the
EXECUTION/REVIEW splice context. Sampled: v1/v2 review schemas (every constraint-comparison
node), W2-record (signing-entry region), supersession-1 (head), `9da20ef` message.
Recomputed: tooling suite 427 passed (58.4s, reviewer-run); schema fixtures 36/36 OK;
review.v2 JSON load ✓; pre-fix README red reproduced (missing == ['review.v2'] exactly
matching the commit message's claim); 427−1=426 self-consistent with "426→427"; 3 signed
blobs × both sides; 14 schema files; unique-child ×2; ancestry ×3; mutations ×3 (1 red /
2 green); README restore hash. Not read: N-records in full, prior read records (pointer
targets, not this subject).

Ceiling declaration (§10): the user-ruling attributions in both commit messages (F1/F4
ruled 2026-07-27, F2 earlier the same day, "mechanize F3 first") are cross-session
authorization — consistent with the ledger's uncommitted pointer and the cold-read record's
disposition entries, no contradiction found, but the rulings themselves are not
independently verifiable from the repository.

Per rule 1: after F1 (provenance back-fill, riding the flag-closure batch) and the user's
ruling on F2, the amended text may be relied on by round ①. Dispositions belong to the
user.

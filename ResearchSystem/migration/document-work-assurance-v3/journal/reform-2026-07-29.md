# Round journal — reform 轮 (2026-07-29)

Executor-owned, append-only within the round, committed with the round. Narrative and
evidence live here so the ledger stays a pointer (2026-07-29 repair batch). First journal
of the convention it itself introduces.

## Scope

The rulings captured at `451e8b0` (drained from the ledger as they land) + cold-read
`v3-cold-read-451e8b0.md` M-1 + the R9 wording rider its O-5 banked (R6 record families).
Change set = commit B (guards / generator read mode / record homes) + commit A
(instruction-layer amendments). FULL over `8e9b60b..HEAD` after both.

## E4 evidence — the three tracked guards (neuter → red → restore → green)

Neuter = the script's single `return 1` → `return 0`; one must-fire test per guard;
restore from sha256-checked scratchpad copies:

```
review_freeze_check: neutered-exit=1(expect1) restored-exit=0(expect0) sha=2ef34839b6e4fdf6/2ef34839b6e4fdf6 match=yes
ledger_cap_check:    neutered-exit=1(expect1) restored-exit=0(expect0) sha=aa4bc454b7e20cf9/aa4bc454b7e20cf9 match=yes
layer_path_check:    neutered-exit=1(expect1) restored-exit=0(expect0) sha=e11d4cd5ebbc3c9b/e11d4cd5ebbc3c9b match=yes
```

Negative controls live in the suite (`test_precommit_checks.py`: no-marker / record-only /
at-cap / resolvable-token / non-layer-file cases), green in the same runs:

```
Ran 164 tests in 26.801s  OK   (document_harness — includes the 13 new guard tests)
Ran 329 tests in 63.780s  OK   (document_harness_review — includes the 4 read-dispatch tests)
```

## CLI smoke — `rsc v3 dispatch --read HEAD --repo-root <temp repo>`

exit=0; stdout equals the golden fixture; freeze marker written as state:

```
{"kind": "layer-read", "subject": "4e3b40be36335083a5a7ceec0f9cf5807e05d05d", "dispatched_at": "2026-07-29T13:22:59+00:00"}
```

## Notes

- `test_precommit_checks` first staged with `add -A`, which staged the marker file itself
  and tripped the freeze guard. Restaged with explicit paths (the E8 shape). The failure
  was the test's, not the guard's.
- `layer_path_check` flags exactly two decidable classes (broken `ResearchSystem/…` path;
  missing-prefix). Tokens resolvable nowhere are skipped as possibly illustrative — the
  supersession-2 prose mention of a `templates/run-v2/`-shaped path stays legal.
- The hand-written dispatch note `v3-dispatch-instruction-layer-cold-read.md` is deleted in
  commit B: never consumed (the read was re-dispatched by phrase), superseded by the
  generator's `--read` mode. The two earlier notes were consumed by their reads and stay as
  history. *[Corrected below — B3: the "never consumed" half of this sentence is false.]*

## Repair — accepted findings {B1, B2, B3, F-2} (user-approved 2026-07-30; B1 = diff-scoped; boundary = layer_path_check.py · test_precommit_checks.py · this journal)

### B3 — correction of this journal's own ground (content per the FULL's named minimum fix)

The note-deletion bullet above, and `717e547`'s body ("The never-consumed hand-written
dispatch note is deleted"), were wrong about the note. The committed record
`v3-cold-read-451e8b0.md` demonstrates consumption four ways: its §1 runs the note's own
filename-derivation command; its filename and commit title are the two the note prescribes;
its §1 tabulates the note's seven blobs against the reader's own eight; its §7 lists the
note among what was read in full. The note was the `451e8b0` cold read's dispatch document,
and its member table is M-1's subject. The deletion stands as **superseded by the
generator's `--read` mode**, not as unconsumed. User ruled 2026-07-30: the file is not
restored — its bytes stay reachable at `451e8b0`.

### B1 — correction, and the measurement that belongs here

The note above ("the supersession-2 prose mention of a `templates/run-v2/`-shaped path
stays legal") was true of that one token and false of the file. The guard's output over all
eight live members (reviewer's run, `v3-review-full-8ec4c60.md` §2):

```
CONSTRUCTION-CHECKLIST.md            clean
README.md                            clean
EXECUTION.md                         clean
REVIEW.md                            clean
v3-harness-operating-contract.md     clean
v3-harness-review-contract.md        `tooling/tests/fixtures/expected-construction-prompt.txt`  -> prefix missing
supersession-1.md                    `schema/document-assurance-v3/review.v2.schema.json`       -> prefix missing
supersession-2.md                    `assurance/runs/`, `schema/`                               -> prefix missing
```

The guard now scans only the lines the staged diff adds (the defect class is a newly
written path — `dcced4e` and `d322816` both were); the three pre-existing hits stay
un-repaired until a batch writes those tokens anew.

### F-2 — the local hook wiring, pasted (`D:/Thesis/.git/hooks/pre-commit`, lines 37-50)

```sh
# v3 harness (2026-07-29 repair batch): three tracked checks, existence-guarded so
# worktrees/branches without them are unaffected. Advisory only (README "Local
# enforcement" row): review-freeze (E9 window) / ledger cap / instruction-layer paths.
for chk in ResearchSystem/tooling/hooks/review_freeze_check.py \
           ResearchSystem/tooling/hooks/ledger_cap_check.py \
           ResearchSystem/tooling/hooks/layer_path_check.py; do
  if [ -f "$chk" ]; then
    python "$chk"
    cstatus=$?
    if [ "$cstatus" -ne 0 ]; then
      exit "$cstatus"
    fi
  fi
done
```

*(Closeout completion per the VERIFY's O-1: the paste above originally stopped one line
short of `done`; the closing line and the 37-50 label were added at closeout so the loop
is reconstructable from this record alone.)*

### E4 — the repaired guard, and the FULL's two escapes re-run

```
Ran 169 tests in 27.023s  OK    (document_harness — 5 cases added by this repair)
MutA(detection neutered) exit=1 expect1
MutB(scope widened to whole file) exit=1 expect1
M3-shape(LEDGER typo) exit=1 expect1                    (GREEN before this repair, RED now)
M5-shape(README dropped from LAYER) exit=1 expect1      (GREEN before this repair, RED now)
sha lp: 864fb40258685d4e restored (x3) ; lc: aa4bc454b7e20cf9 restored
```

MutB's first application did not apply — its own `assert count==1` caught a transport-mangled
match string and the run was discarded, then re-applied escape-proof. A mutation that never
landed proves nothing; only the re-run above is evidence.

### User direction noted for after this round (not a ruling)

Review-supplied exact fixes need not be limited to the amendment path — a low finding whose
record carries directly appliable bytes could be applied instead of banked. To be weighed
after this repair lands (chat, 2026-07-30).

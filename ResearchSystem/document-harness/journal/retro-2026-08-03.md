# 保障面二期复盘 — 证据底账（2026-08-03）

排定于 `HARNESS-LEDGER.md` backlog，触发条件 "C2 关批后"（C2 闭于 2026-07-30）。本文件是**证据**，
不是裁决：裁决由用户下，落在本文件末尾的 §7 与随后的 ledger 行。

来源：四路只读盘点（组件 / 生长-退役 / 看护费 / 两议题）+ 主线程复核。**subagent 的两条结论被主线程
测量推翻，见 §0**——底账里凡与之冲突的一律以复核为准。

**覆盖诚实边界（`R4` 式披露）**：主线程亲自复核的只有五处——hook 安装状态、电池实测耗时、evidence
pass 的 mtime 序列、`document_harness` 对 `rsclib/harness` 的导入关系、两个历史命令组的活 CLI 测试。
**本文件其余数字（组件计数、轮次表、verdict 表、token 账）为 subagent 报告，未逐条复核**；已知它们
在两处出过错（§0），故引用时按"报告值"对待，决策前值得复算的应复算。

## 0. 主线程复核推翻的两条 subagent 结论

1. **"本 worktree 没装 pre-commit hook" — 错。** 该 agent 查了 `D:/Thesis-stage-control-refactor/.git/hooks`，
   但本目录是 worktree，git-dir = `D:/Thesis/.git/worktrees/Thesis-stage-control-refactor`，
   common-dir = `D:/Thesis/.git`。`D:/Thesis/.git/hooks/pre-commit` **存在且已读全文**：依次跑
   repo-audit → `contract_provenance_check`（存在性守卫，该文件已于 07-28 删除故跳过）→
   `review_freeze_check` / `ledger_cap_check` / `layer_path_check` 三守卫，任一非零即 exit。
   **三个守卫每次 commit 都真跑**（本 session 五次 commit 的 repo-audit 输出为佐证）。
2. **电池"~7–8 / ~10 分钟"witness — 数量级错，比例对。** 见 §3。

## 1. 保障面现况（HEAD）

| 类别 | 数量 | 备注 |
|---|---|---|
| pre-commit 守卫 | 3 | 全部 advisory，`--no-verify` 可绕；均已安装并生效（§0） |
| check kinds | 6 + 1 | 闭合 union（file_exists / json_schema / markdown_link / locator_exists / git_diff_boundary / command_exit）+ governance_scan |
| JSON schema | 34 | v3 pack **15** / 根 6 / harness-v2 遗留 13 |
| schema 树 fixture JSON | 90 | 非 schema，测试语料 |
| 模板 | 6 文件 / run-v2 README **7 个规则节** | 节数即累积计数器 |
| record 家族 | 74 文件 / 6 家族 | full 26 · checkpoint-read 24 · verify 8 · cold-read 4 · dispatch 2 · 其他 10 |
| 指令层条款 | **22 自有**（E1–E12 / R1–R10）+ 11 引用（V3-D#/N#/invariant/W1/W2） | 自有全在 CONSTRUCTION-CHECKLIST |
| 三份指令文件行数 | 576（164 + 259 + 153） | |
| 指令层 9 成员总行数 | **904** | 每轮开轮 cold read 的读面 |
| round record 总量 | 62 文件 / **15,196 行**（均 245 行） | |
| CLI | 5 组 / 14 叶命令 | v3 组 6 个 |

**一处未冻结缺口（发现时状态）**：`E2` 冻结 schema pack 时写死 "fourteen files"（2026-07-29），而
`paragraph-map.schema.json` 于 07-31 加入，按当时 `E2` 的文字（"a pack file added later is not frozen
by this rule"）**不在冻结面内**——发现时 pack 15 个文件、其中 1 个不受 E2 保护。
**⚠ errata（2026-08-04，设计轮 VERIFY 的 V-1）**：本段原用现在时，在 HEAD 上已假且与本节标题
「保障面现况（HEAD）」冲突。`22b27aa` 的 re-baseline 已把该文件纳入冻结（15 件全保护），`c05d052`
又把该文件自述的"不在冻结面内"改正——**缺口已闭**。

## 2. 生长 vs 退役（2026-07-18 → 08-03，16 天）

- **42 个构造轮**（harness 自身定义：动用 FULL/VERIFY 预算者），对 **16 次删减/收窄/降本事件**。
- **守卫净增 +2**：07-28 删掉 1 个（`contract_provenance_check`）→ 空窗一天 → 07-29 REFORM 一轮加 3 个 → 至今仍是这 3 个。
- **单轮最大加法** = REFORM（3 守卫 + 第 3 dispatch 族 + 2 记录件家 + 6 条款）——即 O-5 指控的那一轮；
  **单轮最大减法** = 它自己一个 cycle 之后的修复（`1c45e24`：把 `layer_path_check` 的全文件读删成 diff 范围读，
  VERIFY `49d9829` O-4 原文 "adds no component"）。
- **四次明确的降本裁决全部落在 08-02 → 08-03**：AUDIT-CADENCE · PRE-START-OPT carve-out ·
  BATTERY-TIERING · ledger 批免轮。**曲线已在复盘开会前自己转向**——这是复盘要裁的第一件事的前提。
- **FULL verdict 走势**（26 份记录）：18 clean / 7 CHANGES_REQUIRED / 1 SPEC_GAP。**7 个
  CHANGES_REQUIRED 全部落在 07-27→08-02**；07-31 之后的 12 次 FULL 是 11 clean + 1 SPEC_GAP。

## 3. 看护费 — 钱在哪（本节推翻一条今日裁决的 witness）

**机器时间几乎免费，且今日裁决的绝对值错了 4 倍：**

| 测量 | 值 | 出处 |
|---|---|---|
| 电池 6 条腿，工作树直跑 | **130 s**（5 腿 2.1s + pytest 127.9s） | 本 session 主线程 `Measure-Command` |
| p5a-shells evidence pass 真实全程（17 checks） | **2 分 22 秒**（`00:08:34→00:10:56`） | evidence `chk-*.out.txt` mtime 序列；**已排除 checkout 伪影**：文件写于 2026-08-03 00:08–00:10，evidence commit `86defbc` 时间戳 `00:13:13 +1000`，晚 2 分钟——checkout 造不出 pytest 那 108 秒的腿间隔 |
| 其中 pytest 一条腿 | ~108 s | 同上（`00:08:55→00:10:43`） |
| BATTERY-TIERING 裁决所据 witness | "~7–8 of the ~10 minutes" | run-v2 README §Regression-battery tiering |

**结论分两半，必须分开说**：该 witness 的**比例**对——电池约占 evidence pass 的 **85%**（≈120s / 142s）；
**绝对值错约 4 倍**——pass 全程 2.4 分钟而非 10 分钟，故分档实际省的是 **≈2 分钟/次**，不是 8 分钟。
该 FULL 的 O2 已标注"minutes 半边无 repo lock"，本复盘把它测实了。

**真正的钱在 review/audit token，不在机器**：

| 项 | 值 |
|---|---|
| p5a-firewall 4 轮 from-scratch audit | ≈525k tok / ~28 min |
| p5a-shells R1–4（v2 周期） | ≈738.5k tok / 38.9 min |
| p5a-shells R5–6（v3 周期） | ≈426.3k tok / 28.9 min |
| **两个 run 合计 10 轮** | **≈1.69M tok / 95.8 min** |
| 单轮区间 | 171k–215k tok / 8.6–14.9 min |

**量级对比**：砍掉一条电池腿省 2 分钟机器时间；省掉一轮 audit 省 ~200k token。
**过去两天的优化火力集中在便宜的那一半。**

## 4. 版本隔离（v1/v2/v3）

- **零机械 enforcement**。三个守卫分别管：指令层路径可解析 / ledger 行数 / review 冻结窗口，**没有一个管版本**。
  唯一的规则是 N0 §3 的一句话：**"Every old root defaults to historical-only for v3."**
  （`migration/document-work-assurance-v3/N0/N0-record.md` §3），违者 `SPEC_GAP`——靠人判。
- **三起真实事故**（均由独立 read 在 07-31 抓到）：
  1. ledger 与 commit 正文断言三份非 v3 contract "无活代码读者"——**假**：`stage-control-fixtures/validate.py`
     运行时读 `Stage-Control-Contract.md`（`v3-checkpoint-read-dcced4e.md` M-2）。
  2. `rsclib/harness/schemas.py:20` 硬编码读 v2 contract 字节算 digest——旧代码活着读旧合同
     （`v3-checkpoint-read-9f2ec9a.md`）。
  3. "旧器械由各自 track 冻结"是 **false comfort**，实际无人冻结（`v3-checkpoint-read-f9a6600.md` L-1）。
- **新发现的命名塌陷**：**"v2" 一词过载**——`review.v2.schema.json` / `document-work-spec.v2.schema.json` /
  `templates/run-v2/` 是**活的 v3 内部 wave-2 形状**，与已退役的 "General Harness v2" 无关。
  **grep "v2" 无法区分两者**——这正是"旧文本被当活文本"的机制性温床。

## 5. 独立 repo 拆分 — 三硬伤现状

| 硬伤 | 现状（实测） |
|---|---|
| E2 冻结件在 ResearchSystem 外 | **只有 1 件**：签名 plan blob `8ad404b1…` = `.goals/plans/document-work-assurance-harness-v3.plan.md`；其余 3 blob + schema pack 全在内 |
| 指令层/hook 跨根引用 | **9 成员中 5 个**引用 `.goals/` 或 `.harness/`；`review_freeze_check.py` 与 `rsc.py:390` 硬编码 `.harness/review-pending.json`；三守卫均以 `repo_root = cwd` 解析，假定从外层根调用 |
| "276+ 混合历史" | **原数字不可复现**（引入时 252 / 当日 270，两种口径都不是 276）。**现值**：总 commit 491，碰 RS 的 379，其中**同时碰 RS 外路径的 104**，纯 RS 275 |

## 6. 曲线读数（只报事实，判断归用户）

1. 加法侧 42 轮 / 减法侧 16 次；守卫净 +2；条款 22 条自有。
2. 四次降本裁决全在最近两天，即 **O-5 提出后 4 天曲线自发转向**——复盘要裁的是"把这个转向变成常设纪律，还是承认零星修剪已够"。
3. 近 12 次 FULL 有 11 次 clean——**机器在证明自己没问题**。两种读法都成立：(a) 构造质量已高，守卫在正常工作；
   (b) review 已进入边际收益递减，每轮 ~200k token 买一句 "no blocker"。**这是本复盘最贵的一个判断**。
4. 保护面的成本结构与优化火力**错配**（§3）。
5. 版本隔离是**唯一一处"该加而未加"**：三起有记录的事故 + 零机械防线 + grep 无法区分的命名塌陷。

## 7. 用户裁决（2026-08-03，本文件即这些裁决的记录）

1. **砍 audit token — 只做 (d)「下一个 run 先测新基线」，其余全部不做。**
   - **(a) 机械前置审 obligation→check 可达性：撤回**（提出后当场撤）。现有三道门——pre-freeze
     机械对账（指令枚举 ⟷ 树/scope）、checker dry-run（check **能不能跑**）、authoring gate 的
     paragraph-map 三向交叉（指令字节 ⟷ map ⟷ unit）——已覆盖大半；残余只剩 obligation ⟷ check
     的**语义可达性**（R5 f1 那类：check 跑得好好的，但 `load_shells` 按 `object_type == Shell`
     过滤、无 check 读 `live_count`，物理上看不见 obligation 声称的东西），而这一环**只能做到
     启发式**（grep obligation 断言里的标识符是否出现在所绑 check 的代码路径），必然误报。
     用会误报的新守卫省 token，与本复盘方向相反；且 cadence 明文：FULL 的
     instruction-completeness recheck 每次从头重走，是不受任何 cadence 规则影响的下游兜底。
   - **(b) 轮数封顶 + 残差升级：下策，压后**——其他方案落地后 pre-START 长度仍失控再议。
   - **(c) 跨 freeze 版本继承 audit 结论：挂起**，等下一个真 run 的结果再判。理由是因果而非统计：
     (c) 只在**冻结指令中途换版本**时触发，而 p5a-shells 那两次换版本的成因，恰是两道新门各自的
     立案 witness（v1→v2 由指令侧 must-fix 引起 = 机械对账那条；v2→v3 由 `check_shells.py`
     未经执行即冻结引起 = dry-run 那条）。门若管用，(c) 几乎不触发、价值≈0；门若不管用，(c) 每次
     触发省 ~20 万 token（R3 176k + R5 215k = 391k，占已记录 169 万的 23%）。而 (c) 是四案中
     风险最高的一个（按字节判「未变」看不出新增段落改变旧段落含义）——为可能已被消灭的收益买这个
     风险是坏交易，一个真 run 即可判明身处哪个世界。
2. **产品 run 的 FULL 不砍**——它就是产品本身（`V3-D3`：本 harness 的承诺是可见性，FULL 是那个
   可见性的唯一交付物）。**不新造轻量审档**（新造档 = 加组件，与复盘方向相反）；降密度只用
   **按类免轮**这一把刀，白名单类（doc-only 记账、纯指针更新，见 `7a918aa` ledger 批裁决）继续免，
   其余照旧 FULL。**诚实边界：FULL 自身的 token 成本从未被测量过**——169 万全是 pre-START audit。
3. **电池分档留**（无删除必要）；其 witness 数字按 `E3` 改正——已落本批。
4. **不加版本隔离的机械防线**；v1/v2 迟早整体移走，物理隔离在 repo 搬迁前意义有限。
   grep 实证纠正了一处误判：**v3 已自依赖**（`document_harness/` 对 `rsclib/harness` 零导入），
   两个"活读者"都是同代自读（v1 tooling 读 v1 contract、v2 tooling 读 v2 contract），整代搬走
   不断；**唯一的跨代活链是两个历史命令组**——`harness`（`rsc.py:48` 导入、`:707` 注册）与
   `stage`（`rsc.py:46` 导入、`:642` 起的子解析器块）。
   落法：rider `CLI-hist`（删，不是复制），随下一个碰 `rsc.py` 的批或拆分批兑付。
5. **不立退役常设规则**（目前零星修剪已够）。两点定性：**不需要额外记录**——先例 `c43a324`
   GUARD-DELETION-v1 删守卫时理由写在 commit 正文并经 FULL，commit 正文 + FULL 就是记录，
   新造"退役记录件"本身就是加组件；**判据不该是「持续没人用」**——守卫的正常态就是不 fire，
   沉默是它在工作；且要用「没人用」当判据就**必须先加 fire 计数**，那又是加组件。可用判据只有
   两条：**① 价值窗口关闭**（所守缺陷类已被上游结构性消除——`c43a324` 用的就是这条）·
   **② 被更强守卫覆盖**。一事一议。
6. **`E2` 补冻 `paragraph-map.schema.json`**——已落本批，采**再基线**形态（冻至 2026-08-03 的
   15 件，此后新入 pack 的文件仍不自动冻结），保留"新 schema 先稳定再冻"的原设计意图；
   备选的"删掉逃逸条款、整包自动冻"未采（会让新 schema 落地即冻、堵住迭代）。
   **该 amendment 改变了规则要求，按 `E10` 欠一次独立 read。**
   **⚠ errata（2026-08-03，同日 preclear 自查）**：本条原写作「已记 rider `E2-rb`，随下次 layer
   read 搭车（2026-07-30 bytes-channel 先例的同款处理）」——**错**。`E10` 的 rely-before-read 免路
   只给「不加条款、不改规则要求、对在飞轮次影响为零」的 amendment；本 amendment 改变了 `E2` 要求，
   **不在免路内**，read 是**前置条件**而非可搭车的欠账。bytes-channel 先例管的是 low finding 的
   字节套用，覆盖不到规则要求变更。已删 rider `E2-rb`，改记入 `HARNESS-LEDGER.md` 的 P5B 前置栏。
   **⚠ errata 2（2026-08-04）**：上面这条 errata 也只走到一半——read 已回（`c8d9afa`）并抛出
   must-fix M-1：`E10` 的 design 句要求这类 amendment **开轮**，读顶不了。该轮已补跑并结清
   （FULL `15b6e34` → 修 `c05d052` → VERIFY `3eb86bc`）。义务是**一个轮**，不是一次读。

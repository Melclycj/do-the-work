# C4 round journal — M11, the unit-map omission made mechanical

Construction log for Phase C4 of `harness-deletion-first-stabilization` (Step 8; the last
open defect-table row). Evidence lines below are pasted tool output from the executor
session of 2026-07-31, kept here per `E3` (the commit body stays one paragraph) and `E4`
(every new guard mutation-tested).

## Opening rulings (chat, 2026-07-31 — recorded here and in the candidate commit body)

1. Preview card rendered (`E11`), user replied **OK** — shape as carded, including the two
   carded defaults: the new schema carries `schema_version: "1"` from birth, and the
   skeleton generator is a separate template script `make_paragraph_map.py`.
2. cold read: **discharged by citation, no dispatch** (`E10` citation clause; C3
   precedent) — all eight member blobs at the opening HEAD equal the blobs
   `v3-checkpoint-read-784e49b.md` §1 tabulates; derivation pasted below.
3. M11 shape: **as the defect table wrote it** — paragraph skeleton (`instruction.py` new
   function, enumerate + sha256) → run-local `paragraph-map.json` (human fills only the
   classification column, enum `["obligation","context"]`, zero signed bytes) →
   cross-checker three-way; the existing `check_template_instance` gate is upgraded and
   its preamble check is retained un-loosened. The classification column lives on each
   enumerated entry — no parallel arrays (the ⑤ lesson, defect-table original text).
4. Riders: **none due.** C4 touches `instruction.py`, the run-v2 template, one README row
   and a new schema file; no rider names any of these (F-3r targets `rsc.py`, F-d/CT
   target `issues.py`, L-2li/L-1lr target `E10`/`E9`/`R10` clause text, F-4 waits for the
   first real run, F-c targets the digest-narrowing plan, SCC the stage-control contract,
   O-2b is E2-frozen, RA is the I/O design batch).
5. Instruction-layer touch is exactly one line: the `document-harness/README.md`
   enumeration row the README guard forces for the new schema stem. It rides `E10`'s
   deferral channel — it adds no new clause to any rule and its effect on every round in
   flight is nil; both facts recorded in the candidate commit body; the bytes (and the
   new schema's `description` strings, new governed prose joining the pack) ride the next
   read of this layer.

## Cold-read coverage derivation (`E10` citation clause, per member)

`git rev-parse HEAD:<path>` at the opening HEAD (`d5e8806`) for the eight members
`v3-checkpoint-read-784e49b.md` §1 tabulates — every blob identical to that read's table
(dff584d9 / bb84e6f2 / bd490c8b / d050b05a / 17ff31bb / 52a97a48 / 68031fa2 / e1a2f26b):

```
d5e8806fe1671f0baa7fad8208e583d261b03781
--- layer members at HEAD ---
dff584d9  ResearchSystem/document-harness/CONSTRUCTION-CHECKLIST.md
bb84e6f2  ResearchSystem/document-harness/README.md
bd490c8b  ResearchSystem/document-harness/EXECUTION.md
d050b05a  ResearchSystem/document-harness/REVIEW.md
17ff31bb  ResearchSystem/migration/document-work-assurance-v3/v3-harness-operating-contract.md
52a97a48  ResearchSystem/migration/document-work-assurance-v3/v3-harness-review-contract.md
68031fa2  ResearchSystem/contract/Document-Work-Assurance-Contract-v3-supersession-1.md
e1a2f26b  ResearchSystem/contract/Document-Work-Assurance-Contract-v3-supersession-2.md
```

Member-set staleness: `git diff --name-only 784e49b HEAD` lists six paths — ledgers,
riders, one closed plan, the read record itself and its journal — none under
`ResearchSystem/schema/` or `ResearchSystem/contract/`, and no new prose successor, so
the set is still exactly eight:

```
.goals/LEDGER.md
.goals/plans/harness-layer-incorporation-round.plan.md
ResearchSystem/HARNESS-LEDGER.md
ResearchSystem/HARNESS-RIDERS.md
ResearchSystem/document-harness/journal/layer-inc-2026-07-31.md
ResearchSystem/migration/document-work-assurance-v3/v3-checkpoint-read-784e49b.md
```

Every member is therefore covered by citing `v3-checkpoint-read-784e49b.md` (itself an
end-to-end read: five members in full at these blobs, three by verified citation to the
`d58969d` / `403fc9a` reads).

## Frozen surface at the opening HEAD (`E2`)

```
8ad404b1  .goals/plans/document-work-assurance-harness-v3.plan.md (signed plan)
b2dbdf75  ResearchSystem/contract/Document-Work-Assurance-Contract-v3.md
68031fa2  …supersession-1.md   e1a2f26b  …supersession-2.md   (member rows above)
--- schema pack diff since 11d147e ---
0 paths;  git ls-tree HEAD ResearchSystem/schema/document-assurance-v3/ -> 14 files
```

The user-locked oracle `expected-construction-prompt.txt` contains zero schema
references (`grep -c "schema"` → 0), so adding a pack file cannot touch it;
`test_readme_enumeration.py` needs no edit — its directory glob picks the new stem up
and the README row satisfies it.

## RED — the new assertions fail against the pre-fix bytes

`python test_run_v2_template_paragraphs.py -v` against the pre-C4 template — the three
defect instances fail **at value** (the old gate exits 0 over each; `AssertionError:
0 != 1`), the three negative controls are green, the pure-core and generator tests error
on the then-absent symbols:

```
test_a_covered_and_classified_run_passes ... ok                      (control)
test_a_missing_paragraph_map_is_refused ... FAIL                     (0 != 1)
test_a_stale_map_is_refused ... FAIL                                 (0 != 1)
test_an_obligation_paragraph_no_unit_anchors_is_refused ... FAIL     (0 != 1)  ← p3-corr class
test_an_obligation_paragraph_with_a_unit_anchor_passes ... ok        (control)
test_the_preamble_rule_is_retained_unloosened ... ok                 (retention control)
setUpClass (MakeParagraphMap) ... ERROR            (make_paragraph_map.py absent)
test_a_dangling_unit_anchor_is_reported ... ERROR  (paragraph_map_issues absent)
test_a_dropped_entry_is_stale ... ERROR
test_a_map_bound_to_another_instruction_is_refused ... ERROR
test_a_stale_map_suppresses_coverage_judgments ... ERROR
test_an_entry_with_no_classification_is_refused_not_defaulted ... ERROR
test_the_covered_instance_is_clean ... ERROR
Ran 12 tests in 0.198s
FAILED (failures=3, errors=7)
```

`python test_instruction_paragraphs.py` — reachability red only (the module under test
does not yet export the function; its behavioural binding is proven by the hand-literal
expectations going green plus the mutation probes below):

```
ImportError: cannot import name 'paragraph_skeleton' from
'rsclib.document_harness.instruction' (…/rsclib/document_harness/instruction.py)
```

## GREEN — after the fix, on the exact bytes being committed

Both new files green (`Ran 12 … OK` / `Ran 14 … OK`), then the five suites and the
audit:

```
tests => tests: 29   passed: 29   failed: 0  RESULT: OK (compiler + shadow lint behave to contract)
tests/stage_control => stage-control deterministic matrix: 20 run, 0 failure(s), 0 error(s)
tests/harness => Ran 39 tests in 2.805s OK
tests/document_harness => Ran 181 tests in 27.012s OK   (169 before this round; +12, all authored here)
tests/document_harness_review => Ran 368 tests in 55.808s OK   (354 before; +14, all authored here)
repo-audit exit: 0
```

(The `document_harness` line comes from a standalone re-run with exit code captured —
`suite exit: 0`; in the batched first run its summary was overprinted by
`test_precommit_checks`' own stdout, a test's print, not a hook firing.)

Frozen surface on the working tree, immediately before the candidate commit:

```
--- four frozen blobs at HEAD ---
8ad404b1   b2dbdf75   68031fa2   e1a2f26b
--- pack tracked-file modifications (must be empty) ---
(empty)
--- porcelain over pack+contract (expect exactly the one new ?? file) ---
?? ResearchSystem/schema/document-assurance-v3/paragraph-map.schema.json
--- two user-locked oracles (must be empty) ---
(empty)
```

## Mutation probes (`E4`) — neuter → value-level red → restore from sha256-checked copies

Reference snapshots (post-fix bytes), taken before any probe; each file restored from
its copy after every probe and re-hashed each time:

```
72e5df942a89ef202cd39578e67cbb4aaec2732ff145b4e2d49e77e3870dcc74 *check_template_instance.py (= gate.orig.py)
fd34bdb2be489d43f598c1801eb716ef52afa79cd119d46d860e50a003d5775c *instruction.py (= instruction.orig.py)
```

| probe | neutered thing (defect shape) | result |
|---|---|---|
| P1 | Leg 2 obligation-coverage branch — the reported M11 shape (omission invisible again) | **1 value FAIL** (the p3-corr headline test; every control stays green) |
| P2 | Leg 1 skeleton comparison (count + per-field) — stale maps pass | **3 value FAILs** (stale-map main-level, dropped-entry, stale-suppresses-coverage) |
| P3 | Leg 3 dangling-anchor branch | **1 value FAIL** (dangling test) |
| P4 | classification-missing refusal → silent default to context (the M8 shape) | **1 value FAIL** (refused-not-defaulted test) |
| P5 | main's missing-map refusal deleted | **1 value FAIL** (missing-map test, 0 != 1) |
| P6 | `_fence_run` call → `None` (fences treated as ordinary lines) | **3 value FAILs** (backtick, tilde-length, unclosed-fence tests) |

6/6 probes bind, zero ERRORs, every failure on a value. After each restore the file hash
equals its snapshot; final state re-verified green (12/12 + 14/14 within the suite runs
above).

## Disclosures for the FULL (read before crediting anything above)

1. **Instruction-layer touch is one README row** (`document-harness/README.md`,
   paragraph-map enumeration line) riding `E10`'s deferral channel — it adds no new
   clause to any rule and its effect on every round in flight is nil; both facts are in
   the candidate commit body; the bytes, and the new schema's `description` strings
   (new governed prose joining the pack), ride the next read of this layer.
2. **`paragraph_skeleton`'s RED was reachability-only** (ImportError — the function did
   not exist). Its behavioural binding comes from the hand-literal expectations going
   green plus probe P6 — the same class C1 disclosed for M3.
3. **The gate double-covers unclassified entries**: `main` schema-validates the map
   (`validate("paragraph_map", …)`) and the pure core refuses missing classification
   independently. Deliberate M8 mirror — the pure core must not depend on callers
   having validated; frozen run-dir copies of this script outlive rsclib versions.
4. **The pinned-revision read branch is untested** in both template scripts: the
   main-level tests exercise the worktree fallback only (temp dirs are not git repos).
   Same coverage class as the pre-existing gate `main`; the fallback path prints which
   tree it read, per the R1 discipline.
5. **Frozen run-dir copies untouched**: `runs/w1-r1/` and `runs/p3-corr/` keep their
   own `check_template_instance.py` per the 2026-07-28 ruling (38 frozen scripts); the
   upgrade governs future instantiations of the template only.
6. **`pack_digests()` output changes** for future evidence (the pack gained a file);
   nothing pins the old value — the five suites are green — and closed runs' recorded
   pack digests are historical bindings, not re-derived.

## Closeout (2026-07-31)

FULL record `v3-review-full-d50d9e5.md` committed `ce5196c`: **REVIEWED_NO_BLOCKER** —
zero blockers, zero lows, repair and VERIFY budget untouched. The reviewer reproduced
the RED at value against the pre-C4 bytes (the one-error delta vs. this journal's RED
is its worktree keeping the maker), ran four probes of its own from snapshots
hash-identical to this journal's, and exercised the pinned-revision read branch live —
upgrading disclosure 4's coverage claim. Dispositions:

- **W-1** (wording-level, `R9`): the schema root description says "over each block's
  UTF-8 bytes" where the actual rule is SHA-256 over the block's `\n`-joined lines
  (CRLF-normalized, no trailing newline; precise in `instruction.py`'s docstring).
  Machine sides compute identically — no actor's action changes. Rides the next batch
  touching the layer, and the ride is already scheduled: these very description strings
  owe the next layer read under `E10`'s deferral. No row, no round.
- **O-1** → the user's design slot (ledger open-question line): should the gate also
  flag a classification disagreement between the paragraph map and the unit map? Today
  any anchoring unit counts as coverage — the defect row's literal wording, inside the
  明令不修 ceiling (the paragraph is then present in the unit map and the disagreement
  is visible to FULL review). `R5`: the question is the user's.
- **O-2** (anchor substring semantics, inherited from W2-A5 unchanged) — recorded, no
  action.
- **O-3** (component-accumulation shape note) → added as a data point to the standing
  保障面二期复盘 agenda in the ledger backlog.

Round chain: card OK → cold read by citation (zero budget) → candidate `d50d9e5` →
FULL `ce5196c` → closeout. **M1–M11 all closed; next is plan Step 9, Phase D.**

# C2 round journal — flow/summary bindings (M5 + M6 + M7)

Construction log for Phase C2 of `harness-deletion-first-stabilization`. Evidence lines
below are pasted tool output from the executor session of 2026-07-30, kept here per `E3`
(the commit body stays one paragraph) and `E4` (every new guard mutation-tested).

## Opening rulings (chat, 2026-07-30 — recorded here and in the candidate commit body)

1. cold read: **dispatch** (not waived) — record `v3-cold-read-ae4df09.md`, committed `853fe4c`;
   it also discharged the `a6b87ad` in-layer application debt.
2. M6 shape: **option B** — `review_refs` content-bound against the in-hand `reviews` by
   canonical digest; the six evidence/spec refs stay stored-without-content-check
   (supersession-2 narrowed state digests to the five protected fields, so no
   independently-authored digest is in hand to check them against).
3. M7 shape: **confirmed as the defect table wrote it** — `generate_summary` refuses a
   `decision_ref` that does not bind the FINAL decision in hand; `check_summary` names a
   `final_decision_ref` that binds different decision bytes. Digest kind: canonical
   (w1-r1 authoring precedent, `runs/w1-r1/run_final.py:87-90`; same kind as the existing
   candidate binding in `check_summary`).

M5 needed no ruling (ledger: unaffected by C1.5; binding rides commit SHA, not digests).

## RED — the new assertions fail against the pre-fix modules

`python ResearchSystem/tooling/tests/document_harness_review/run_tests.py` at the pre-fix
modules: `Ran 338 tests` → `FAILED (failures=10)`, and the ten are exactly this round's
new assertions (grep `^FAIL:` of that run):

```
FAIL: test_a_decision_naming_no_run_is_reported_unverified (RepairDecisionBinding)
FAIL: test_a_no_repair_answering_another_runs_review_is_named (RepairDecisionBinding)
FAIL: test_a_no_repair_bound_to_a_different_candidate_is_named (RepairDecisionBinding)
FAIL: test_a_review_ref_binding_bytes_no_review_has_is_named (AssuranceCandidateIsFaithful)
FAIL: test_a_stale_round_zero_ref_in_a_repaired_run_is_named (AssuranceCandidateIsFaithful)
FAIL: test_a_summary_citing_a_decision_it_was_not_generated_from_is_named (SummaryIsTheUsersDecision)
FAIL: test_a_v2_no_repair_bound_to_the_wrong_candidate_is_still_named (RepairDecisionBindingAcrossResultVersions)
FAIL: test_an_apply_answering_another_works_review_is_named (RepairDecisionBinding)
FAIL: test_generate_refuses_a_decision_ref_binding_other_bytes (SummaryIsTheUsersDecision)
FAIL: test_the_module_level_codes_that_do_fire_are_reachable_with_valid_documents (EnforcementLayerIsPinned)
```

Zero pre-existing tests failed in that run: 328 green, 10 red, all ten authored this round.

## GREEN — after the fixes, on the exact bytes being committed

```
tests => tests: 29   passed: 29   failed: 0 RESULT: OK (compiler + shadow lint behave to contract)
tests/stage_control => Ran 20 tests in 5.870s OK stage-control deterministic matrix: 20 run, 0 failure(s), 0 error(s)
tests/harness => Ran 39 tests in 2.647s OK
tests/document_harness => Ran 169 tests in 26.045s OK
tests/document_harness_review => Ran 338 tests in 55.644s OK
repo-audit exit: 0
```

## Mutation probes (`E4`) — neuter → value-level red → restore from sha256-checked copies

Reference snapshots (post-fix bytes), taken before any probe:

```
431f351df4b13cad88cbf04b09c6b4865bc554ed1247d23add3817a7744336d0 *flow.py (= flow.py.orig)
d09439ebf28d17ff6d9ab8d1d874830861242e276341f9d2a8dae9ba362f221a *summary.py (= summary.py.orig)
```

| # | guard | mutation (defect shape restored) | targeted run | result |
|---|---|---|---|---|
| 1 | M5 binding-before-decline | early `if decision["decision"] == "NO_REPAIR": return schema_report` reinserted ahead of the binding checks | 3 NO_REPAIR binding tests | `FAILED (failures=3)`, each value-level: `[] != [('V3-FLOW-REPAIR-WRONG-CANDIDATE', 'target/candidate_ref')]` etc. |
| 2 | M6 review content binding | `bound_digests = set(known)` — content check degenerates to count-only | 2 review_refs content tests | `FAILED (failures=2)`, value-level: `[] != [REVIEW-UNBOUND, REVIEW-INVENTED]` |
| 3 | M7 generate refusal | `if False:` on the `decision_ref` comparison in `generate_summary` | `test_generate_refuses_a_decision_ref_binding_other_bytes` | `FAILED (failures=1)`: `AssertionError: SpecGap not raised` |
| 4 | M7 check comparison | `if False:` on the `final_decision_ref` comparison in `check_summary` | `test_a_summary_citing_a_decision_it_was_not_generated_from_is_named` | `FAILED (failures=1)`, value-level: `[] != [('V3-ASSURANCE-DECISION-BINDING-MISMATCH', 'final_decision_ref/digest_sha256')]` |

After each probe the module was restored by copying the scratchpad snapshot back (never
`git checkout --`) and re-hashed; final state of both files re-verified equal to the
snapshots above, and the GREEN block was produced on those restored bytes.

## Deliberate non-coverage, recorded

- The six evidence/spec refs of the AssuranceCandidate (`work_spec` / `resolved_plan` /
  `instruction_audit` / `fulfillment` / `manifest` / `coverage`, plus `check_result_refs`)
  remain stored-without-content-check — M6 option B, the user's 2026-07-30 ruling. The code
  comment at the check site carries the reason.
- Test-fixture upgrade was load-bearing, not cosmetic: `make_summary` / `make_candidate` /
  the KnownDefects generate call carried junk digests (`dig(0x17)`, `dig(0x21)`), which the
  new enforcement would refuse wholesale; they now compute real canonical digests
  (`review_refs_for(...)`, `flow.decision_digest(...)`). `test_fix_round_locks.py`'s local
  review/decision fixtures gained the `work_id`/`run_id` the new binding checks compare.
- `tests/document_harness_review/test_run_v2_template_fulfillment.py` untouched — the C0
  residual low stays parked for a round that touches that file.

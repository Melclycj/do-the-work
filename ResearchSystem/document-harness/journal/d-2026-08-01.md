# Phase D round journal — historical-layer exit + memory migration recheck

Construction log for Phase D of `harness-deletion-first-stabilization` (Step 9). Evidence
lines below are pasted tool output from the executor session of 2026-08-01, kept here per
`E3` (the commit body stays one paragraph) and `E4` (every new guard mutation-tested).
Scope executed: rsc.py `stage`/`harness` historical markers · rider redemptions CT + F-d +
F-3r · channel ISSUE_TRIAGE decision · memory-atom migration recheck (user-tightened, see
ruling ③) · checklist `E1` session-role sentence · W-1 wording fix. Burden ISSUE_TRIAGE
deliberately not done — plan Step 9 leaves it for Phase E's material.

## Opening rulings (chat, 2026-08-01 — recorded here and in the candidate commit body)

1. Preview card rendered (`E11`); the user ruled the card's four open items one by one
   (①–④ below) — that per-item reply is the approval this round runs under.
2. **① Opening cold read: waived by the user.** Seven of the eight member blobs at the
   opening HEAD equal `v3-checkpoint-read-784e49b.md` §1's table; the eighth
   (`document-harness/README.md`, `bb84e6f2` → `f3a31208`) changed in exactly the C4
   enumeration row whose read already rides `E10`'s deferral channel (recorded in
   `d50d9e5`'s body and verified by the C4 FULL §5). Phase D does not rely on that row.
   The round's **closing layer read** covers, in one dispatch: the C4 deferred bytes
   (README row + `paragraph-map.schema.json` description strings), this round's `E1`
   amendment, and the W-1 wording fix.
3. **② Rider CT redeemed this round** (F-d same car). The bank row's deadline said
   "最晚 Phase E 的 ISSUE_TRIAGE 前", but the defect bites at any triage check over a
   multi-issue run — and Phase D's own channel triage over p3-corr (8 issues, one
   work_id) is exactly the recorded scenario. F-3r redeemed on its written trigger (this
   batch touches `rsc.py`).
4. **③ Session-role habit becomes layer text; memory keeps nothing.** One sentence added
   to `E1` (one session, one role, for the session's whole life; work out the role at
   start; a request belonging to the other role is flagged for the user to route, never
   absorbed). The user tightened the carded shape: *"memory 不算 harness 的一部分 — link
   也不要留"* — so the memory atom is deleted outright, not downgraded to a pointer, and
   the same ruling is applied to the only other pure-harness memory file
   (`v3-harness-operating-contract`, itself a self-described non-authoritative pointer).
   Full texts of both preserved in §Memory below before deletion.
5. **④ Channel triage route = CORE_CANDIDATE**, decided by the user via the card reply;
   rationale in the decision file points at Phase A's channel freeze (`R6`) and plan
   Step 9.

## Layer and frozen surface at the opening HEAD (`83e88fd`)

```
=== 8 member blobs at HEAD ===
dff584d9  ResearchSystem/document-harness/CONSTRUCTION-CHECKLIST.md
f3a31208  ResearchSystem/document-harness/README.md          <- changed by d50d9e5 (C4, deferred)
bd490c8b  ResearchSystem/document-harness/EXECUTION.md
d050b05a  ResearchSystem/document-harness/REVIEW.md
17ff31bb  ResearchSystem/migration/document-work-assurance-v3/v3-harness-operating-contract.md
52a97a48  ResearchSystem/migration/document-work-assurance-v3/v3-harness-review-contract.md
68031fa2  ResearchSystem/contract/Document-Work-Assurance-Contract-v3-supersession-1.md
e1a2f26b  ResearchSystem/contract/Document-Work-Assurance-Contract-v3-supersession-2.md
=== E2 frozen blobs ===
100644 blob 8ad404b12b3242e700d0ad215048dffccada7d9c  .goals/plans/document-work-assurance-harness-v3.plan.md
b2dbdf75  ResearchSystem/contract/Document-Work-Assurance-Contract-v3.md
=== README last change ===
d50d9e5 V3-PHASE-C4-M11-v1
=== pack file count ===
15
```

Post-C4 commits (`ce5196c` record, `12fe096` closeout, `83e88fd` ruling) touched ledgers,
journal and the review record only — no member, so the seven-blob citation to the
`784e49b` read holds and the README delta is exactly the recorded deferral.

## CT + F-d — `check_triage` binds the target to the in-hand issue

**Defect reproduced first** (rider CT): `check_triage` validated phase, route, non-empty
target and `work_id` — but never compared WHICH issue the target names against the issue
in hand, so with several issues on one work a decision about a sibling reported clean.

RED — test-side first (fixture default aligned `control/issue.json` → `control/hi-one.json`;
two new tests; reachability enumeration gains the new code), against pre-fix `issues.py`:

```
FAIL: test_the_module_level_codes_that_do_fire_are_reachable_with_valid_documents
AssertionError: Lists differ: ['V3-HARNESS-ISSUE-TARGET-MISMATCH'] != []
FAIL: test_a_triage_pointing_at_a_sibling_issue_of_the_same_work_is_named
AssertionError: Lists differ: [] != [('V3-HARNESS-ISSUE-TARGET-MISMATCH', 'target/harness_issue_ref/path')]
FAIL: test_an_issue_with_no_identity_cannot_be_bound_to_any_target
AssertionError: Lists differ: [] != [('V3-HARNESS-ISSUE-TARGET-MISMATCH', 'target/harness_issue_ref/path')]
Ran 131 tests in 0.247s
FAILED (failures=3)
```

Three value-level failures, zero errors, the other 128 green — the fixture alignment broke
nothing. Fix: the target's filename is compared to `<issue_id>.json` (no new I/O — the
M6 discipline); an issue with no `issue_id` cannot be bound and reports the same code.
GREEN: `Ran 131 tests ... OK`. F-d same car: `record_issue`'s docstring verb corrected
("Build one observation (the caller persists it)") — the function builds and returns,
never writes.

## F-3r — the dispatch PRODUCER writes `.harness/review-pending.json`

New CLI-level suite `test_dispatch_freeze_marker.py` (subprocess against a disposable
git repo, the `test_review_cli_v2_subject.py` pattern): a successful
`rsc v3 dispatch --range` writes the marker with kind `construction-round` and the
resolved `BASE..TIP` as subject; a failed dispatch (empty range, exit 1) writes nothing.
Green on the landed bytes — the behaviour pre-exists; this rider was a missing TEST, so
its binding force is proven by probe M-B below, the C1/M3 precedent class
(green-born guard, force shown by mutation).

```
Ran 2 tests in 1.062s
OK
```

## Channel ISSUE_TRIAGE (ruling ④)

`user-decision-triage-review-record-channel-unspecified.json` written to
`assurance/runs/p3-corr/issues/` (compact JSON, sorted keys, no trailing newline — the
byte style of the seven existing decisions; target ref carries the issue file's sha256
`d38e72a8c207dac145134e21eb3e3358167e4a8704982d5e45df7a24ad91b8be`). `check_triage` run
over every decision–issue pair in the run — now under the new TARGET-MISMATCH guard, so
"clean" provably means "this decision routes this issue":

```
user-decision-triage-command-exit-subject-tree.json -> clean
user-decision-triage-digest-binds-nothing-against-the-only-writer.json -> clean
user-decision-triage-harness-knowledge-in-memory.json -> clean
user-decision-triage-no-dispatch-generator.json -> clean
user-decision-triage-no-vocabulary-for-repaired-blocker.json -> clean
user-decision-triage-review-record-channel-unspecified.json -> clean
user-decision-triage-template-next-action-round-blind.json -> clean
user-decision-triage-template-write-text-newline.json -> clean
```

Real-data negative control — the new decision checked against a sibling issue of the
same work:

```
cross-pair (channel decision vs knowledge-in-memory issue) -> ['V3-HARNESS-ISSUE-TARGET-MISMATCH']
```

Of the run's three untriaged issues at opening, two remain by design: burden waits for
Phase E's material (plan Step 10); `knowledge-in-memory`'s decision was already in the
repository (2026-07-28).

## Historical markers (`stage` / `harness`)

Help text and module docstrings only — `rsc.py` module docstring (both mode entries),
the `stage` parser help, `rsclib/harness/cli.py`'s docstring and register help. No
behaviour change, no code or test deleted; both legacy suites green immediately after:

```
tests/harness      Ran 39 tests in 3.863s  OK
tests/stage_control  stage-control deterministic matrix: 20 run, 0 failure(s), 0 error(s)
rsc --help now renders:
    stage    [historical] deterministic Stage Control operations (superseded by v3; kept
             to replay existing Stage Records)
    harness  [historical] General Harness v2 (superseded by v3; kept to replay existing
             v2 records)
```

## Instruction-layer touches this round (`E10` facts for the FULL)

1. **`E1` gains one sentence** (ruling ③) — session-role separation. This **adds a
   clause to a rule**, so it is design under `E10` and correctly runs inside a carded
   round; its independent read is the round's closing layer read (never banked as the
   FULL). Until that read lands, no round relies on it (this round's own conduct is
   governed by the pre-amendment text plus the standing memory habit it codifies).
2. **W-1 wording fix** in `paragraph-map.schema.json`'s root description ("UTF-8 bytes"
   → lines joined with `"\n"`, UTF-8 encoded, line endings normalized, no trailing
   newline — now matching `instruction.py`'s docstring). Wording-level under `R9`; the
   C4 FULL scheduled exactly this ride ("fix the wording when that read lands"). The
   file is the 15th pack member, post-2026-07-29, not E2-frozen; schema still parses
   (verified by load + the suites).
3. The closing layer read therefore covers: the C4-deferred README row + schema
   description strings (now including this fix), and the new `E1` sentence.

## Mutation probes (`E4`) — neuter → value-level red → restore from sha256-checked copies

Reference snapshots (post-fix bytes) in the session scratchpad, hashes recorded before
any probe; each file restored from its copy and re-hashed after its probe:

```
3ba42ffc397fe06c6d92e75543f244ddb278326e308fa5a4ac9e49395b625734 *issues.orig.py
a33f445d74f8a6a7ec07ba6eeff9cf777dcbceae5e5b54f31a4f7d72628a9710 *rsc.orig.py
```

| probe | neutered thing (defect shape) | result |
|---|---|---|
| M-A | `check_triage` target↔issue comparison → `if False:` (the CT shape: sibling triage reads as this issue's routing) | **3 value FAILs** — both new tests + the reachability enumeration; zero errors |
| M-B | `rsc.py` freeze-marker write block → `if False:` (dispatch silently stops holding E9's window) | **1 value FAIL** — "a successful dispatch left no freeze marker"; the failed-dispatch negative control stays green |

After each restore the file hash equals its snapshot (pasted in-session:
`3ba42ffc…` / `a33f445d…`); both suites re-ran green after restore (`Ran 131 … OK`,
`Ran 2 … OK`). One process slip disclosed: probe M-A's restore first ran with a wrong
relative path (cwd had moved) and failed; the neutered file sat on disk for the
duration of one failed command, was then restored by absolute path, and the hash check
above is of the actual restored bytes. No commit happened in between; the committed
bytes are the snapshot bytes.

## Memory disposition (ruling ③ — the Step 9 recheck, user-tightened)

Adjudication of every harness-related atom in
`C:\Users\j3236\.claude\projects\D--Thesis\memory\` (the local Claude memory this
repository's sessions load). Verification of "already migrated" is against the current
checklist text read this session, not from recollection:

| atom | finding | action |
|---|---|---|
| `v3-review-craft-lessons`, `feedback-prose-claims-derive-or-omit`, `feedback-derivability-is-not-a-reason`, `feedback-guard-expectation-independence`, `v3-review-priority-implementation-correctness` | absent from the directory (migrated by B2); owners verified in layer: `E3` derive-or-omit + enumeration arm, `E6` derivability gate, `E5`+`E4` guard-expectation independence + mutation, `R3` implementation-first | none — confirmed done |
| `v3-session-role-separation` | the one remaining operative habit, memory-only | rule → `E1` sentence; **file deleted, no pointer** (ruling ③) |
| `v3-harness-operating-contract` | pure harness pointer atom (self-described non-authoritative; stale `R1–R9`) | **file deleted** under the same ruling — memory is not part of the harness |
| `feedback-subjects-before-argument` | core moved 2026-07-28 to global CLAUDE.md §0.5 (a wider home than this harness); file keeps provenance + 3 residues the user deliberately did NOT globalise | stays — preference/provenance, the class the plan's acceptance keeps |
| `feedback-concise-reporting` | subsumed 2026-07-28 into global §0.5 clause-by-clause; file is the V3-N2 incident record | stays — provenance |
| `feedback-thesis-change-impact-pass`, `goals-ledger-entrypoint`, remaining atoms | thesis-side / repo-navigation, not harness-construction material | out of Phase D's scope |

Plan acceptance line "memory 里不再有 operative harness 指令（preference 类保留）" is
reached: after the two deletions, no memory file carries harness-operative content.
`MEMORY.md` index rows for the two deleted files removed in the same pass. The deletion
itself happens outside the repository, where no instrument here can witness it — the
same custody property the knowledge-in-memory issue named; this journal is the record.

### Full text preserved before deletion — `v3-session-role-separation.md`

```markdown
---
name: v3-session-role-separation
description: "Harness v3 runs one session per role — work out which role this session holds, and flag a mis-addressed request instead of executing it; the user routes"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: b8982193-67d2-4c14-9217-672b09a1cf38
  modified: 2026-07-27T19:44:13.231Z
---

Harness v3 work runs in **two sessions with distinct roles**, and the user carries dispatches between
them by hand. The role boundary itself is repo-owned — `E1` and `R1` of the file
[[v3-harness-operating-contract]] points at — and is deliberately not restated here.

**What this atom carries is the collaboration habit, not the rule:** at session start in this repo,
work out which role this session holds (the shape of what the user pastes usually tells; ask if it is
ambiguous). If the user hands this session work belonging to the other role, **flag the mis-address
before executing it** and let them route it. The failure mode is silently absorbing the other role's
work, never a dispute about where the boundary lies.

Related: [[goals-ledger-entrypoint]].
```

### Full text preserved before deletion — `v3-harness-operating-contract.md`

```markdown
---
name: v3-harness-operating-contract
description: "Pointer only, NON-AUTHORITATIVE — harness v3's construction rules live in the repo at ResearchSystem/document-harness/CONSTRUCTION-CHECKLIST.md, never in memory"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 44deb119-df9d-44ed-b3fc-b6fc4b45a886
  modified: 2026-07-27T19:44:08.534Z
---

**Non-authoritative pointer — nothing in this file governs anything.** It exists so a cold session
knows where to look.

Harness v3's construction rules live **in the repository**, in one file:
`D:\Thesis-stage-control-refactor\ResearchSystem\document-harness\CONSTRUCTION-CHECKLIST.md` —
execution side `E1`–`E12`, review side `R1`–`R9`, one file carrying both roles, and each role reads
all of it. Where that file is silent on a question a round actually faces, the two contracts it
superseded are the reference of record; their old paths under
`ResearchSystem/migration/document-work-assurance-v3/` now hold supersession stubs naming the commit
that carries the full text.

**Why none of it is copied here:** rules governing repository work must be version-controlled —
bindable by blob, reviewable by commit, readable by the independent review side. A memory copy is a
second copy of the same rules, and duplication drifting apart is the defect this harness exists to
prevent. Read the file; never restate it here.

Related: [[project-thesis-department-redesign]], [[goals-ledger-entrypoint]].
```

## Riders after this batch

Redeemed and deleted in this commit: **CT**, **F-d**, **F-3r**. Remaining seven rows
checked against this batch's surfaces: F-4 (first real run — Phase E), F-c
(digest-narrowing plan, untouched), O-2b (E2-frozen), SCC (stage-control contract file,
untouched — the historical markers live in rsc.py/cli.py, not the contract), RA (I/O
design batch), L-2li (`E10` deferral clause text, untouched — the `E1` edit is a
different rule), L-1lr (`E9` budget / `R10` routing sentences, untouched). None else due.

## Disclosures for the FULL (read before crediting anything above)

1. The `E1` sentence is a **design amendment relied on by no round yet**; its
   independent read is this round's closing layer read, dispatched after the FULL. If
   the FULL finds the sentence itself defective, the fix leg is open — nothing has
   relied on it.
2. `make_triage`'s fixture default changed (`control/issue.json` → `control/hi-one.json`)
   to state what well-formed means under the new guard; it was the only use of the old
   literal, and the pre-fix suite ran green with the new default before the guard landed
   (the RED shows exactly 3 failures, all new assertions).
3. The F-3r suite was **born green** (the producer behaviour pre-exists); its force is
   probe M-B, not a RED — the C1/M3 precedent class.
4. The two memory deletions are **unwitnessable from the repository**; this journal's
   §Memory carries the full pre-deletion texts, and the MEMORY.md index edit is equally
   outside the repo.
5. `stage`/`harness` marker edits change `--help` text (user-visible) but no operation,
   exit code, or output of any operation; both legacy suites plus the full matrix ran
   green after.
6. Probe M-A's first restore attempt used a stale relative cwd and failed; disclosed
   above with the recovery and the hash proof.

## Suites and frozen surface on the exact bytes being committed

Pasted from the pre-commit verification run of 2026-08-01 (each suite via its own
`run_tests.py`; counts grep-extracted from the runs' own summaries):

```
tests                         => tests: 29   passed: 29   failed: 0
                                 RESULT: OK (compiler + shadow lint behave to contract)
tests/stage_control           => stage-control deterministic matrix: 20 run, 0 failure(s), 0 error(s)
tests/harness                 => Ran 39 tests in 3.635s  OK
tests/document_harness        => Ran 181 tests in 30.142s  OK   suite4 exit: 0
tests/document_harness_review => Ran 372 tests in 61.544s  OK   suite5 exit: 0
                                 (368 before this round; +2 triage guard, +2 freeze marker)
repo-audit exit: 0
=== four frozen blobs at HEAD ===
8ad404b1 b2dbdf75 68031fa2 e1a2f26b
=== pack committed diff since 11d147e ===
A	ResearchSystem/schema/document-assurance-v3/paragraph-map.schema.json
=== pack worktree porcelain ===
 M ResearchSystem/schema/document-assurance-v3/paragraph-map.schema.json      (W-1, 15th file, not frozen)
=== oracles porcelain (expect empty) ===
(empty)
=== full worktree porcelain — exactly this round's ten paths ===
 M ResearchSystem/HARNESS-RIDERS.md
 M ResearchSystem/document-harness/CONSTRUCTION-CHECKLIST.md
 M ResearchSystem/schema/document-assurance-v3/paragraph-map.schema.json
 M ResearchSystem/tooling/rsc.py
 M ResearchSystem/tooling/rsclib/document_harness/issues.py
 M ResearchSystem/tooling/rsclib/harness/cli.py
 M ResearchSystem/tooling/tests/document_harness_review/test_flow_repair_disposition.py
?? ResearchSystem/assurance/runs/p3-corr/issues/user-decision-triage-review-record-channel-unspecified.json
?? ResearchSystem/document-harness/journal/d-2026-08-01.md
?? ResearchSystem/tooling/tests/document_harness_review/test_dispatch_freeze_marker.py
```

## L-2 applied via the byte channel (post-FULL, 2026-08-01)

The FULL (`v3-review-full-34cf85b.md`, `REVIEWED_NO_BLOCKER`) left L-2 non-blocking with
a named minimum fix: compare `decision.run_id` ↔ `issue.run_id` when both documents carry
one, plus one test. Put to the user per `R10`; the user ruled the fix is done **now,
incidentally, without the fix leg** ("这不是非常小的修改吗…顺带一起做"). Channel: the
2026-07-30 **(a)** ruling's named-content arm (record 点名内容的 low 即时套用，层内外一致)
— applied immediately, spends no budget, obliges **no VERIFY** (`E10`'s free channel; the
`L-1lr` rider documents exactly this against the phantom-VERIFY misreading of `E9`
alone). Reported after the fact, reversible. Nothing banks — the debt is paid in the same
batch it was found, so no rider row is created.

RED first (guard absent), then green, then probe:

```
FAIL: test_the_module_level_codes_that_do_fire_are_reachable_with_valid_documents
AssertionError: Lists differ: ['V3-HARNESS-ISSUE-TRIAGE-RUN-MISMATCH'] != []
FAIL: test_a_triage_from_another_run_of_the_same_work_is_named
AssertionError: Lists differ: [] != [('V3-HARNESS-ISSUE-TRIAGE-RUN-MISMATCH', 'run_id')]
Ran 132 tests in 0.190s
FAILED (failures=2)
--- fix applied (issues.py: run comparison when both sides carry run_id) ---
Ran 132 tests ... OK
```

Probe M-C (`E4`): post-fix snapshot
`9a1e066d734ec8553affea4997fe2ad52484f0e0e9f7022bd392d6fc69101ee5 *issues.l2.orig.py`;
comparison neutered to `if False:` → **2 value FAILs** (the new test + the reachability
enumeration); restored from the snapshot, file hash re-verified equal, suite `OK`.

Real-data sweep re-run under the new guard — all 8 decision–issue pairs still clean;
synthetic cross-run control (the channel decision with `run_id` altered to
`p3-corr-second-run`, checked against its own issue) fires:

```
8 × user-decision-triage-*.json -> clean
synthetic cross-run control -> ['V3-HARNESS-ISSUE-TRIAGE-RUN-MISMATCH']
```

Suites after the application: `tests` OK · stage-control `20 run, 0 failure(s)` ·
harness `Ran 39` · document_harness `Ran 181 … OK` · document_harness_review
`Ran 373 … OK` (+1, the new test) · repo-audit exit 0 (one earlier `exit 2` in this
session was the runner invoked from the wrong cwd — the script path did not resolve;
not an audit result). The well-formed-pair fixtures already carry matching `run_id`s, so
the existing clean-baseline test doubles as this guard's negative control.

## Closing-read L-1 applied via the byte channel (post-read, 2026-08-01)

`v3-checkpoint-read-d01615b.md` (0 must-fix; record committed `b22eca4`) supplied L-1's
exact bytes: `layer_path_check.py`'s `LAYER` tuple claims to mirror `E10`'s membership
sentence, but the sentence's open tail became non-empty when `34cf85b` amended the
paragraph-map schema's `description` strings — the mirror was a proper subset, so a
future broken path written into those strings would land without the advisory backstop
(the `dcced4e`/`d322816` class). Applied as supplied, the mirror claim standing as
written; code side, outside every frozen surface; reversible if the user instead rules
`LAYER` a deliberately fixed enumeration.

RED first — the hand-written membership fixture (`test_precommit_checks.py
LayerMembership.EXPECTED`) gained the ninth path before the module did, and the
must-fire loop demonstrated the defect live (the hook does not scan the schema file):

```
FAIL: test_every_member_is_scanned (member='ResearchSystem/schema/document-assurance-v3/paragraph-map.schema.json')
AssertionError: 0 != 1
FAIL: test_layer_equals_the_hand_written_membership
AssertionError: Tuples differ: ('Res[523 chars]2.md') != ('Res[523 chars]2.md', 'ResearchSystem/schema/document-assuran[28 chars]son')
Ran 18 tests in 5.090s
FAILED (failures=2)
--- one line added to LAYER ---
Ran 18 tests in 6.564s
OK
```

The red→green pair is itself the mutation evidence here: the pre-fix state IS the
neutered state (member absent → scan silently skipped, `0 != 1` at value), and the
must-fire subtest now reaches the ninth member exactly as it reaches the other eight.
`document_harness` suite re-run standalone: `Ran 181 … OK`. No rider row — the debt is
paid in the batch that found it.

## Closeout (2026-08-01)

Round chain: card ①–④ ruled per item → candidate `34cf85b` → FULL `766fe02`
**REVIEWED_NO_BLOCKER** (0 blockers; budget virgin) → FULL L-2 applied via the (a)
channel at `d01615b` (user ruling: incidental, no fix leg, no VERIFY) → closing layer
read `b22eca4` (0 must-fix; five obligations discharged in one dispatch; the layer
member table is **nine entries** from here on, read O-1) → read L-1 applied via the
same channel at `ace0845` → this closeout. **Fix and VERIFY budget never spent.** No
rider row created or left due by this round: three redeemed at the candidate (CT, F-d,
F-3r), seven remain, none touched since.

Dispositions:
- **FULL L-1** (9 vs 8 issue count, wording): accepted as dispositioned — no action;
  the FULL record carries the accurate count against the immutable commit body.
- **FULL L-2**: paid at `d01615b` (run-dimension guard; probe M-C).
- **Read L-1**: paid at `ace0845` (LAYER ninth member; red→green as the probe).
- **Read O-1**: inherited — future layer reads enumerate nine members until `E10`'s
  sentence says otherwise. **O-2**: `L-1lr` stays banked; the seam held twice. **O-3**:
  multi-obligation read consolidation stays a per-time user ruling, never a default.
  **O-4**: noted, no action (`R9`'s batch, not a read, is the redemption vehicle).
- Plan-acceptance state: ISSUE_TRIAGE decisions exist for `knowledge-in-memory` and
  `channel`; `burden` waits for Phase E's material **by design** (plan Steps 9/10).

Step 9 closed; the ledger pointer moves to Phase E (Step 10).

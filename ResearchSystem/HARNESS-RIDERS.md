# HARNESS RIDERS — banked findings, redeemed on touch

Pure data table. The governing rules — the bank/HarnessIssue split, routing, row format,
redemption, and the 2026-07-30 tightenings — live in
`document-harness/CONSTRUCTION-CHECKLIST.md` R10 (incorporated 2026-07-31; this header
carried them until then). The rule-① backfill of 2026-07-30 is recorded in commit
`0a3d18d`'s body.

| id | what | redeem when | source |
|---|---|---|---|
| frozen-path-prefix | 两份 `E2` 冻结的 supersession 里有**四个路径 token 按写法解析不到**：`supersession-1:89` `` `schema/document-assurance-v3/review.v2.schema.json` `` · `supersession-2:60` `` `assurance/runs/` `` · `supersession-2:83` `` `schema/` ``（以上三个属 `layer_path_check` 的 missing-prefix 类），外加 `supersession-2:99` `` `templates/run-v2/` ``（哪儿都解析不到，故被 `unresolved_tokens` 按设计跳过——真实归宿是 `ResearchSystem/assurance/templates/run-v2/`）。**已签文件全篇用 ResearchSystem-relative 约定**，所以「这是缺陷、还是把 guard 的约定倒读到签字文本上」**是用户的判断**；本行存在的意义是：等这些字节变得可写的那一刻，这个问题有人问。**另一半事实**：`layer_path_check` 只扫 staged diff 新增行，层内**存量**从未被查过——首次全量扫描在 read `f3f31c0` 的 `O-1` 与 `18ac031` 的 §3 | `E2` 对两份 supersession 的 recorded ruling，或下一批碰这两个文件的路径 token，孰先；**无 deadline**——字节冻着的时候这个缺陷咬不到人 | v3-checkpoint-read-0aed595.md `O-1` · v3-checkpoint-read-8884f47.md `L-1` |
| wl-route | **供了字节的 wording-level finding 有两条路，三句规则二比一**：`E10` 自由通道枚举写的是「a finding below must-fix — **low or observation alike** — whose record supplies the exact bytes」（**没点名 wording-level**）· `R9` 抬头句说「A read's **wording-level** findings are **banked**, never rounds」，只把「no-decision-nameable」那一类写成搭下一批 · `R10` 路由句说自由通道收**any finding** whose record supplies the exact bytes 且「档位不改变路由」。两句说当场套、一句说入 bank。批 B R5 的 `W-1`（有名下游决定 + 有字节）**走了当场套**。改变的决定：一条 wording-level 的修**立刻落地并在层里待到下次 read**，还是等一个批——两读文本依据等强，两个 executor 必分歧（同形先例见本 bank 的 `E1-suff` 与 `chk-thin`）。**读者刻意不供字节**：任何 tiebreak 都是给 `E10` 枚举或 `R9` 加 bound = design | 修法为 design 形状，故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `E10` 自由通道枚举句 / `R9` 抬头句 / `R10` 路由句的设计批；**deadline = 下一份对 wording-level finding 供字节的 read 记录**（分歧第一次真能咬人的时刻，按 `HD-37` ① 不在创造本行的 R5 之内） | v3-checkpoint-read-f61ce2c.md `L-2` |
| E1-suff | `E1` 的新句「a reviewer the orchestrator dispatches … is independent」把 orchestrator 派发说成**充分条件**，而 `R1` 的失格判据是四项合取（dispatched / prompted / scoped / reported 全归 executor），独立性只在「executor 一项不占」时成立。**混合态无解**：orchestrator 派发、executor 起草提示词或定 scope——按 `E1` 算独立，按 `R1` 无答案。改变的决定：那次评审出不出 verdict、吃不吃 `E9` 的评审腿。**与 commit 正文声明「刻意不开」的四合取问题不是同一件**（那件问的是「四项之一是否就该失格」，本条问的是新句的充分性主张）。**读者刻意不供字节**：任何限定词都是加 bound = 设计开轮（`E10`）| 下一批碰 `E1` subagent 句或 `R1`；**deadline = 第一个真把 reviewer 派成 subagent 的轮次**（混合态第一次可能存在的那一刻）| v3-checkpoint-read-be9878a.md L-1 |
| tier-file-vs-clause | tiering 节的例外句（`EXECUTION.md` §Regression-battery tiering，doc-only 档那条的 Exception）**按文件写、被按子句用**：句面说「code enumerates 或 tests pin 的 doc 文件」即 tooling-touching，而批 B R4 改的两个成员都在 `layer_path_check.LAYER` 与 `test_precommit_checks.py` 的 `EXPECTED` 里，照句面该跑八条命令全电池。实跑取 doc-only，依据是「被枚举的是**路径**、而路径没变」——**这个读法文本里一个字都没有**。实质判断站得住（两个绑定 pin 实跑 43 passed；`HD-22` 已裁镜像不加机器），故这是**文本缺陷不是验证缺陷**。改变的决定：下一个构造批照此句自选档位，文本给一个答案、两轮先例（`838c413` 与 R4）给相反答案。**无字节**：两个方向的限定都是加 bound = design | **redeem-when 按 `R10` 新判据（批 B R5）重写**——本行修法为 design 形状，故只能点名**有资格开轮**的表面：下一个有资格开轮且碰 tiering 节的设计批，或**下一个有资格开轮的构造批按此句自选档位的那一刻**，孰先。**原 deadline「下一个碰 code 枚举层文件的 doc-only 构造批」已作废**——它在 R4 的 amendment commit 上到期，而 amendment 通道结构上装不下 design 字节（read 3 `O-4`），是一个出生即到期且无人有资格动的 deadline | v3-review-full-418b89c.md L-2 + O-1 · v3-review-verify-fbcb035.md V-1 · v3-checkpoint-read-8884f47.md `O-4` |
| wspec-owner | WorkSpec author 重指到 executor（本轮 `EXECUTION.md` 两处）后，**另有三处仍写着被取代的读法，且三处全是已签或 `E2` 冻结**：已签契约 §3 接口归属表 `Document-Work-Assurance-Contract-v3.md:62`「`DocumentWorkSpec` \| stage author / planning agent」（blob `b2dbdf75`，`E2` 冻结的那份契约），以及 `document-work-spec.schema.json` 与 `document-work-spec.v2.schema.json` 两个 title 里的「sole owner: stage author / planning agent」（`E2` 冻结的 pack 内）。宽容读法成立并已被读者核实（「stage author」是角色标签、其承载者由 `HD-35` 改派；契约 V3-D5 禁令表不含 WorkSpec；无任何代码比对 spec author 与 executor），**但指令层里没有一句话这么说**。改变的决定：评审员拿 run 声明的 spec author 去比一张写着 **sole owner** 的表，读出角色分离违规，且背后站着三份已签/冻结件。**无字节**：披露句是设计，另三处 `E2` 冻结、其字节欠 `E2` 裁决方可动 | 下一批碰 `EXECUTION.md` WorkSpec 归属句或该三处承载；**deadline = 第一个按新模型起草 WorkSpec 的 run** | v3-checkpoint-read-be9878a.md L-3 |
| ledger-assert | `chk-ledger-note` 拆除后（批 B R3），「这一轮该记的事真记下来了」**无任何机械承接物**——`HARNESS-POLICY.md` §2 只管文件管理规则（哪两本账 / 什么进 / 120 行上限），不断言内容完整性；§4 现声明本机不设机器、纪律承接。`HD-31` 把「承接物存在与否」定为调用者的选择，用户 2026-08-13 preclear 裁**先记待办、之后再决定**（未追认亦未否决）。**两条理由支持现状**：`E6`（刚裁掉一道检查就造新的等于没裁）· 原锚点脆（`locator_exists` 钉 `.goals/LEDGER.md` 一句人写的中文散文，8 个 run 里 1 个用过）。**与 orchestrator 自动化无关**：即便 orchestrator 由 session 改为自动载体（io-design §8 列为后话），此断言仍须是调用者仓 pre-commit/CI 的一段机器，不会随之出现。**修法方向**（真要做时）：`HARNESS-POLICY.md` 声明锚点 → 断言进本仓 pre-commit/CI，**不回 harness 侧** | 下一批碰 `HARNESS-POLICY.md` §4 或调用者侧 pre-commit/CI 判据面；**deadline = 拆分批**（调用者仓与 harness 仓分家的那一刻，「谁来验记账」必须当场有答案，否则两仓都没人管） | `v3-review-full-c7e0ba0.md` §3（`HD-31` 承接义务的落法）· 用户裁决 2026-08-13 |
| F-c | digest-narrowing plan:247-248 边界编号错（"边界 3 降级"实为 plan 表 #2，对两份枚举皆无解；:219 警示已加、247/248 未改——照抄会降错边界） | 碰 harness-digest-narrowing.plan.md | v3-review-full-f2507a5.md F-c |
| RA | `run_all` 现有**一个产品调用者**——run-v2 evidence 模板（`HD-25`，批 B R1 `e9166d2` 接线，`run_evidence_v2.py:200`）；**另有两处单元测试调用** `tooling/tests/document_harness/test_candidate_checks.py:874`/`:887`（自 `11ce5b4`（2026-07-28）起，即本行出处那个 FULL 的同一个 commit——一条钉住「gap 之后的 check 不跑」、另一条是其负对照，**它们是今天唯一钉住 `run_all` stop 契约的东西**）。故原文"全仓零调用者"指的一直是产品侧、从来不是字面全仓（VERIFY `dbbec28` `V-2`）。**剩下的是 CLI 那半边**：产品 run 机检引擎仍无 CLI 接线，且模板是**抄**进 run 的，八个已关闭 run 不继承（rider `delta-prose`）。（原文"全仓零调用者"自 `e9166d2` 起为假，FULL `e9166d2` `L-2` 指出；用户 2026-07-30 归口） | **拆分批**（2026-08-12 用户裁重定范围，非兑付：R2 的 I/O 设计把独立 CLI 与「跑这一轮」的执行入口整体归拆分批（io-design §7/§8），CLI 半边随之；原 deadline「R2 开轮」已到期而行未动，过期事实由 R2 转录核查 finding 26 记录），或下一个碰 `run_all` 签名的批（FULL `e9166d2` `O-1`：docstring 承诺保留 `SPEC_GAP` 证据，但 `raise` 时 `results` 是局部变量，调用者拿不到——动签名时 `test_candidate_checks.py:874/887` 就是影响面），孰先 | v3-review-full-11ce5b4.md O-4 · v3-review-full-e9166d2.md L-2 |
| PD | `document_harness/__init__.py:238` `pack_digests()` 哈希整个 v3 pack（含成员 9）却全仓零调用者（两处活调用是 v2 `schemas.pack_digests()`，另一 pack）；`E2` 现零机械 enforcement，此为唯一现成挂点。与 `RA` 同形状 | **拆分批**（`HD-27` 2026-08-11 重定范围，非兑付：`E2` 不加守卫、`pack_digests` 不接，三条理由——产品 run 侧 `_check_git_diff_boundary` 已把 `schema`/契约列进 `boundary.out`、构造批每轮独立评审、`HD-16` 使"证据离仓自证"落空——在拆分批同时变形），或下一个碰 `E2` 冻结面的批，孰先。**同批分出的第二问**：`pack_digests` 零调用的真因是 **v3 证据从不记 interface 版本**（v2 `resolver.py:272` 记在 `bindings`，v3 零命中），与 `E2` 无关，随此行一并再议 | v3-review-verify-c05d052.md V-4 · `HD-27` |
| E10-sync | `E10` 钉死九成员后，成员名单同时活在三处：`CONSTRUCTION-CHECKLIST.md` 的 `E10` 成员句、`tooling/hooks/layer_path_check.py` 的 `LAYER`、`tooling/tests/document_harness/test_precommit_checks.py:176` 的 `EXPECTED`。后两者由 `test_layer_equals_the_hand_written_membership` 互相断言；**散文那一腿无任何守卫**——mutation 实证：从 `E10` 句删掉一条路径后它列八条却仍宣称 "exactly these nine paths and nothing else"，三 guard + 20 测试全绿（`7a08265` VERIFY P3 于十成员复证；route-b 轮 P2 又证 **(b) 必读义务句同腿同暴露**——删句后三 guard + 632 测试仍全绿）。钉死前散文是开口的、不可能因遗漏而与 `LAYER` 矛盾，现在可以且静默 | **碰 `E10` 成员句的任何批：三处（成员句 / `LAYER` / `EXPECTED`）同改并在 commit 正文点名**——本行是每次触碰时的核对项，非一次性兑付；**deadline = 下一次碰成员句的批**（2026-08-08 裁 `HD-22`：不加机器（`E6`），原 deadline「v4 铸成」经 `V-4` 判不再 bound damage） | v3-review-full-838c413.md L-3 + v3-review-verify-7a08265.md V-4 |
| chk-thin | `REVIEW.md` 新增的 §*What is not in the subject: the run's own checkers*（`:24-47`）与同文件 §*`UNVERIFIABLE` is a real answer*（`:221-231`）对**同一形状**给相反处置：`local_check` 现在意为「确定性检查独自判定该义务」，而新节要评审员只确认检查**跑过、可复现、看的是它有权看的树**——三条一个只断言 `battery` 字样的检查全部满足——随后把「这检查该不该断言更多」判为出圈、"never move a verdict"。于是义务可凭一个几乎什么都没判的检查拿 `SUPPORTED`，正是 §`UNVERIFIABLE` 点名的产品特征性失效。镜像方向已定且定在反面：§*The `review_only` question* 把「脚本本可判定却声明 review_only」判为**对 WorkSpec 的 finding**，而 WorkSpec 是控制面，正被新节首句划进 subject 内。**后果**：一名评审员把薄检查报成 mode 声明的控制面 finding、另一名当作 checker weakness 压掉，两边文本依据等强，义务是否 `SUPPORTED` 由此分歧。**读者刻意不供字节**：任何 tiebreak 都给两条规则之一加约束，按 `E10` 即开轮 | 下一个 product run 的 FULL（**deadline**——ledger 断点即 P5B 批次；该节自身的实证案例是 `p5b-firewall` 七条 finding 里四条属此类），或下一批碰 `REVIEW.md` 上述两节文本，孰先 | v3-checkpoint-read-a5a04c3.md L-1 |
| HI-route | 新节从 verdict 里摘出去的那类观察被路由到 `HarnessIssue`，而评审员**没有任何被告知的抵达方式**。`HarnessIssue` 在 `REVIEW.md` 全文只出现这一次（`:29-31`）；该文件 deliverables 节写明评审员持久化并提交的**恰好两件** artifact，`HarnessIssue` 不在其中；schema 把 `observed_after` 收窄到 `CLOSED`/`STOPPED_REPLAN` 并明写「声称 run 中途观察到的 issue 不可表示」，而评审轮就发生在 run 中途——评审员在**拿到**该观察的那一刻无法表示它。契约 §3 确实把 owner 定为**观察者**而非执行者（故评审员有资格），但没有一句话告诉他这点，`CLOSED` 之前没有载体，之后也没人被指派去收。`EXECUTION.md:26` 把 `HarnessIssue` 列在**执行者**所有物之下，是全层唯一的角色-artifact 陈述。**后果**：评审员正确地不把它做成 finding，然后无处可放、观察丢失——零复述 dispatch 契约下他只从该文件学职责，而该文件 `:120-125` 已记着同类先例（p4-doc FULL 做对了却因「没写结果去哪」而在 session 内停住）。**无字节**：指定载体即给 `REVIEW.md` 加义务，是设计 | 同 `chk-thin`（同一 deadline、同一理由），或下一批碰 `REVIEW.md` `:29-31` / deliverables 节，孰先 | v3-checkpoint-read-a5a04c3.md L-2 |
| ctx-ground | 「7 份真实指令的 Context 小节标题皆恰为 `## Context (non-normative)`」是 `_is_context_title` 精确匹配**与其负对照**共同站的地基，而**没有任何测试绑定它**：负对照断言的是测试文件自己的 `CONFORMING` fixture，不是 `assurance/runs/*/instruction.md`。将来某份指令把该标题写歪，方向是 fail-safe（掉进 prose 态、欠回全部派生件），但**静默**，且 docstring 那句会悄悄停止为真。**用户裁决不做**：让 harness 测试去读 run artifact 是新机器（`E6` 反面），且泛化抽取器正是 `RA`/`PD` 已记两次的「无调用者常驻件」形状 | 第一份编号态指令（P5B 批次）落地时，或下一批碰 `_is_context_title` / `test_instruction_form.py` 的负对照，孰先 | v3-review-verify-c7fb720.md O-2 |
| mark-case | `_NORMATIVE_MARKERS`（`rsclib/document_harness/instruction.py:142`）大小写敏感：Context 小节里的 `MUST` / `必须` 触发 `FORM-NORMATIVE-IN-CONTEXT`，小写 `must` 不触发——一条规范要求可以无标记地留在 START 批准面之外。**不是字节修**：改成大小写不敏感会让普通英文里的 must 满地误报，取舍（收窄标记词表 vs 只在句首匹配 vs 接受漏报）是设计题。已写进交付的 `_is_context_title` docstring，故记录不会读起来像已关 | 下一批碰 `_NORMATIVE_MARKERS` 或 `form_conformance` 标记词腿的批；**deadline = 第一份编号态指令的 FULL**（在那之前没有真实指令会踩到它） | v3-review-verify-c7fb720.md O-4 |
| freeze-audit | **两道守卫互锁**：`review_freeze_check.py` 规定冻结窗口内只有评审记录能落（`E9`），而 `repo-audit.py` 是同一道 pre-commit 上的硬 gate——**返回的记录一旦踩中 audit，就没有任何顺序能同时满足两者**。本轮实例：FULL 记录引用评审员自己的 `grep '[[:space:]]'`，被读成坏 wikilink 挡住；先修 audit 再落记录这条路经实测被冻结钩子拦下（`review_freeze exit=1`，见 journal §8），于是只剩「删 marker」或「`--no-verify`」，两者都违反 `E9`。本次靠根因修 audit 绕开（`32cf220`），**代价是 base commit 的树不能自过 gate**（VERIFY `O-3v`）。下次未必有这么干净的根因修法：记录是不可编辑的独立证据（`R6`），而 audit 的判据可能根本不该为它让路 | 下一批碰 `review_freeze_check.py` / `repo-audit.py` 的 pre-commit 判据，或下一次返回记录被任何 gate 挡住时（**那一刻就是 deadline**——届时必须当场裁「谁让路」而无法再推迟） | v3-review-verify-25511d9.md O-3v + journal v1-context-exact-2026-08-05.md §8 实测 |
| qp-index | `rsclib/document_harness/paths.py` `TrackedPaths.from_index` 的 `git ls-files` 未加 `core.quotepath=off`（`check()` 与 `staged_added_lines` 已加）。C-quoted 的索引条目不注册它真实的 ASCII 祖先目录，于是**只含非 ASCII 名子文件的目录解析为「哪儿都不存在」→ 误拦**。实测：tracked `ResearchSystem/notes/笔记/x.md`，产物引用 `ResearchSystem/notes/` → `check()=1`。比它所出自的那条（静默漏扫）**更糟一档**：漏扫是漏，误拦招 `--no-verify`，而这正是本轮为规约面开豁免时自己给的理由。最小修=该处加同一 flag + 一条负对照 | 下一批碰 `paths.py` / `candidate_path_check.py`；**deadline = 仓库出现第一个非 ASCII tracked 路径**（`git ls-files \| grep -c '^"'` → 0，今日惰性） | v3-review-verify-a1fad7e.md L-3 |
| qp-inert | `paths.py` `staged_added_lines` 上的 `core.quotepath=off` **空转**：quoting 只影响 `+++ b/"…"` 头行，而该行被 `+++` 守卫滤掉；评审员 mutation V4 删掉它 46 测试全绿。不是错，是无任何决定依赖的机器（`E6`）。处置二选一：绑一条测试，或删掉。**倾向删**——`E6` 原文是"需要新机器的修，是重新质疑被守之物的信号，不是加守卫" | 同 `qp-index` | v3-review-verify-a1fad7e.md L-4 |
| cache-count | `Thesis/Work/Tooling/repo-audit.py:32-34` 新增注释自称 pytest 缓存的代价是 "four markdown files"，实测为**二**（同批 commit 正文里写的也是二），当前树上只剩一。四来自更早那次两-checkout 观察——**另一个时刻**的数字，与 `L-2` 同属陈旧数字类（`E3`），只是落在注释而非正文。字节已明：把 four 改成不写死数量的说法，或写当次实测值并注明测量时刻 | 下一批碰 `repo-audit.py` 的 `EXCLUDE` 或其头部注释 | v3-review-verify-3b28116.md V-3 |
| waiver-live | `E10` 开轮豁免够不够到 (b) 必读句的 `§live` read，两个载体都没写：checklist 豁免从句只挂在层 cold read 上（`:109-116`），README `:29` "every cold read MUST" 在 cold read 被豁免时悬空，决策簿头部同样沉默且按构造管不了自己。分歧改变动作——豁免轮里一个 session 照读、另一个引豁免跳过，跳过会漏 live 裁决（如 `HD-23` 的预算判据）。**读者刻意不供字节**：任何澄清都给 `E10` 义务加 bound=设计开轮 | 下一批碰 `E10` cold-read/`§live` 句；**deadline = 用户第一次真给开轮豁免**（分歧咬人的那一刻） | v3-checkpoint-read-bd77fd4.md L-1 |
| decl-dup | bind 的 declarations 路径两处拼写（`:70` 常量 `DECLARATIONS` 只用于 STOP 消息，`:276` 独立重建 `CONTROL / "bind-declarations.json"`）——copy 类分叉，今日由 missing/complete 两条测试可捕。**字节已供**：`declarations_path = run_dir / DECLARATIONS`。未入修腿系 executor 呈表时漏报此条、用户未裁，按未接受项 bank | 下一批碰 `run_bind_v2.py` 的 declarations 读取或其 STOP 消息 | v3-review-full-eec4171.md L-3 |
| argv-cap | R2 把 comparator 声明搬上 check-spec argv 后撞 schema 约束：argv token 禁反引号/美元符/管道/分号/与号/尖括号且单 token ≤500 字符，候选面 2313 个标题中 **329（14%，126 文件）无法写成一条 argv 声明**（p4-doc 自身 0 拒、四份 frozen spec 不受影响）。R2 与修腿均未披露此天花板；修法（声明改文件承载、或转义约定）是设计题，字节不可现套 | **deadline = 第一个要绑 comparator mode 的 run 起草时**（届时 329 里任一标题成为 site 即咬人）；或下一批碰 `compare_blocks.py` 参数面，孰先 | v3-review-verify-3b6267c.md V-1 |
| retire-suite | VERIFY de8f4ef 两条同套件项：**V-5** kept 计数守卫绑在来源未绑谓词——fixture 给每个 ordered id 都写了 out.txt，把 `.is_file()` 谓词整个删掉 12 测试仍全绿；p4-doc 上该 mutation 打印 23 而实存 13（比 L-3 原病还糟一档）。**字节已供**：fixture 去掉一个 ordered id 的 out.txt（kept 断言与 survivors 循环两处随调）。**V-3** 双前缀第四实例 `test_run_v2_template_retire.py:105`（fixture docstring 写 `chk-<id>.out.txt`）——`E7` 同类清扫漏网 | 下一批碰 retire 套件或其 fixture | v3-review-verify-de8f4ef.md V-5 · V-3 |
| readme-three | README:12 「the three step scripts additionally take the round's refs」悬在四条 invocation 上（第四条 retire 零 round flags）；FULL d52f41b L-1 明言不以 executor 披露作 discharge，修腿未动。句字面仍真（恰好那三个吃 refs）但按句数脚本的读者会漏数 retire。字节方向：限定成 the three round steps (evidence, bind, repair) 类 | 下一批碰 run-v2 README 实例化段 | v3-review-full-d52f41b.md L-1 后半 · v3-review-verify-de8f4ef.md V-4 |
| status-key | `check_subject` 的 CLOSED carve-out 钥匙是 run **自写**的 `status` 字段，本路径零交叉核对（其余四个身份字段都对 CandidateRecord 核；`flow.check_state_pointers` 会要求 CLOSED 携 `final_decision_ref`/`summary_ref` 但不在此路径——callers 仅 `rsclib/document_harness/cli.py:346`/`flow.py:307`（前者原 `rsc.py:309`，随 R2 的 CLI 摘出而改址））。实测同场景一词之差：EVIDENCED+缺一份 → review exit 1；CLOSED+缺同一份 → review exit 0。补偿控制真实但在另一模块且响不硬（dispatch 腿 `NOT-DISPATCHABLE` exit 1）；具体代价 = `dtw review --subject` 不再能抓「递错 commit」（递了 closing commit 得干净报告）。用户裁（2026-08-10）：不加机器（`E6`），bank | **deadline = 第一个真实 run 的 closeout 评审**（递错 commit 可能性首次真实存在的时刻），或下一批碰 `check_subject` 的 status/carve-out 面，孰先 | v3-review-full-d52f41b.md O-1 |
| delta-prose | 「A future run's directory carries its delta and not the shared core」（A2 plan acceptance）现仅**散文承载**（run-v2 README + 修后三 docstring 同向），零机械 enforcement：已批的 write_scope 设计判断挡的是「候选**改**模板」，而 run **抄**模板写在自己 control root 内完全 conformant，warrant 够不着；`check_template_instance.py`（唯一 pre-START gate）只查 spec 版本/preamble/paragraph-map，不看 run 目录携带什么脚本。要不要加守卫是设计题（`E6` 倾向不加——需要新机器的修是重新质疑被守之物的信号；`R5` 归用户裁） | 首个新 run 起草时（散文成唯一防线的第一次实测），或下一批碰 `check_template_instance.py` 的 gate 判据面，孰先 | v3-review-full-7dd027e.md L-2 |
| tier-scope | **只剩 ② Revert anchor**：tiering 节头自称「this section is the revert unit」是 2026-08-03 裁决给用户的条件，但该节自 `HD-14`（A2-R1 `418b89c`）搬进指令层后，行使它（整节 revert）本身就是改指令层 = 设计开轮——当时拿到的那个便宜条件已不存在，且 `418b89c` 正文未披露此代价。**同行另三件已于批 B R4 兑付**（`EXECUTION.md` 三处）：① 全电池枚举句由四项改为按 R3 修腿实测枚举的八条命令并写明「nothing fewer」· ④ pytest 补 scope 到 `ResearchSystem/tooling` 并注明仓库根直跑的撞包原因 · L-2 头部 disclaimer 加限定，点名 tiering 节是其唯一施工侧例外 | ② 自带条件被触发的那一刻（评审开始返回 skipped-battery 类 `SPEC_GAP`），或下一批碰 tiering 节头，孰先 | v3-review-full-418b89c.md O-1 · v3-review-verify-fbcb035.md V-1 |
| battery-travel | `EXECUTION.md` 的 Regression-battery tiering 节要求全档跑「these six commands and nothing fewer」，而六条里**五条不 travel**：`tooling/tests/run_tests.py` · `run_p4_tests.py` · `run_p5a_tests.py` · `schema/fixtures/validate_fixtures.py` · `rsc.py compile --check`（`rsc.py` 按 `split-design.md` §1 留调用者）——travel 的只有 `python -m pytest`。指令层随仪器走、电池腿留在调用者仓，故**新仓无法满足自己指令层要求的电池**。今日不咬人（构造轮全在调用者仓跑）；R3 挂 submodule 后第一次在 harness 仓内开轮即咬。修法是设计题（收窄 tiering 节的适用面 / 让电池腿一同 travel / 给 harness 仓自己的电池定义），按 `R5` 该不该改是用户的问题 | 修法为 design 形状，故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个碰 `EXECUTION.md` tiering 节的设计批；**deadline = 第一个在 harness 仓内开的构造轮** | v3-review-full-297bb2b.md `O-2` |

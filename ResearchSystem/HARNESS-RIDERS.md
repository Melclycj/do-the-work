# HARNESS RIDERS — banked findings, redeemed on touch

Pure data table. The governing rules — the bank/HarnessIssue split, routing, row format,
redemption, and the 2026-07-30 tightenings — live in
`document-harness/CONSTRUCTION-CHECKLIST.md` R10 (incorporated 2026-07-31; this header
carried them until then). The rule-① backfill of 2026-07-30 is recorded in commit
`0a3d18d`'s body.

| id | what | redeem when | source |
|---|---|---|---|
| frozen-path-prefix | 两份 `E2` 冻结的 supersession 里有**四个路径 token 按写法解析不到**：`supersession-1:89` `` `schema/document-assurance-v3/review.v2.schema.json` `` · `supersession-2:60` `` `assurance/runs/` `` · `supersession-2:83` `` `schema/` ``（以上三个属 `layer_path_check` 的 missing-prefix 类），外加 `supersession-2:99` `` `templates/run-v2/` ``（哪儿都解析不到，故被 `unresolved_tokens` 按设计跳过——真实归宿是 `ResearchSystem/assurance/templates/run-v2/`）。**已签文件全篇用 ResearchSystem-relative 约定**，所以「这是缺陷、还是把 guard 的约定倒读到签字文本上」**是用户的判断**；本行存在的意义是：等这些字节变得可写的那一刻，这个问题有人问。**另一半事实**：`layer_path_check` 只扫 staged diff 新增行，层内**存量**从未被查过——首次全量扫描在 read `f3f31c0` 的 `O-1` 与 `18ac031` 的 §3 | `E2` 对两份 supersession 的 recorded ruling，或下一批碰这两个文件的路径 token，孰先；**无 deadline**——字节冻着的时候这个缺陷咬不到人 | v3-checkpoint-read-0aed595.md `O-1` · v3-checkpoint-read-8884f47.md `L-1` |
| wl-route | **供了字节的 wording-level finding 有两条路，三句规则二比一**：`E10` 自由通道枚举写的是「a finding below must-fix — **low or observation alike** — whose record supplies the exact bytes」（**没点名 wording-level**）· `R9` 抬头句说「A read's **wording-level** findings are **banked**, never rounds」，只把「no-decision-nameable」那一类写成搭下一批 · `R10` 路由句说自由通道收**any finding** whose record supplies the exact bytes 且「档位不改变路由」。两句说当场套、一句说入 bank。批 B R5 的 `W-1`（有名下游决定 + 有字节）**走了当场套**。改变的决定：一条 wording-level 的修**立刻落地并在层里待到下次 read**，还是等一个批——两读文本依据等强，两个 executor 必分歧（同形先例：本 bank 的 `chk-thin`，以及 `E1-suff`——已于 2026-08-18 轮 `ORCHESTRATOR-CHARTER` 兑付删行）。**读者刻意不供字节**：任何 tiebreak 都是给 `E10` 枚举或 `R9` 加 bound = design | 修法为 design 形状，故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `E10` 自由通道枚举句 / `R9` 抬头句 / `R10` 路由句的设计批；**deadline = 下一份对 wording-level finding 供字节的 read 记录**（分歧第一次真能咬人的时刻，按 `HD-37` ① 不在创造本行的 R5 之内） | v3-checkpoint-read-f61ce2c.md `L-2` |
| wspec-owner | WorkSpec author 重指到 executor（本轮 `EXECUTION.md` 两处）后，**另有三处仍写着被取代的读法，且三处全是已签或 `E2` 冻结**：已签契约 §3 接口归属表 `Document-Work-Assurance-Contract-v3.md:62`「`DocumentWorkSpec` \| stage author / planning agent」（blob `b2dbdf75`，`E2` 冻结的那份契约），以及 `document-work-spec.schema.json` 与 `document-work-spec.v2.schema.json` 两个 title 里的「sole owner: stage author / planning agent」（`E2` 冻结的 pack 内）。宽容读法成立并已被读者核实（「stage author」是角色标签、其承载者由 `HD-35` 改派；契约 V3-D5 禁令表不含 WorkSpec；无任何代码比对 spec author 与 executor），**但指令层里没有一句话这么说**。改变的决定：评审员拿 run 声明的 spec author 去比一张写着 **sole owner** 的表，读出角色分离违规，且背后站着三份已签/冻结件。**无字节**：披露句是设计，另三处 `E2` 冻结、其字节欠 `E2` 裁决方可动 | 下一批碰 `EXECUTION.md` WorkSpec 归属句或该三处承载；**deadline = 第一个按新模型起草 WorkSpec 的 run** | v3-checkpoint-read-be9878a.md L-3 |
| F-c | digest-narrowing plan:247-248 边界编号错（"边界 3 降级"实为 plan 表 #2，对两份枚举皆无解；:219 警示已加、247/248 未改——照抄会降错边界） | 碰 harness-digest-narrowing.plan.md | v3-review-full-f2507a5.md F-c |
| RA | `run_all` 现有**一个产品调用者**——run-v2 evidence 模板（`HD-25`，批 B R1 `e9166d2` 接线，`run_evidence_v2.py:200`）；**另有两处单元测试调用** `tooling/tests/document_harness/test_candidate_checks.py:874`/`:887`（自 `11ce5b4`（2026-07-28）起，即本行出处那个 FULL 的同一个 commit——一条钉住「gap 之后的 check 不跑」、另一条是其负对照，**它们是今天唯一钉住 `run_all` stop 契约的东西**）。故原文"全仓零调用者"指的一直是产品侧、从来不是字面全仓（VERIFY `dbbec28` `V-2`）。**剩下的是 CLI 那半边**：产品 run 机检引擎仍无 CLI 接线，且模板是**抄**进 run 的，八个已关闭 run 不继承（rider `delta-prose`）。（原文"全仓零调用者"自 `e9166d2` 起为假，FULL `e9166d2` `L-2` 指出；用户 2026-07-30 归口） | **2026-08-17 用户裁再次重定范围、非兑付**——本行至此只剩 **CLI 入口**一件事。产品调用者那半**已由 `HD-25` 关闭**（批 B R1 `e9166d2` 把引擎接进 run-v2 evidence 模板，条目已 implemented；R4 实测今日一个产品调用者 `run_evidence_v2.py:200` + 三处测试引用），剩下的是「跑这一轮的机检」仍只住在**抄**进 run 目录的模板脚本里、`dtw` 七命令无一是它（2026-08-19 前写「六命令」，`init` 加入后该计数为假而本行实质不变——`init` 同样不是机检入口）。`split-design` §1 已裁六命令原样，故加不加第七个命令按 `R5` 归用户；用户判定这是**便利性而非正确性**——接线已让产品 run 真的在用引擎，`HD-25` 接受的 delta 代价（模板是抄的、已关闭 run 不继承）加不加 CLI 都一样存在——**不为此推翻 §1**（**该判断限于本行这个案子**：2026-08-19 用户就 onboarding 另裁了一个第七命令`dtw init`，`HD-47`——§1 的「六命令原样」自此读作搬迁指令而非命令数上限，本行的答案不因此改变）。redeem-when = **第一次需要在 run 目录之外跑机检**的场合，或下一个碰 `run_all` 签名的批（FULL `e9166d2` `O-1`：docstring 承诺保留 `SPEC_GAP` 证据，但 `raise` 时 `results` 是局部变量，调用者拿不到——动签名时 `test_candidate_checks.py:874/887` 就是影响面），孰先 | v3-review-full-11ce5b4.md O-4 · v3-review-full-e9166d2.md L-2 |
| PD | `document_harness/__init__.py:238` `pack_digests()` 哈希整个 v3 pack（含成员 9）却全仓零调用者（两处活调用是 v2 `schemas.pack_digests()`，另一 pack）；`E2` 现零机械 enforcement，此为唯一现成挂点。与 `RA` 同形状 | **2026-08-17 用户裁重定范围、非兑付**——`HD-27` 写明的重开条件（拆分批）已到达，R4 逐条复核后认定其三条理由**并未同时变形**：产品 run 的 `_check_git_diff_boundary` 仍把 schema 与契约列进 `boundary.out`、构造批仍每轮独立评审，只有第三条（`HD-16` 使「证据离仓自证」落空）随 `HD-28` 的 supersede 而变。拆分带来的新事实是冻结面**同时存在于两个仓**且无维持机制，但那是 rider `pin-drift` 记的问题，而一道 pre-commit 守卫守的是「不得无裁决写入」、**守不到「两仓一致」**，解不了它。故 **`E2` 维持不接**（`E6`）。redeem-when 重定为****去前缀那一件之后**（原文写「重扎根轮之后」，而该轮 2026-08-18 拆成三件、名字不再指任何单一轮次；条件的实质——调用者副本已删、冻结面只剩一份——已由 `SPLIT-COPY-RETIRE` 满足）——那时冻结面只剩一份，形势才真的变，届时再问一次；或下一个碰 `E2` 冻结面的批，孰先。**第二问随行不变**：`pack_digests` 零调用（R4 实测仍只有 `__init__.py:238` 定义与 `:266` 导出）的真因是 v3 证据从不记自己由哪个 interface 版本产出，与 `E2` 无关 | v3-review-verify-c05d052.md V-4 · `HD-27` |
| E10-sync | `E10` 钉死九成员后，成员名单同时活在三处：`CONSTRUCTION-CHECKLIST.md` 的 `E10` 成员句、`tooling/hooks/layer_path_check.py` 的 `LAYER`、`tooling/tests/document_harness/test_precommit_checks.py:176` 的 `EXPECTED`。后两者由 `test_layer_equals_the_hand_written_membership` 互相断言；**散文那一腿无任何守卫**——mutation 实证：从 `E10` 句删掉一条路径后它列八条却仍宣称 "exactly these nine paths and nothing else"，三 guard + 20 测试全绿（`7a08265` VERIFY P3 于十成员复证；route-b 轮 P2 又证 **(b) 必读义务句同腿同暴露**——删句后三 guard + 632 测试仍全绿）。钉死前散文是开口的、不可能因遗漏而与 `LAYER` 矛盾，现在可以且静默 | **碰 `E10` 成员句的任何批：三处（成员句 / `LAYER` / `EXPECTED`）同改并在 commit 正文点名**——本行是每次触碰时的核对项，非一次性兑付；**deadline = 下一次碰成员句的批**（2026-08-08 裁 `HD-22`：不加机器（`E6`），原 deadline「v4 铸成」经 `V-4` 判不再 bound damage）——2026-08-18 轮 `ORCHESTRATOR-CHARTER` 触碰成员句并三处同改（九→十，新成员 `ORCHESTRATION.md`），deadline 顺延至下一次碰成员句的批。**另记：`HD-22` 三处之外的散文站点已增至五**（read `7701f03` `L-2` 实测，量程＝全仓 tracked 排除 migration 与 journal）：调用者仓的 `.githooks/pre-commit`（2026-08-19 已更正为 ten）＋本仓 `CALLER-ONBOARDING` 轮新增三处——`document-harness/ONBOARDING.md:130` · 本仓 `.githooks/pre-commit:14-15` · 根 `README.md:33/:50`（`document-harness/README.md:34` 原已在册）。这些散文腿无任何守卫，触碰成员句的批按本行核对时**连带这五处** | v3-review-full-838c413.md L-3 + v3-review-verify-7a08265.md V-4 |
| chk-thin | `REVIEW.md` 新增的 §*What is not in the subject: the run's own checkers*（`:24-47`）与同文件 §*`UNVERIFIABLE` is a real answer*（`:221-231`）对**同一形状**给相反处置：`local_check` 现在意为「确定性检查独自判定该义务」，而新节要评审员只确认检查**跑过、可复现、看的是它有权看的树**——三条一个只断言 `battery` 字样的检查全部满足——随后把「这检查该不该断言更多」判为出圈、"never move a verdict"。于是义务可凭一个几乎什么都没判的检查拿 `SUPPORTED`，正是 §`UNVERIFIABLE` 点名的产品特征性失效。镜像方向已定且定在反面：§*The `review_only` question* 把「脚本本可判定却声明 review_only」判为**对 WorkSpec 的 finding**，而 WorkSpec 是控制面，正被新节首句划进 subject 内。**后果**：一名评审员把薄检查报成 mode 声明的控制面 finding、另一名当作 checker weakness 压掉，两边文本依据等强，义务是否 `SUPPORTED` 由此分歧。**读者刻意不供字节**：任何 tiebreak 都给两条规则之一加约束，按 `E10` 即开轮 | 下一个 product run 的 FULL（**deadline**——ledger 断点即 P5B 批次；该节自身的实证案例是 `p5b-firewall` 七条 finding 里四条属此类），或下一批碰 `REVIEW.md` 上述两节文本，孰先 | v3-checkpoint-read-a5a04c3.md L-1 |
| HI-route | 新节从 verdict 里摘出去的那类观察被路由到 `HarnessIssue`，而评审员**没有任何被告知的抵达方式**。`HarnessIssue` 在 `REVIEW.md` 全文只出现这一次（`:29-31`）；该文件 deliverables 节写明评审员持久化并提交的**恰好两件** artifact，`HarnessIssue` 不在其中；schema 把 `observed_after` 收窄到 `CLOSED`/`STOPPED_REPLAN` 并明写「声称 run 中途观察到的 issue 不可表示」，而评审轮就发生在 run 中途——评审员在**拿到**该观察的那一刻无法表示它。契约 §3 确实把 owner 定为**观察者**而非执行者（故评审员有资格），但没有一句话告诉他这点，`CLOSED` 之前没有载体，之后也没人被指派去收。`EXECUTION.md:26` 把 `HarnessIssue` 列在**执行者**所有物之下，是全层唯一的角色-artifact 陈述。**后果**：评审员正确地不把它做成 finding，然后无处可放、观察丢失——零复述 dispatch 契约下他只从该文件学职责，而该文件 `:120-125` 已记着同类先例（p4-doc FULL 做对了却因「没写结果去哪」而在 session 内停住）。**无字节**：指定载体即给 `REVIEW.md` 加义务，是设计 | 同 `chk-thin`（同一 deadline、同一理由），或下一批碰 `REVIEW.md` `:29-31` / deliverables 节，孰先 | v3-checkpoint-read-a5a04c3.md L-2 |
| ctx-ground | 「7 份真实指令的 Context 小节标题皆恰为 `## Context (non-normative)`」是 `_is_context_title` 精确匹配**与其负对照**共同站的地基，而**没有任何测试绑定它**：负对照断言的是测试文件自己的 `CONFORMING` fixture，不是 `assurance/runs/*/instruction.md`。将来某份指令把该标题写歪，方向是 fail-safe（掉进 prose 态、欠回全部派生件），但**静默**，且 docstring 那句会悄悄停止为真。**用户裁决不做**：让 harness 测试去读 run artifact 是新机器（`E6` 反面），且泛化抽取器正是 `RA`/`PD` 已记两次的「无调用者常驻件」形状 | 第一份编号态指令（P5B 批次）落地时，或下一批碰 `_is_context_title` / `test_instruction_form.py` 的负对照，孰先 | v3-review-verify-c7fb720.md O-2 |
| mark-case | `_NORMATIVE_MARKERS`（`rsclib/document_harness/instruction.py:142`）大小写敏感：Context 小节里的 `MUST` / `必须` 触发 `FORM-NORMATIVE-IN-CONTEXT`，小写 `must` 不触发——一条规范要求可以无标记地留在 START 批准面之外。**不是字节修**：改成大小写不敏感会让普通英文里的 must 满地误报，取舍（收窄标记词表 vs 只在句首匹配 vs 接受漏报）是设计题。已写进交付的 `_is_context_title` docstring，故记录不会读起来像已关 | 下一批碰 `_NORMATIVE_MARKERS` 或 `form_conformance` 标记词腿的批；**deadline = 第一份编号态指令的 FULL**（在那之前没有真实指令会踩到它） | v3-review-verify-c7fb720.md O-4 |
| freeze-audit | **两道守卫互锁**：`review_freeze_check.py` 规定冻结窗口内只有评审记录能落（`E9`），而 `repo-audit.py` 是同一道 pre-commit 上的硬 gate——**返回的记录一旦踩中 audit，就没有任何顺序能同时满足两者**。本轮实例：FULL 记录引用评审员自己的 `grep '[[:space:]]'`，被读成坏 wikilink 挡住；先修 audit 再落记录这条路经实测被冻结钩子拦下（`review_freeze exit=1`，见 journal §8），于是只剩「删 marker」或「`--no-verify`」，两者都违反 `E9`。本次靠根因修 audit 绕开（`32cf220`），**代价是 base commit 的树不能自过 gate**（VERIFY `O-3v`）。下次未必有这么干净的根因修法：记录是不可编辑的独立证据（`R6`），而 audit 的判据可能根本不该为它让路 | 下一批碰 `review_freeze_check.py` / `repo-audit.py` 的 pre-commit 判据，或下一次返回记录被任何 gate 挡住时（**那一刻就是 deadline**——届时必须当场裁「谁让路」而无法再推迟） | v3-review-verify-25511d9.md O-3v + journal v1-context-exact-2026-08-05.md §8 实测 |
| waiver-live | `E10` 开轮豁免够不够到 (b) 必读句的 `§live` read，两个载体都没写：checklist 豁免从句只挂在层 cold read 上（`:109-116`），README `:29` "every cold read MUST" 在 cold read 被豁免时悬空，决策簿头部同样沉默且按构造管不了自己。分歧改变动作——豁免轮里一个 session 照读、另一个引豁免跳过，跳过会漏 live 裁决（如 `HD-23` 的预算判据）。**读者刻意不供字节**：任何澄清都给 `E10` 义务加 bound=设计开轮 | 下一批碰 `E10` cold-read/`§live` 句；**deadline = 用户第一次真给开轮豁免**（分歧咬人的那一刻） | v3-checkpoint-read-bd77fd4.md L-1 |
| decl-dup | bind 的 declarations 路径两处拼写（`:70` 常量 `DECLARATIONS` 只用于 STOP 消息，`:276` 独立重建 `CONTROL / "bind-declarations.json"`）——copy 类分叉，今日由 missing/complete 两条测试可捕。**字节已供**：`declarations_path = run_dir / DECLARATIONS`。未入修腿系 executor 呈表时漏报此条、用户未裁，按未接受项 bank | 下一批碰 `run_bind_v2.py` 的 declarations 读取或其 STOP 消息 | v3-review-full-eec4171.md L-3 |
| argv-cap | R2 把 comparator 声明搬上 check-spec argv 后撞 schema 约束：argv token 禁反引号/美元符/管道/分号/与号/尖括号且单 token ≤500 字符，候选面 2313 个标题中 **329（14%，126 文件）无法写成一条 argv 声明**（p4-doc 自身 0 拒、四份 frozen spec 不受影响）。R2 与修腿均未披露此天花板；修法（声明改文件承载、或转义约定）是设计题，字节不可现套 | **deadline = 第一个要绑 comparator mode 的 run 起草时**（届时 329 里任一标题成为 site 即咬人）；或下一批碰 `compare_blocks.py` 参数面，孰先 | v3-review-verify-3b6267c.md V-1 |
| retire-suite | VERIFY de8f4ef 两条同套件项：**V-5** kept 计数守卫绑在来源未绑谓词——fixture 给每个 ordered id 都写了 out.txt，把 `.is_file()` 谓词整个删掉 12 测试仍全绿；p4-doc 上该 mutation 打印 23 而实存 13（比 L-3 原病还糟一档）。**字节已供**：fixture 去掉一个 ordered id 的 out.txt（kept 断言与 survivors 循环两处随调）。**V-3** 双前缀第四实例 `test_run_v2_template_retire.py:105`（fixture docstring 写 `chk-<id>.out.txt`）——`E7` 同类清扫漏网 | 下一批碰 retire 套件或其 fixture | v3-review-verify-de8f4ef.md V-5 · V-3 |
| readme-three | README:12 「the three step scripts additionally take the round's refs」悬在四条 invocation 上（第四条 retire 零 round flags）；FULL d52f41b L-1 明言不以 executor 披露作 discharge，修腿未动。句字面仍真（恰好那三个吃 refs）但按句数脚本的读者会漏数 retire。字节方向：限定成 the three round steps (evidence, bind, repair) 类 | 下一批碰 run-v2 README 实例化段 | v3-review-full-d52f41b.md L-1 后半 · v3-review-verify-de8f4ef.md V-4 |
| status-key | `check_subject` 的 CLOSED carve-out 钥匙是 run **自写**的 `status` 字段，本路径零交叉核对（其余四个身份字段都对 CandidateRecord 核；`flow.check_state_pointers` 会要求 CLOSED 携 `final_decision_ref`/`summary_ref` 但不在此路径——callers 仅 `rsclib/document_harness/cli.py:346`/`flow.py:307`（前者原 `rsc.py:309`，随 R2 的 CLI 摘出而改址））。实测同场景一词之差：EVIDENCED+缺一份 → review exit 1；CLOSED+缺同一份 → review exit 0。补偿控制真实但在另一模块且响不硬（dispatch 腿 `NOT-DISPATCHABLE` exit 1）；具体代价 = `dtw review --subject` 不再能抓「递错 commit」（递了 closing commit 得干净报告）。用户裁（2026-08-10）：不加机器（`E6`），bank | **deadline = 第一个真实 run 的 closeout 评审**（递错 commit 可能性首次真实存在的时刻），或下一批碰 `check_subject` 的 status/carve-out 面，孰先 | v3-review-full-d52f41b.md O-1 |
| delta-prose | 「A future run's directory carries its delta and not the shared core」（A2 plan acceptance）现仅**散文承载**（run-v2 README + 修后三 docstring 同向），零机械 enforcement：已批的 write_scope 设计判断挡的是「候选**改**模板」，而 run **抄**模板写在自己 control root 内完全 conformant，warrant 够不着；`check_template_instance.py`（唯一 pre-START gate）只查 spec 版本/preamble/paragraph-map，不看 run 目录携带什么脚本。要不要加守卫是设计题（`E6` 倾向不加——需要新机器的修是重新质疑被守之物的信号；`R5` 归用户裁） | 首个新 run 起草时（散文成唯一防线的第一次实测），或下一批碰 `check_template_instance.py` 的 gate 判据面，孰先 | v3-review-full-7dd027e.md L-2 |
| submod-index | `candidate_path_check.py` 经 `TrackedPaths.from_index` 用 `git ls-files` 建 tracked 索引，**submodule 内容不在调用者仓 index 里**（那里只有一条 gitlink），故任何指向 submodule 内部的路径写进候选文档即被**误拦**。R3 挂载当场实证：`HARNESS-POLICY.md` 里一条**真实存在**的 submodule 内 hooks 目录路径被 block（假阳），同一次里 plan 的一条本就写错的相对片段一并被 block（真阳）。与 `qp-index` 同形同档（漏扫是漏，误拦招 `--no-verify`），根因不同：那条是 quoting，这条是**扫描面不含 submodule**。**当前绕法只是措辞**——policy 与 plan 都改成不写完整的 submodule 内路径，这个绕法会随 submodule 内路径进入日常文档而失效。**无字节**：修在 harness 侧（`paths.py`），而「checker 该不该认 submodule 内部路径」是设计题（`R5` 归用户）；本轮零字节落 harness 仓，故不能当场修 | 下一批碰 `paths.py` / `candidate_path_check.py`（同 `qp-index`）；**deadline 已于 2026-08-18 到达且未付**（`SPLIT-COPY-RETIRE` 重写了 42 条指向 submodule 的链接，误拦真的发生了——七个 token 被 `candidate_path_check` 拦下，绕法是去反引号，见 rider `decited-paths`）；重定为下一批碰 `paths.py` / `candidate_path_check.py`。原文写「deadline = 重扎根轮」，那轮必然要重写指向 submodule 内的路径，届时误拦是必然而非偶然 | R3 施工实测（`V3-SPLIT-R3-WIRING-v1` 正文） |
| mount-inert | `.githooks/pre-commit` 把三支守卫的目录硬编码进一个 `H=` 变量（挂载点内路径），每次调用都带 `-f` 守卫，故该路径一旦解析不到就**静默跳过、零输出**。重扎根轮要在 harness 仓内去掉 `ResearchSystem/` 前缀，那一刻 `$H` 解析不到任何东西，调用者**同时失去三支守卫且不会有任何信号**——正是 `HARNESS-POLICY.md` §3 写下那三行想防的失效形态，而策略文件只告诉人「应该跑什么」、在没跑时不发信号。同一轮还会删掉调用者副本，届时 `layer_path_check` 的 `LAYER` 里九条调用者路径与 `E10` 成员句描述的文件将只存在于挂载点下。**无字节**：让 hook 在脚本缺失时硬失败、或要求 `$H` 随重扎根同改，都是给现有规则加一条要求 = design | 修法为 design 形状（让 hook 在脚本缺失时硬失败＝给规则加一条要求），故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `.githooks/pre-commit` 的 `H` 变量或 `layer_path_check` 的 `LAYER` 的批；**deadline = 去前缀那一件**（`$H` 第一次解析不到的那一刻；原文写「重扎根轮」，该轮 2026-08-18 拆成三件，去前缀是其中未做的一件——`SPLIT-COPY-RETIRE` 没有动 `$H`，故此 deadline 尚未到达）| v3-review-full-eb6fbc2.md `O-1` |
| pin-drift | 两棵树在 pin 上逐一相同（**在 `eb6fbc2` 的 tip 上由评审员实测**：当时的九成员 + 三守卫 + `cli.py` 全 SAME；此后成员增至十、守卫减至二，该实测是那个 revision 的事实而非现状），但**没有任何东西维持它**：一个 `E10` amendment 落在调用者侧之后，到 resync commit 加 gitlink bump 之间，真正执行的守卫是 pin 里那份旧的、不是刚改的那份。`HD-34` 的纪律覆盖的是反方向（调用者不得改 harness 内容），这个方向无载体。**无字节**：加一致性检查是新机器（`E6`），该不该做归用户（`R5`）| 修法为 design 形状（加一致性检查是新机器，`E6`），故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `HD-34` 纪律面或 resync 流程的批；**deadline = 第一个在调用者侧落 `E10` amendment 而未同步 pin 的轮次** | v3-review-full-eb6fbc2.md `O-4` |
| design-route | 已签的 `ResearchSystem/document-harness/split-design.md` §2 提议「`EXCLUDE` 新增 submodule 目录」，R3 实现改用独立的路径式常量（更好，且在 commit 正文与 plan 都披露），于是**签过的文本描述了一条没走的路、且无任何标记**。`HD-40` 使十节成为 R1–R4 的执行依据并要求对该文件的实质修改重签，故不能顺手改正。后果：日后拿实现对签字稿核账的人撞见不一致，而其解释只活在 commit 正文里。**用户 2026-08-17 裁乙：入 bank 等打包批**，不为此单独重签 | 打包批（该设计稿 §10.5 立项那批），或下一次因别的实质修改重签该文件，孰先 | v3-review-full-eb6fbc2.md `L-4` · 用户裁决 2026-08-17 |
| decited-paths | 本仓被删副本后，调用者侧 markdown 链接的**文本**是坏路径（**十处**来自 `SPLIT-COPY-RETIRE` 的去反引号扫类，**另加至少一处**由该轮 `B-2` 修复自身引入——`assurance/README.md` 的 `templates/run-v2/`；VERIFY `V-1`(b) 指出十处这个数在写下时已经旧了，**兑付时按当时的 base 重数**），而它们**只是被去掉了反引号**，不是被修好：`candidate_path_check` 只判反引号 token，去了引号它就看不见，于是十处「指向本仓不存在的文件」的字样对检查隐形。写成挂载点内路径会被同一支守卫误拦（rider `submod-index`：`TrackedPaths.from_index` 走 `git ls-files`，submodule 内容不在 index 里），所以今天无可用字节。**真修依赖 `submod-index` 先落地**——让 checker 认 submodule 内部路径之后，这十处改成真路径即可 | 随 `submod-index` 兑付的同一批，或下一个碰 `candidate_path_check` / `paths.py` 的批，孰先 | v3-review-full-2d148f3.md `L-2` |
| layer-crossrepo-token | `layer_path_check` 的判据是「反引号里以 ResearchSystem/ 开头的 token 必须从本仓根解析得到」，而拆仓后指令层**必须合法地点名另一个仓的路径**——`EXECUTION.md` 的按仓枚举句写了四个 caller 侧电池脚本路径，并在同一句声明 do not resolve here。读者在 throwaway clone 里把该成员改动 stage 后实跑：**exit=1，四条全中**；负对照（去掉那四对反引号，其余一字不动）**exit=0**。故这道 guard 在该类上的全部约束力就是那对反引号，而唯一现成的绕法只是让它看不见、不改变任何路径是否解析（与 `decited-paths` 同形、方向相反）。**原文写「今日不咬人：该 check 自陈 currently runs nowhere，本仓无 hook」——该前提已于 2026-08-19 失效**（`CALLER-ONBOARDING` 轮把 guard 接进本仓的 tracked `.githooks/pre-commit`，README Local-enforcement 行同批更正）。**故本行 deadline 已到达且未付**：今天不咬人只剩一个更窄的理由——guard 只扫 staged diff 的新增行，而那四个 caller 侧路径是存量行；下一个重写 `EXECUTION.md` 按仓枚举句的批会当场被挡。**无字节**：教 guard 认跨仓形式、或禁止层文本点名 caller 路径，两条修法都改变规则要求 = design | 修法为 design 形状，故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `layer_path_check` 判据或 `EXECUTION.md` 按仓枚举句的批；**deadline = README 称为 open 的那次 re-homing 落地**（guard 被接进本仓＝它第一次挡住一个正确 commit 的时刻） | v3-cold-read-28501fe.md `L-1` |
| layer-outbound-refs | 指令层自身**四处引用（三个不同目标）在本仓解析不到**（`acbc553` 后由本轮 executor 独立重测；量程 = 十成员全文的 markdown 链接目标 + 反引号路径 token，「解析得到」以**落在本仓内**为准——经 `..` 逃出挂载点落到调用者的那种不算）：`REVIEW.md:45` 指向 `v3-review-full-fef3a2e.md`（*What is not in the subject* 一节的唯一实证出处，markdown 链接）· `EXECUTION.md:186` 与 `:449` 两处 assurance/runs/p5a-shells 的 audit-rounds token · `EXECUTION.md:452` 一处 p4-doc 的 issue JSON。**部分兑付 `acbc553`（轮 `LEDGER-SPLIT`）**：README《Authoritative documents》与 Predecessors 的两条 `.goals/plans/*` 链接随 plan 迁入本仓一并改指，本行原枚举的六处去掉这两条；README 现零 `.goals` 命中。余下一处是 markdown 链接——`layer_path_check` 只认反引号 token，**从设计上就看不见它**（同读 `O-4`）；两处反引号 token 早于本轮。挂载点在 ResearchSystem/harness/，故 `../../` 落回 harness 仓内，两种挂载语境都够不着。**不属本行的同形不解析件**（同次重测一并见到，另有归宿）：`EXECUTION.md` 分层节的五处调用者路径，其同一句已自陈「the five paths that follow are the caller's and do not resolve here」· `README.md:35` / `REVIEW.md:138` 的 `.harness/review-pending.json` 是 `dtw dispatch` 的运行期产物 · 两份 supersession 的四处缺前缀 token 在 `E2` 冻结面上，按 `HD-20` 另 bank。**无字节**：替代形式（改成名而非路径 / 加「在调用者仓」注 / 去链接）是设计题；`R5` 半边归用户——拆仓之后本层还该不该指向调用者持有的 authority | 兑付 `decited-paths` / `submod-index` 的同一批，或下一批碰 `REVIEW.md` 或 `EXECUTION.md`，孰先（原写「下一批碰 README《Authoritative documents》表」，该触发已于 `acbc553` 到达并付掉其中两条，剩下三处不在那张表上）；**deadline 已到达且早于本行**——这些链接自 `345acdd`（本仓第一个 commit）起就是死的，非任何一轮造成，比照 `submod-index` 的已到达未付形状记 | v3-cold-read-28501fe.md `L-2` + `O-4` · v3-review-full-e74be07.md `L-1` |
| e1-disclose-home | `E1` 的中间态披露句（本轮新加）要求「the round **states in its record** which of the four the executor held」，但**没说记在哪、也没说谁记**：commit 正文 / 轮 journal / 预览卡三者都是候选，`E3` 对另一条义务的先例写的是「the commit body or the round journal」；`ORCHESTRATION.md` 的九条义务表**没有收这一条**，而 `E1` 本身挂在同时绑 orchestrator 与 executor 的 `Execution side` 节头下。于是本层最新的一条 bound 既无载体也无责任人。本轮三次记录都写在 commit 正文里（自定，非规则要求）。**无字节**：指定载体或责任人都是给 `E1` 加 bound = design | 修法为 design 形状，故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `E1` 中间态句或 `ORCHESTRATION.md` 义务表的批；**deadline = 下一个必须遵守该句的构造轮**（本轮之外的第一个，按 `HD-37` ① 不在创造本行的轮内） | v3-cold-read-53fcf88.md `O-5` |
| charter-qualifiers | `ORCHESTRATION.md` 九条 cite-only 行里有**三行压掉了被引规则自带的限定**：`E10` 的开轮豁免（unless the user waives it）· `E11` 的 wait 条件（unless told otherwise）· `R10` 的前提（只在 FULL 返 `REVIEWED_NO_BLOCKER` 带 low 时才有那个选择）。三处方向**都是更严**，故不会放行不该放行的事，但与被引规则**不等价**——而该表自陈的契约恰是「read the rule」，即指向即全部。改变的决定：一个只读该表的 orchestrator 会以为豁免不存在。**无字节**：补限定＝把规则抄进第二个成员（正是本文件收窄形要避免的），删该行＝减义务，两个方向都是 design | 下一个有资格开轮且碰 `ORCHESTRATION.md` 义务表的批；**deadline = 用户第一次真给开轮豁免**（三处里第一处会当场咬人的那一刻，与 rider `waiver-live` 同一时刻、不同缺陷） | v3-cold-read-50016a8.md `O-2` |
| charter-prose-overreach | `ORCHESTRATION.md` 的两处论证散文说得比依据多，均由 read `53fcf88` 点出、均无字节，且**收批时两条都没被处置**（本行即其归宿，preclear 补记）：① **:13 引 `E10` 说它 forbids re-typing member text with the same content**——`E10` 那句管的是**本层怎么被修改**（Its **edits** are additive or subtractive…），不是「一个成员能不能复述另一个成员的规则」；该句要下的结论由同句 `HD-5` 那半独立承载，故下游零变化。读者**明说这条该摆给用户而非当场改**，因为 `HD-46` ② 用了同一措辞，动文本会连带动一条已记裁决。② **:32「no rule in this layer answers it either」对 product-run executor 说过头**——`EXECUTION.md` 的 Instruction authoring rules 要求「the rules the session runs under … live in `EXECUTION.md`；the instruction references them from its non-normative Context section」，那就是一条交付路径；真正无人回答的只有**构造轮 executor** 那半 ③ **:70「that pointer is the **only** discovery path a cold orchestrator has」是一个没带量程的绝对量词**（`HD-41` ② 的形状，由本轮 executor 自己写下），第三次 read 按 `R4` 标为 `UNVERIFIABLE`——只证到「本机确有那个指针」，没证也无法证「唯一」。三条同属一类：charter 的论证散文越过了它能站住的依据 | 下一批碰 `ORCHESTRATION.md` 的 §thin 开头或 Handing the executor its instruction 节；**deadline = 用户对「construction executor 的 charter 要不要有载体」的裁决**（那一刻 ② 必然重写，① 与 `HD-46` 一并处置，③ 顺手带量程） | v3-cold-read-53fcf88.md `O-3` + `O-4` · v3-cold-read-50016a8.md §5（`UNVERIFIABLE` 项） |
| six-signed | **已签的两处仍按被取代的读法写命令面**：`document-harness/split-design.md:44`「六命令原样」（签字 `HD-40`）与 `document-harness/io-design.md:115`「六个命令中五个纯读」（签字 `HD-35`）。自 `HD-47`（第七个命令 `init`）起两句在字面上与现状相抵，而 `HD-47` 之所以停在 `live` 正是以此为条件。**无字节**：实质修改已签文件欠重签，不能顺手改；先例＝rider `design-route` 的处置（入 bank 等打包批，不为此单独重签） | 打包批（`split-design` §10.5 立项那批），或下一次因别的实质修改重签该二文件之一，孰先；**无 deadline**——错的是计数不是规矩，且 `HD-47` 在决策簿里替它们说话 | v3-review-full-2026a14.md `L-3` · `CALLER-ONBOARDING` 修腿的 `L-1` 扫类 |
| readme-cli-stale | 仪器仓**根** `README.md` 三处 CLI 断言为假，且三处恰为全部（FULL 实测并复核计数）：`:38`「Re-rooting is R2's work, together with this repository's own command-line entry point」·`:59`「The CLI is not here.」·`:61`「the CLI extraction alone will not make the suite green」。均由拆分批 R2（2026-08-17 摘出 `dtw`）与今日 `733 passed` 证伪，非 `CALLER-ONBOARDING` 造成。讽刺处：该节自陈 carries commands instead of claims，这三条正是它要拒绝的形状 | 下一批碰根 `README.md` 的 *State of this repository* 段；**deadline = 第二个调用者按 `ONBOARDING.md` 读这个仓的时刻**（该文件已把根 README 列进入口路径，故新调用者必读到 `:59`） | v3-review-full-2026a14.md `O-3` |
| self-caller-guards | 仪器仓的 `.githooks/pre-commit` 只跑 `layer_path_check`，而该仓对自己的构造轮**也是调用者**：`dtw dispatch` 在此写 `.harness/review-pending.json`（`CALLER-ONBOARDING` 一轮四次），故 `E9` 的冻结窗口在该仓**零机械执行**，`candidate_path_check` 同理不跑。加不加按 `R5` 归用户，**用户 2026-08-19 裁不进 `HD-48` 那批** | 下一批碰仪器仓 `.githooks/pre-commit`，或 **deadline = 该仓第一次在冻结窗口内落下非记录 commit**（`E9` 违反第一次真发生的时刻；不在本轮内——本轮四次窗口全由 orchestrator 手工守住） | `CALLER-ONBOARDING` executor 报告第 2 条 · `.githooks/pre-commit` 自陈段 · 用户裁决 2026-08-19 |
| posix-mode-wording | `ONBOARDING.md` 第 9 条新加的模式句说 POSIX clone 对 `100644` 的 hook 是 **silently** stops running it，而 `v3-review-full-eb6fbc2.md:184` 记的是 *skipped with a warning*；本机无 POSIX checkout，VERIFY 按 `R4` 标 `UNVERIFIABLE` 而非折进 supported。改变的决定：照这句判断「有没有信号」的人会以为完全无声 | 第一次在 POSIX 机器上克隆本仓（一条命令即可判），或下一批碰 `ONBOARDING.md` 第 9 条，孰先；**无 deadline**——两读法都指向同一个动作（commit 成 `100755`） | v3-review-verify-4029b43.md `O-2` |
| chk-caller-prefixes | `tooling/hooks/candidate_path_check.py` 的 record / specification 豁免是**硬编码前缀**（`RECORD_SURFACE` / `SPECIFICATION_SURFACE`），写的是长出本 harness 的那个调用者的目录名。新调用者若把评审记录放在别处，其记录会被当 work product 扫，于是「记录引用了它所报告的那条坏路径」本身被挡——rider `freeze-audit` 记的互锁形状换到新调用者身上重演。`HD-34` 禁止调用者就地改仪器，故修法只能在本仓 | 下一批碰 `candidate_path_check.py` 的前缀常量，或 **deadline = 第二个真调用者接线该守卫的时刻**（`ONBOARDING.md` 第 9 条把它列为调用者可选两支之一，故接线即撞上） | `CALLER-ONBOARDING` executor 报告第 6 条 · `ONBOARDING.md` 第 9 条第二个 bullet |

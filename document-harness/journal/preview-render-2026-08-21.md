# Round journal — PREVIEW-RENDER (2026-08-21)

The construction round that executed the second half of the `E11` carrier ruling
(2026-08-21, recorded in `CONSTRUCTION-LEDGER.md`'s open-items block): a product run's
authorization body is its frozen control plane, and the plane's human-readable rendering is
scripted — deterministic, no LLM, re-derivable at any time, therefore never stored. The
deliverable is `rsclib/document_harness/preview.py` plus the eighth command `dtw preview`;
`EXECUTION.md`'s SIMP-C4 bullet now names the script as what renders the START card.

## The chain

cold read `v3-cold-read-dd22789.md` (0 must-fix · 1 low · 3 observations; all ten members
read end to end, the `9fe60b9` deferred read discharged) → candidate `57d1312` → FULL
`v3-review-full-57d1312.md` **CHANGES_REQUIRED** (B-1 boundary-as-dict-keys · B-2 elision
swallows nested sections; 4 lows · 5 observations) → one user-approved fix `15a53fe`
(full-package boundary, ruled 2026-08-21) → targeted VERIFY `v3-review-verify-15a53fe.md`
**REVIEWED_NO_BLOCKER** (2 lows, both in the new tests with exact bytes; 3 observations) →
free-channel byte application of V-1/V-2 (E10 channel, consumes nothing). `E9`: three legs
walked — one FULL, one user-approved fix, one targeted VERIFY, nothing else consumed.

## In-round user rulings (2026-08-21, three-option card + fix-boundary card, in session)

1. **`dtw preview` admitted as the eighth command** — the per-case ruling `HD-47` requires;
   register entry `HD-51` (its `basis` is this journal plus the candidate body, per FULL
   `L-3`, which found the ruling living only in the commit body).
2. **`EXECUTION.md` SIMP-C4 wiring sentence: yes** — carried by the layer text itself, so
   no register entry; the sentence is the carrier.
3. **Fix boundary: the full package** — B-1 + B-2 + L-1 + L-2 + O-1 + the `E1` disclosure.
4. **VERIFY V-1's `E6` aside: the boundary counts are deleted** (a derived output nothing
   bound; the lists render alone and the whole-line assertions bind them).
5. **VERIFY V-3: no catch-all is added** (`E6` — the reachable class is closed; the other
   seven commands share the same two-exception shape; the record carries the observation).

## `E1` — which of the four holdings the executor held

Orchestrator and executor were one work-side session this round. All four of `R1`'s
holdings — dispatched by, prompted by, scoped by, reported through — sat with that single
session in the operational sense: it ran `dtw dispatch`, handed each reviewer the CLI's
derived output verbatim with the standing contract as the prompt, and committed the
returned records unchanged. Stated here and in the fix commit body; the round does not
call its reviews structurally independent. The reviewers ran cold and re-derived
everything, which is a discipline, not a structure.

## Finding dispositions beyond the fix leg

- **cold read `L-1`** (stale rider row `review-record-loc`, its banked defect already
  deleted by `bba6f94`): user ruled 2026-08-21 — delete the whole row, late redemption
  stated in the deleting commit's body. The residual half-claim (the migration-directory
  convention holds in only two repositories) is a reminder for a third caller that does
  not exist, and `ONBOARDING.md` is that caller's formal entry when it does.
- **cold read `O-1`** (REVIEW.md item 1 holderless caller artifact): the supplied bytes
  rode the candidate per `R9` — fixed at `57d1312`.
- **cold read `O-2`** (guard-probe datum): annotated onto rider `e10-cannot-see` in the
  closeout's riders commit — redemption edits two sentences, not one, because the
  class-entire quantifier above the blind-spot clause is falsified by the same probe.
- **cold read `O-3`**: reproductions of live riders `e1-table` / `onboarding-labels`; no
  action, the rows already carry the debt.
- **FULL `L-3`** → register entry `HD-51` at closeout, precedent `HD-47`.
- **FULL `L-4` / `O-5`** → answered by the `E1` disclosure above and in `15a53fe`.
- **FULL `O-2`** (unaccounted +1 test between batteries) → corrected in `15a53fe`'s body:
  the test was `NamedIssueReachability`.
- **FULL `O-3`** (scan-evidence locator drift + short enumeration) → `R9` wording-level;
  its subject is commit-body practice, which has no file target a rider row could name, so
  the record and this line carry it. The practice correction is absorbed going forward:
  scan output pasted post-tree, locators re-derived at the tree the scope names.
- **FULL `O-4` and VERIFY `V-4`** (multi-paragraph bodies vs `E8`'s "one dense paragraph")
  → already answered by the standing 2026-08-07 ruling (`CONSTRUCTION-LEDGER.md`
  已裁-conversation-only list): the clause buys density and no trailers, not a literal
  single paragraph. No action.
- **VERIFY `V-1` / `V-2`** (both with exact bytes) → the free-channel application after
  the record landed: whole-line boundary assertions over unequal-length fixture lists
  (counts deleted by ruling 4), the second nested heading, and the appendix case; both of
  the VERIFY's surviving mutations (M6 min→max, M9 label) re-run and now red.
- **VERIFY `V-3`** → ruling 5: not added; the record carries it.
- **VERIFY `V-5`** → this closeout: `HD-51` and the `RA` count annotation land here.
- **rider `RA`**: its 「`dtw` 七命令」 count goes stale again with the eighth command,
  substance unchanged — `preview` is not the machine-check entry either. Count annotated
  in the closeout riders commit, precedent `v3-review-verify-4029b43.md` `V-1`.

## Layer debt this round leaves

`EXECUTION.md` and `REVIEW.md` changed inside the round (design under ruling 2 and `R9`
bytes respectively, then reviewed by the FULL); their independent `E10` read rides the
next round's opening layer read — the DE-PREFIX / INIT-SURFACE precedent.

# Round journal — `STRANGER-PROOF` + batch `SUBMOD-HOOKENV` (2026-08-24, closed together)

> Closeout dispositions for the two work units that shared one FULL (user consolidation
> ruling of 2026-08-24). The evidence artifacts are their own files:
> `stranger-proof-walk-2026-08-24.md` (the nine-item walk, pasted outputs) and
> `submod-hookenv-2026-08-24.md` (the fix batch's journal with the B-2 errata and the
> remote-pinned quickstart re-walk). Plans: `stranger-proof.plan.md` ·
> `submod-hookenv.plan.md`. Records:
> `v3-{cold-read-21dad76,checkpoint-read-153302a,review-full-3d5c705,review-verify-3149581}.md`.
> Written by the orchestrator at closeout; figures quoted from records, none new.

## What closed

The batch C promise is now walked, not asserted: a fresh repository on a deliberately
different layout followed ONBOARDING end to end; three documentation defects the walk
caught were fixed in the candidate, and the fourth — the guard's submodule recognition
being dead (then measured *inverted* under `git commit -a`) in the pre-commit hook, its
only real runtime — was fixed by the follow-on batch with the env-var list asked of
`git rev-parse --local-env-vars` rather than hardcoded. The root README now opens for a
human stranger, and after the FULL's B-1 its quickstart was re-walked verbatim from the
**published remote** (closing the local-clone ceiling the first walk carried). Battery
792 → 851 across the two units; CI had already run green three times by push before this
closeout — the ledger's "首跑等用户 push" note went stale at the first of them and is
corrected in this closeout.

## The swallowed commit — attribution errata (path 1, user ruling)

`0133d1b`, titled ledger-only, carries 587 lines of the `STRANGER-PROOF` candidate
(README rewrite, ONBOARDING fixes, walk record, rider rows): the orchestrator committed
while the executor session was staging into the same git index. Content verified intact
(per-file, CRLF-normalized) by the executor at `e620b43`, which also carries the owed
candidate-body disclosures; the FULL recorded the mixture as an observation with no
executable fix under `E8`. The user ruled path 1: history stands, this paragraph is the
attribution record. **Standing lesson (second of the day after the dispatch-range one):
the orchestrator lands no commit while any executor session runs in this worktree — the
shared index makes every commit a sweep.** The earlier same-day plan section that
recorded overlap rules *after* their dispatches had run was itself ruled an ineffective
carrier; the scheduling carrier for making any of this mechanical is the ledger's
dispatch-economy backlog line.

## Process dispositions with no other home (`L-4` of the FULL, and the read's `L-1`)

- The session-side memory mirror recorded in `be03c55` was deleted the same day under the
  user's ruling **本仓禁用 memory 承载** (2026-08-24): all discipline carries in committed
  state only; the project memory directory holds a single tombstone line saying so.
- The same day's two rulings — dispatch form (now `E1` text itself) and the one-time
  waiver of the round apparatus for the `E1` keyword edit — take **no register entry** by
  the user's explicit "都不立"; their carriers remain the commit bodies (`1a0a200`, this
  journal). This paragraph is the disposition note, deliberately not a register entry.
- The first FULL dispatch of the shared subject was cancelled on the user's order before
  any record landed — per `E9`'s own clause it never occurred and consumed nothing; the
  orchestrator removed its freeze marker and disclosed at `af002e2`.

## VERIFY observation, recorded without action

`HD-41` ④'s scan-paste was absent from the fix commit body; the verifier ran the three
class scans itself, all clean, output in its record. The 2026-08-17 reaffirmation chose
discipline over machinery for exactly this shape; the streak bookkeeping lives in the
rider bank's history (`fixleg-scan-paste`, redeemed 2026-08-22; `fixleg-scan-raw`,
redeemed 2026-08-23).

## Honesty boundaries

- Same machine throughout; the walker and re-walker are agents. Closed: "the flow works
  as documented, on a second layout, pinned from the published remote." Still open: a
  human stranger on another machine (the `CALLER-ONBOARDING` caps stand; the interpreter
  divergence the re-walk hit live — hook probing `python3` while step 0 installed into
  `python` — is exactly the warned case and is documented, not solved).
- Environment measurements are one git version (`2.48.1.windows.1`); the tests observe
  git rather than assert it, so a divergent git reddens loudly.
- Riders: `submod-hookenv` redeemed in the fix batch's candidate; `discover-root-env`
  (same env class, hook-unreachable today, bytes ready) banked; the walk's three smaller
  findings banked (`move-cost-member-site` / `onboard-clone-decl` / `onboard-cmd-count`);
  `decited-paths`' caller-side redemption is now actually unblocked — the hook-context
  fix was its stated prerequisite.

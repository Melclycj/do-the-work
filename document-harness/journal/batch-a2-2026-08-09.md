# 批 A / A2 · R0.1 —— AMBIG 存活审计（2026-08-09）

> **这是什么。** plan `harness-a2-construction.plan.md` 的 R0.1（`HD-17`）：对七个 v2/v1 遗留树
> 逐一查「还有没有活消费者」，只读、不构造、无轮、无 revert unit。产物 = 本文件 + 摆给用户的
> travel / stay / retire 裁决表。判据先写死（§0）再套用；全部数字测于 HEAD **`8e018e1`**，
> 工作树对这些路径干净（唯一脏文件是另一 session 在改的
> `.goals/plans/research-system-p5c-p8-revision.plan.md`，与七树无交、未入本次任何 commit）。
> 形状照 A1 journal：实测在前，判断在后；凡计数皆由命令产出并给把手（§10.3 的教训）。

## 0. 判据（应用前写死）

- **live consumer**：已提交字节里存在今天的正常操作就会 import / 执行 / 读其字节 / 按其规则
  行事的消费者——代码 import 链、注册在案的 command、现行回归电池、指令层规则任一即算。
- **records-only**：仅被已关闭的 record（migration 档、closed run、journal、旧 plan、archive）
  以路径或链接提及；今天的操作不触碰它。
- **dead**：连 records 都不指它。
- 行数口径承 A1 §0：逐文件 `data.count(b"\n")`；文件集 = `git ls-files -- <tree>`（自动排除
  未跟踪的 `__pycache__`）。复核命令：对每树 `git ls-files` + 逐文件换行字节计数。

## 1. 先把 A1 的总账对上：AMBIG 138 = 七树 + `rsc.py`，精确闭合

| 树 | files | lines | A1 §13.5 记 |
|---|---|---|---|
| `ResearchSystem/harness/` | 14 | 580 | 14 / 580 ✓ |
| `tooling/rsclib/harness/` | 11 | 1,421 | 11 / 1,421 ✓ |
| `tooling/tests/harness/` | 1 | 754 | 1 / 754 ✓ |
| `schema/harness-v2/` | **81** | 3,607 | **15**（无行数）✗ |
| `migration/general-harness-v2/` | 26 | 3,192 | 26 / 3,192 ✓ |
| `migration/stage-control-refactor/` | 2 | 217 | 2 / 217 ✓ |
| `stages/` | 2 | 365 | 2 / 365 ✓ |
| 七树合计 | **137** | **10,136** | |
| + `tooling/rsc.py` | 1 | 856 | 1 / 856 ✓ |
| = 合计 | **138** | **10,992** | §13.3 AMBIG：**138 / 10,992** |

**两个结论一次拿到。** 其一（更正，按 `HD-23` 落本 journal、A1 原行不改）：§13.5 表里
`schema/harness-v2/` 的「15 files」只数了顶层——递归后是 81，`fixtures/` 子树占 67 件
（case JSON + `validate_fixtures.py` + `cases.json`）。其二（对账）：改用递归数后，
七树 + `rsc.py` 与 §13.3 的 AMBIG **file 数、行数双双精确闭合**——AMBIG 那 138 件就是
`HD-17` 点名的这七树加 `rsc.py`，一个不多、一个不少。§13.3 的总数一直是对的，错的只是
§13.5 那一格的展示口径。

## 2. 接线实测——谁 import、谁执行、谁读字节、谁链接

把手：`rsclib[./\\]harness`（*.py 全仓）· `harness-v2` · `issues[/\\]registry` ·
`ResearchSystem[/\\]stages` · `harness-v2[/\\]fixtures`（限 v3 migration 树）·
`nodes[/\\]A1[/\\]validate` · wikilink 试探（双方括号语法 + 七树内 md 的 stem）。逐项：

**① `ResearchSystem/harness/`（profiles ×5 · adapters ×4 · issues/registry.json · README ×4）**
- 读字节（活）：`rsclib/harness/cli.py:20-21` 硬编码 `PROFILES_DIR`/`ADAPTERS_DIR` 指向此树；
  `tests/harness/run_tests.py:36-37` **import 时**就 `load_published()` 真读两目录，`:696-697`
  再把真 profile/adapter 文件收进 c14n corpus——39 测试每跑一次，此树被读一次。
- validate（休眠）：`schema/harness-v2/fixtures/cases.json:38` 拿真 `harness/issues/registry.json`
  当 POS case（runner 状态见 ④）。
- `registry.json` 内容 = `{"registry_version":1,"issues":[]}`——**空**。v3 的 HarnessIssue 落
  `assurance/runs/<run>/issues/`，全仓 `issues[/\\]registry` 仅 fixture 一处命中：v3 不写这里。

**② `tooling/rsclib/harness/`（11 个 .py）**
- `rsc.py:50` 顶层 `from rsclib.harness import cli as harness_cli`，`:739` `harness_cli.register(sub)`；
  同一 import 块 `:48` 还顶层拉 `stage_close, stage_control`（v1）。
- `tests/harness/run_tests.py:28-33` import 六个子模块。
- v3 侧零 import：`rsclib/document_harness/__init__.py:12` 自述「v3 代码 import `rsclib.harness`
  即 `SPEC_GAP`」，grep 复证 `document_harness/` 无一命中（与 rider `CLI-hist` 的实证一致）。
- `cli.py:1` docstring 自述身份：*[historical] Side-by-side v2 CLI surface (A3), superseded by v3
  (Phase D, 2026-08-01) and kept, with its tests, to replay existing v2 records*。
- 杂项：`__pycache__/` 里有已删模块 `feedback.py`/`review.py` 的 stale `.pyc`（未跟踪，不计数）。

**③ `tooling/tests/harness/`（run_tests.py，39 测试）**
- 执行记录：journal `c2/c3/c4/layer-inc/d`（2026-07-30 → 08-01）与多份 FULL/VERIFY record 记
  `tests/harness => Ran 39 OK`；`.goals/plans/harness-digest-narrowing.plan.md:114` 把它与
  `tests/stage_control`（20）写进基线电池并在 `:279` 复测。
- **规则句缺口（不裁，记给拆分批）**：现行 tiering 规则（run-v2 README `:193-194`）对 full
  battery 的枚举是 *P2/P4/P5A goldens, schema fixtures, pytest, `compile --check`*——**没点名**
  39/20 两套；practice 与 plan 基线在跑，规则句没写。pytest 默认发现规则（`test_*.py`）也
  收不进 `run_tests.py` 这个文件名。
- rider `CLI-hist` 点名 `:725 test_cli_validate_and_resolve` 是 v2 CLI 的活测试。

**④ `schema/harness-v2/`（13 schema + README + fixtures/ 67 件）**
- 读字节（活）：`rsclib/harness/schemas.py:18-19` 硬编码 `SCHEMA_DIR`；`pack_digests()`（`:75-86`）
  读**全部 13 份 schema 字节 + `contract/General-Harness-Contract-v2.md` 字节**；
  `tests/harness/run_tests.py:39` `PACK = schemas.pack_digests()` **import 即读**；
  `resolver.py:272` 每次 resolve 把 pack digests 内嵌进 ResolvedStage。
- fixtures runner（休眠）：`validate_fixtures.py`（A3 记录 93/93）在
  `migration/document-work-assurance-v3/` 全树 **零执行记录**（`harness-v2[/\\]fixtures` 零命中）；
  各 v3 record 里的 `validate_fixtures.py` 行全是 P1 `schema/fixtures/` 或 N0 fixtures，另两个运行器。

**⑤ `migration/general-harness-v2/`（26 件：设计档 + A0–A3 node record）**
- inbound 全是 record / 旧 plan / 本批 paperwork：`HARNESS-LEDGER-archive`、v3 评审 record 引作
  先例、`.goals/plans/general-harness-v2-architecture-revision.plan.md`（A4–A7 从未执行，v2 于
  Phase D 被 v3 超越）、`HD-17`/A1 journal/A2 plan。
- `nodes/A1/validate.py` 可执行，但引用者全是本 node 自己的 review record（4 处）。
- 零代码、零电池、零指令层引用。

**⑥ `migration/stage-control-refactor/`（CTRL-BOOT-v1.md + pre-refactor manifest）**
- inbound 全集 7 个文件：自身 2 + 本批 paperwork 3（`HD-17` / A1 journal / A2 plan）+
  旧 v1 plan（`research-system-stage-control-refactor.plan.md`）+ A0 record 各 1。

**⑦ `stages/`（README + `_stage-record-template.md`）**
- 零代码硬编码：`ResearchSystem[/\\]stages` 在 .py 里零命中（`stage preflight --record` 是参数；
  stage-control fixtures 里的 `ResearchSystem/stages/sample-stage.md` 只是字符串值，不读文件）。
- 活的注册：`.claude/commands/rs-execute.md`（本仓、本 session 可调的 command）以 v1 Stage 流为
  操作序列，canonical Stage Record 的家就是此树（模板 frontmatter `record_ref:
  "ResearchSystem/stages/<stage-id>.md"`）；`rsc.py:674` 的 `stage` 组自述 *kept to replay
  existing Stage Records*。
- **从未被行使**：canonical Stage Record 至今零份（A0 基线即 absent，今日 `git ls-files` 仍只有
  README + 模板）；`generated/stages/` 只有 README（`--write-view` 从未留痕）。
- wikilink 试探：七树内各 md 的 distinctive stem 无一被双方括号 wikilink 指向（指向
  `General-Harness-Contract-v2` 的两处 wikilink 指的是 contract/，不在七树）。

## 3. 分类（判据 §0）

| # | 树 | 分类 | 一句话：谁、在什么时候消费 |
|---|---|---|---|
| ① | `harness/` | **live** | 每次 full battery（39 测试 import 时读 profiles/adapters）+ `rsc harness resolve` 默认目录 |
| ② | `rsclib/harness/` | **live** | **每次 `rsc.py` 任何子命令**顶层 import（含 `v3` 组）；39 测试；自述 replay-only |
| ③ | `tests/harness/` | **live** | full battery 成员（practice + plan 基线；规则句未列，见 §2③） |
| ④ | `schema/harness-v2/` | **live**（fixtures 子树休眠） | 39 测试 import 即读 13 schema + v2 契约字节；fixture runner v3 期零执行 |
| ⑤ | `migration/general-harness-v2/` | **records-only** | 只有 record / 旧 plan / 本批 paperwork 指它 |
| ⑥ | `migration/stage-control-refactor/` | **records-only** | inbound 全集 7 件，全是 record 与 paperwork |
| ⑦ | `stages/` | **注册在案、从未行使**（live-by-registration） | `/rs-execute` command + `rsc stage` 组挂名的 canonical 家园；零份 Stage Record 曾存在 |

七项无一 **dead**。①–④ 是一个连通件：①④ 是 ② 的数据，③ 是 ②①④ 的测试，② 是 `rsc.py` 的
import 子树。⑦ 与 ⑤⑥ 各自独立。

## 4. 裁决前要知道的四条边界事实（都不在七项清单内，但同一张网）

- **(a) `rsc.py` 的 import 共生**：`rsc.py:44-50` 顶层 import 意味着 **`rsc v3 dispatch` 每次
  运行都 load 全部 v1+v2 模块**——其中任何一个文件语法坏掉，整个 CLI（含 v3 通道）一起死。
  搬走 ② 必须同批动 `rsc.py`（`:48` `:50` `:674-736` `:739`），而 `rsc.py` 归属被 `HD-17`
  明令本轮不裁 → **①–④ 的 travel 可以今天裁，但执行落在拆分批（与 rider `CLI-hist` 同批）**。
- **(b) v1 stage-control 家族 32 件 / 7,150 行不在七项里**：`rsclib/stage_control.py`(951) ·
  `stage_close.py`(315) · `tests/stage_control/`(2/439，电池在跑) · `schema/stage-record.schema.json`(922) ·
  `schema/stage-control-fixtures/`(24/3,793) · `contract/Stage-Control-Contract.md`(349，已签只读，
  rider `SCC`) · `contract/General-Harness-Contract-v2.md`(370，**UNSIGNED CANDIDATE 却被
  `pack_digests()` 运行时读字节**——动它断 39 测试) · `generated/stages/README.md`(11) ·
  `generated/harness/`(空)。A1 §13.3 的三分类把它们归进了 HARNESS 或 PRODUCT（分组底表未落盘，
  §13.6 自认；不可复原）→ **裁七项时若不同时点名这 32 件与两份契约的去向，拆分批继承歧义**。
- **(c) 电池规则句与 practice 的缺口**（§2③）：39/20 两套在跑但不在 README `:193` 的枚举里。
  七项无论怎么裁，这句迟早要与事实对齐——归 `HD-14`（R1 搬六节时同一文件）或拆分批，不归 R0。
- **(d) 移走/退役后的悬空引用全部落在 records**：①–⑦ 的 inbound 除 §2 点名的活接线外全是
  已关闭 record 的路径提及——与 `HD-16` 已接受的「记录跟对象走、新仓不背只读路径」同型代价，
  不新增决策维度。

## 5. 诚实边界

1. **git 记写不记读**（A1 §12 同款）：「fixture runner v3 期零执行」= 零落盘证据，不等于物理上
   没人跑过；「live」判据反向同理——引证的是已提交字节里的接线，不是运行日志。
2. **⑦ 的分类是我造的第四档**：三分类装不下「活注册、零行使」，硬塞 live 或 records-only 都会
   误导裁决，故单列。用户可改判。
3. **(b) 的 32 件清单是我按「v1 stage-control 机器」圈的**，判据（同网络、不在七项）写死在先，
   但圈法本身是人判；A1 当时把它们归哪组已不可复原。
4. **未量**：七项 travel 后 `repo-audit.py` 对 records 里悬空**路径提及**（非 wikilink）是否报错
   ——wikilink 检查只管双方括号语法，路径提及的 lint 行为未逐类实测；此句本身待证，拆分批要量。
5. 本文件通篇不使用双方括号 wikilink 语法，避免给 basename 索引加新边。**顺带一条实测**（部分
   回答第 4 条）：`repo-audit.py` 的 wikilink 解析**连内联代码都扫**——本文件初稿在反引号里写了
   该语法的字面例子，即被 pre-commit 拦下（Broken wikilinks: 1）。改词根治，未绕过。

## 6. 摆给用户的裁决表（每项 travel / stay / retire；`E1`/`R5`：本轮不裁）

| # | 树 | 分类 | 各选项的实测约束 |
|---|---|---|---|
| ① | `harness/` | live | travel：与 ②④ 同批（cli.py 硬编码相对根）。stay：零成本。**retire 不可提议**（plan 明令 live 项不提议退役） |
| ② | `rsclib/harness/` | live | travel：必须同批动 `rsc.py`（§4a），落拆分批。stay：零成本。**retire 不可提议** |
| ③ | `tests/harness/` | live | 跟 ② 走（它测的就是 ②①④）。**retire 不可提议** |
| ④ | `schema/harness-v2/` | live | travel：`schemas.py` 硬编码 `RS_ROOT` 相对路径 + v2 契约字节依赖（§4b）同批处置。fixtures 子树（67 件、runner 休眠）可单独议。**retire 不可提议** |
| ⑤ | `migration/general-harness-v2/` | records-only | 三选皆开。travel/retire 的代价 = record 悬空（§4d）；retire 另受 `HD-6` 双条件（可从 record 反推 + 不再援引）约束 |
| ⑥ | `migration/stage-control-refactor/` | records-only | 同 ⑤，inbound 仅 7 件，最便宜的一项 |
| ⑦ | `stages/` | 注册在案、从未行使 | retire 需先裁 `/rs-execute` command 与 `rsc stage` 组的去留（= `CLI-hist` 的另一半）；travel/stay 则与 v1 家族（§4b）绑定 |

裁决落点：user ruling 后，travel/stay 项记入拆分批的成员账（`HD-15`/`HD-16` 之下），retire 项
（如有）各成一个带 revert unit 的删除批。本轮到此为止。

## 7. 裁决后补测（2026-08-09 同日；裁决本身落 `HD-24`，本节只存测量）

七项裁决经用户逐项确认，登记于 `HARNESS-DECISIONS.md` `HD-24`，`HD-17` 同 commit 转 retired
入 archive。呈表过程中用户追问逼出的三组补测：

1. **⑦ 的钉子是真 markdown 链接，不只是概念引用**：全仓搜链接语法指向 stages 的共 4 命中——
   `contract/Stage-Control-Contract.md:23`（**已签契约**链接模板）+ `ResearchSystem/README.md`
   `:35`/`:41`/`:42`。单删 stages/ 即 repo-audit「Broken markdown links」硬失败——「直接删」
   不存在，处置必须连已签契约那行一起裁（用户裁：归拆分批与 v1 族同批）。
2. **⑤⑥ 外向引用量级**（子串计数，口径同 A1 §13.6 第 2 条——量级可信、精确值不可信；token
   组：v2 件 6 词 / 产品侧 8 词 / 仓外 5 词）：⑤ →v2 件 61 · →产品侧 18 · →仓外 7，大头指向
   随行的 v2 件本身，搬走后仍指得着；⑥ →v2 件 0 · →产品侧 25 · →仓外 7，v1 史指回产品侧，
   travel 后悬空落在 records（`HD-16` 已接受的代价类）。
3. **① 的字节级身份**（14 件逐类读毕）：5 profile = 五种**工作形态原型**的策略文档（参数含义 /
   能力执法等级 hard·instrumented·audit-only / 不可弱化 policy floor / 恢复词汇），4 adapter
   **全部 `status: declared`、从未 implemented**（实现原定 A3 后，v2 在那之前被 v3 超越），
   issue registry 空（`{"registry_version":1,"issues":[]}`），整个区域自标 A2 UNSIGNED
   CANDIDATE、从未签署。`research-evidence` 是原型名（A1 场景 SC-03），字节层域中立——README
   边界条 GH2-R-22"nothing in this area names a consumer domain"，JSON summary 自述"no
   consumer-domain vocabulary"。「看着 ResearchSystem 专属」来自宿主目录与原型命名的观感，
   不是耦合：产品侧对它零读取，今天唯一活消费 = 39 测试当数据读。

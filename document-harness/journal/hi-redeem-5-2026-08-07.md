# 构造轮 `HI-REDEEM-5` — 五条 HarnessIssue 的源头兑付（2026-08-07）

> 范围 `da603da..HEAD`，六个提交（开轮记账 · 候选 · 执行期记账 · FULL 记录 · 一修 · VERIFY 记录 ·
> 本次 closeout）。无 supersession、无签字事件、不触碰 `E2` 冻结面与 `E10` 指令层。
> `E9` 预算三样全部用完：一 FULL（`REVIEWED_NO_BLOCKER`，0 blocker / 4 low / 4 observation）·
> 一次用户批准的修 · 一次 targeted VERIFY（`REVIEWED_NO_BLOCKER`，3 low / 4 observation）。
> **本轮五条 issue 文件字节未动**——HarnessIssue 不可变，兑付不修改也不关闭它。

## 1. 一条用户裁决，只在这里和 commit 正文里活着

**`E8` 的 "one dense paragraph" 买的是密度与无 trailer，不是字面一个段落**（用户 2026-08-07，
答 FULL 的 `L-4`）。起因：本轮候选正文写了七段（五条 issue 各一段），而最近三个构造轮
（`214f743` / `c667d08` / `a1fad7e`）都是字面一段，评审员指出「一条被一致先例刚刚停止遵守的规则，
要么重申要么开始执行，否则下一轮就是掷硬币」。裁决取前者，`L-4` 当场消解，`E8` 正文不改。

**归档位置说明（诚实边界）**：按惯例这类裁决进 `HARNESS-LEDGER.md`「只此一处」那节。落本 journal
是因为 closeout 当时 `HARNESS-LEDGER.md` 带着**另一个 session 的未提交改动**（见 §5），本轮不去动
同一个文件。裁决因此活在两处不可变载体：本 journal 与 `V3-HI-REDEEM-5-FIX-v1` 正文。

## 2. 五条兑付的落点

| issue | 路由 | 落在哪 |
|---|---|---|
| `repair-step-unnamed-in-readme` | `WORKFLOW_FIX` | 新增 `templates/run-v2/run_repair.py` + run-v2 README《Steps that did not change》新增一段 |
| `template-round-field-unwritten` | `WORKFLOW_FIX` | `run_evidence_v2.py` 的 `advance()` 加 `repair_round=REPAIR_ROUND` |
| `bind-round0-no-blocked-branch` | `WORKFLOW_FIX` | `run_bind_v2.py` 的 round-0 verdict 分支 |
| `evidence-not-byte-stable` | `VERIFIER_FIX` | `repo-audit.py` scope 行改相对 |
| `dispatch-types-product-run-as-construction` | `WORKFLOW_FIX`（base 半边原为 DEFER） | `dispatch.py` 的拒绝语 + 删掉 marker 的 `kind` 字段 |

## 3. 被拒绝的两条路线，和撤销 DEFER 的那次对话

第五条最初的修法是**教 `--range` 认出产品 run**：range 内所有改动路径落在同一个 `runs/<id>/` 下即
判为 `product-run`（四个真实 range 实测过：三个构造轮不触发，`f2c449a..fef3a2e` 触发）。先作标签、
被否后改作**拒绝守卫**，仍被否。用户的判词是「B 有点不相干」，而它确实不相干——真正的失败点是
执行者**绕过了一次正确的拒绝**：`--subject` 拒收 `fef3a2e` 是对的判断，读到拒绝之后换另一扇门才是
错。`E6` 原文：需要新机器的修法，是回头质疑被守之物的信号。回头一质疑，缺陷就露出来了——那句拒绝
只陈述事实、不给补救，于是「换一扇门」是读者最合理的下一步。

同一次对话里用户**撤销了 range base 的 DEFER**并要求根治。查完的结论是**没有剩余病灶可修**：
三个真实构造轮的 base 各自精确框住本轮；唯一错的 base 是本不该走 range 的产品 run，A+B′ 关掉它；
构造轮没有 control plane，base 无从派生，要派生就得新建「轮次起点」artifact——`E6` 拒绝的机器。
中途起草的第四条（dispatch 时打印「N 提交 / M 文件」当自查）**被自己的数据否掉**：三个正确构造轮里
有两个也只改一个文件，这个数字不区分好坏 base。

## 4. 两条陈旧数字，同一个毛病

- **`L-2`**：候选正文与计划都写 167 insertions，实为 **256**。167 是**未 stage** 的
  `git diff --stat` 小计，漏掉了新增的 `run_repair.py`（89 行），167+89=256。`E3` 说数字从产出它的
  命令里出——我在**最后一次 stage 之前**测的，之后没有重测。
- **`V-3`**：修腿给 `repo-audit.py` 写的注释说 pytest 缓存的代价是 four，实测为二。四来自更早那次
  两-checkout 观察，是**另一个时刻**的数字。

两条同类，一条在正文、一条在注释，都是「测了，但不是在该测的那一刻测的」。修腿里把 diffstat 改成
**stage 之后**再测（112+/3−），是把教训用上而不只是承认。`V-3` 入 bank（`cache-count`），因为它是
被评审的 work product 而预算已空。

## 5. 收轮时撞上的并发

VERIFY 在外期间，**另一个 session 在同一个 worktree 里改了四个文件**并新建了一份 plan：
`HARNESS-LEDGER.md` · `.goals/LEDGER.md` · `ResearchSystem/README.md` ·
`.goals/plans/research-system-agent-integration.plan.md`，新增
`.goals/plans/research-system-p5c-p8-revision.plan.md`。全部未提交。

冻结钩子在此期间**正确地**挡着它们——这是 `E9` 窗口按设计工作，不是故障。落 VERIFY 记录时只 stage
了记录本身一条路径，窗口随即关闭。本轮**不去动** `HARNESS-LEDGER.md`：它带着别人的未提交改动，
在同一文件上并行写是写竞争。后果是本轮的 ledger 断点更新与 §1 的裁决入账**留给用户**在那个 session
落定之后处理；两者都不会丢，裁决在上面，断点锚点是本 journal 与 closeout commit。

**已知的一处未清**：那个 session 已经把断点 (b) 改写成「五条已路由 HarnessIssue 的源头修复批，
权威执行状态与 resume pointer 只在 plan」——这句在本轮**完成之后**已经过时，需要换成收轮锚点。

## 6. 诚实边界

- `O-2` 只关了一半（VERIFY 的 `V-1`）：`next_action` 按 verdict 分支了，紧邻的 `print` 没有。
  VERIFY 没有「修得不完整」这个 verdict（评审员的 `O-4v`），所以这句限定只能活在散文里——就是这里。
- `L-1` 只关了缺陷的**一个实例**：`run_bind_v2.py` 的另一个 `--emit` 块（写 AssuranceCandidate 的
  那个）对测试套仍是死的，三处 mutation 全绿（`V-2`）。`E7` 要的是关掉整个类。
- 五条修里有三条没有回归守卫（FULL 的 `O-1`）：`#4` 与 `更正 E` 用可复现的实测数字替代，`B′` 是一句
  拒绝语。这个天花板在**集合层面**是否可接受，评审员明说是判断题、不是规则。
- 三条 rider 已入 bank，字节都已写明：`sg-print` · `bind-emit2` · `cache-count`。

## 7. 收轮后复核 — 「修一点露一点」是不是无底洞（跨轮设计判断）

用户读完收轮报告后指出：`O-2` 与 `L-1` 这种一轮修一处、下一轮再露一处的形状，结构上像枚举式补丁。
机械枚举的结果是**有界**，两条性质共五个落点：

| 性质 | 落点 | 状态 |
|---|---|---|
| **P1** round-0 的 verdict 决定行为 | `blocked` 闸门（`run_bind_v2.py:204`） | 本轮加，正确 |
| | `next_action` 的 SPEC_GAP 分支（`:215-219`） | 本轮修腿关 |
| | `print` 尾句 "owes a repair decision"（`:221-222`） | **开**（`V-1` → rider `sg-print`）|
| **P2** `--emit` 块的状态转换被测试守着 | blocked 块（`:223`） | 本轮 `L-1` 关 |
| | 写 AssuranceCandidate 的块（`:278`） | **开**（`V-2` → rider `bind-emit2`）|

模板里只有这五个：`run_evidence_v2.py` 无 `--emit` 闸，`run_repair.py` 一个取反式；各 run 目录下的
副本是各自轮次的评审对象，不计入本类。

**一次只漏一个的原因不是运气，是修了 finding 点名的实例而非缺陷类** —— `E7` 整条就为此存在，两个
评审员各引了它一次。

**P1 的根因不是"漏了一个分支"，是同一事实住在两处，且是两个改写而非字节拷贝**：`next_action` 写
"user REPAIR decision … naming the accepted finding ids and the repair boundary"，`print` 写
"owes a repair decision"。改写比拷贝更糟——拷贝分叉看得见，改写分叉静默。**正确修法是删**：让
`print` 直接打印 `next_action`，落点由三降为二，该分叉永久消失。给这条分支再加一个条件式是**错的
兑付**，它把落点留在三个、下一轮再漏一个。

**这个形状不是系统性的，是本轮亲手引入的。** 同族其余处都已做对：`run_evidence_v2.py` 的
`next_action` 由函数 `next_action_for(repair_round)` 单一来源产出、无 print 复述；`run_repair.py`
一处字面量、无复述；`run_bind_v2.py` 未阻断路径的两处（`:283`/`:292`）旁边的 print 只报状态名。
现成的正确写法就在隔壁文件里。

两条 rider 的兑付判据据此加注（`0b114b1`）：把"补实例"改成"关类"，并写明为什么补实例是错的兑付 ——
**将来兑付的人读的是 rider，不是这次对话**，判据不落进 rider 就等于没写。

**未查**：只枚举了 run-v2 模板这一族的"同一句话住两处"，没有扫全仓同形。要扩大范围得单开。

# Round `CONTRACT-V4` — candidate-revision measurements

2026-08-23. Candidate `23ca45b`, plan `document-harness/plans/contract-v4.plan.md`.

## Measurements at the candidate revision

- Windows (Python 3.13.6), working tree at commit time: `792 passed in 95.84s` (793 → 792:
  the R4 real-document class went from three tests to two when v4 removed the real
  flagged-then-exempted sample; the flagged/exempted behaviours keep their in-memory
  coverage).
- WSL Ubuntu (Python 3.12.3), fresh clone at `23ca45b`: `792 passed, 863 subtests passed in
  12.61s`; the eight-member layer resolves in the clone (`unresolved: []`).

## Two in-round self-catches, recorded so the FULL meets them disclosed

1. **The E2 blob literal chased the file it froze.** v4 was staged (blob `b1f651a2…`), the
   `E2` clause was written with that literal, and then the lineage paragraph was corrected
   (the supersessions were *signed* — 2026-07-24 and 2026-07-30, records in tree — not
   merely adjudicated; the dh-README rows this candidate replaced carried the signature
   records and the first draft had under-read them). The restaged blob is `a775e28f…` and
   the `E2` literal was updated before the commit. The lesson is the known one: a literal
   bound to bytes is written last, from the staged object, never from memory. If a fix leg
   touches v4, the literal moves with it, and the signature will bind whatever blob is
   final.
2. **A mechanical ten→eight sweep would have falsified history.** The sweep's first pass
   rewrote root `README.md:80`'s "the act that stops all ten from resolving" — a sentence
   about round `DE-PREFIX`'s day, when the layer had ten members. Restored as a historical
   "ten" with an explicit count-note pointing at this round. Scope discipline (`HD-41`)
   distinguishes present-tense member-count claims (swept to eight) from past-tense ones
   (left, with the round named when clarity needed it).

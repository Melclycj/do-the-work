# C3 round journal — run-v2 bind step, repair-round branch (M9)

Construction log for Phase C3 of `harness-deletion-first-stabilization` (Step 7; M8/M10
were delivered by C0, so this round is M9 alone). Evidence lines below are pasted tool
output from the executor session of 2026-07-30, kept here per `E3` (the commit body stays
one paragraph) and `E4` (every new guard mutation-tested).

## Opening rulings (chat, 2026-07-30 — recorded here and in the candidate commit body)

1. Preview card rendered (`E11`), user replied **OK** — shape as carded.
2. cold read: **discharged by citation, no dispatch** — the ledger's recorded rule for this
   opening ("八成员 blob 自 `v3-cold-read-ae4df09.md` 起未变则全可引用覆盖") applies because
   all eight member blobs are unchanged since that read; derivation pasted below.
3. M9 shape: **as the defect table wrote it** — "按 repair_round 读 full/verify，`review_refs`
   绑全". The template's bind step absorbs the AssuranceCandidate assembly, because the
   state carries only the singular `review_ref`, so `review_refs` has no other home than
   `bind_candidate`; both worked precedents put assembly in the bind step (round 0:
   `runs/w1-r1/run_bind.py`; round 1: `runs/p3-corr/run_bind_candidate.py`), and the
   round-driven assembly is exactly what supersession-1 changed, which is the template
   README's own admission criterion. `unresolved_finding_ids` is derived from the in-hand
   reviews via the existing `review.blocking_findings` (N2-A9's check enforces the exact
   set bidirectionally, so a hand-filled list is wrong whenever it drifts). C1.5/C2 did not
   stale this table row (unlike M6/M7 at the C2 opening), so no shape re-ruling was needed;
   the card disclosed the narrower option and the user approved the carded shape.
4. Riders: **F-a due at closeout** (this round's closeout touches `HARNESS-LEDGER.md`; the
   ledger text F-a names was already removed by the 2026-07-29 restructure — verified by
   `grep issues\.py ResearchSystem/HARNESS-LEDGER.md` → no matches — so redemption is the
   row deletion plus this note). **F-4 / F-5 / C0-V1 not due**: F-4 is ruled "no template
   guard until the first real run"; F-5's owner (`assurance_state.py`) is untouched — this
   round adds one more `pointer_for` call site of the same duplicated-identity shape,
   disclosed for the FULL; C0-V1's file (`test_run_v2_template_fulfillment.py`) is not
   touched — M9's tests live in a new file.

## Cold-read coverage derivation (`E10` citation clause, per member)

`git rev-parse ae4df09:<path>` vs `HEAD:<path>` (HEAD = `6f2fde9`) for the eight members
`v3-cold-read-ae4df09.md` §1 tabulates:

```
SAME  ResearchSystem/document-harness/CONSTRUCTION-CHECKLIST.md  33126c19 -> 33126c19
SAME  ResearchSystem/document-harness/README.md  4daab565 -> 4daab565
SAME  ResearchSystem/document-harness/EXECUTION.md  bd490c8b -> bd490c8b
SAME  ResearchSystem/document-harness/REVIEW.md  70bc521e -> 70bc521e
SAME  ResearchSystem/migration/document-work-assurance-v3/v3-harness-operating-contract.md  0ae222fd -> 0ae222fd
SAME  ResearchSystem/migration/document-work-assurance-v3/v3-harness-review-contract.md  7dcdb817 -> 7dcdb817
SAME  ResearchSystem/contract/Document-Work-Assurance-Contract-v3-supersession-1.md  68031fa2 -> 68031fa2
SAME  ResearchSystem/contract/Document-Work-Assurance-Contract-v3-supersession-2.md  e1a2f26b -> e1a2f26b
```

Member-set staleness: `git diff --name-only ae4df09 HEAD` lists no path under
`ResearchSystem/schema/` or `ResearchSystem/contract/` and no new prose successor, so the
set is still exactly eight. Every member is therefore covered by citing
`v3-cold-read-ae4df09.md` (itself an end-to-end read: member 2 in full, the other seven by
verified citation to the `d58969d` / `403fc9a` reads).

## RED — the new assertions fail against the pre-fix template

`python ResearchSystem/tooling/tests/document_harness_review/test_run_v2_template_bind.py`
at the pre-fix template: `Ran 13 tests` → `FAILED (failures=6, errors=5)`. The eleven red
are exactly this round's new assertions; the two green are the two negative controls
(`test_a_complete_review_set_does_not_trip_the_refusal`,
`test_round_zero_never_asks_for_a_repair_decision`), whose pre-fix green is what makes
them controls. Every failure is value-level via the `run_main` capture (None != 1 /
structure mismatch), no reachability-only ERROR among the FAILs:

```
ERROR: test_a_blocker_from_any_round_is_listed_in_the_unresolved_set   (unresolved_ids missing)
ERROR: test_each_round_that_happened_gets_its_own_bytes_bound          (review_refs_of missing)
ERROR: test_rounds_with_no_blockers_contribute_nothing                 (unresolved_ids missing)
ERROR: test_a_repaired_round_binds_the_full_then_the_verify            (round_documents missing)
ERROR: test_round_zero_binds_the_full_alone                            (round_documents missing)
FAIL:  test_a_repaired_runs_candidate_passes_the_real_faithfulness_gate
FAIL:  test_a_repaired_round_with_no_verify_on_disk_is_refused         (None != 1)
FAIL:  test_round_zero_with_no_full_on_disk_is_refused                 (None != 1)
FAIL:  test_after_a_repair_the_verify_is_what_the_checker_receives     (None != 1)
FAIL:  test_at_round_zero_the_full_is_what_the_checker_receives        (None != 1)
FAIL:  test_the_verify_outcome_gate_receives_the_verify_and_the_users_decision (None != 1)
```

## GREEN — after the fix, on the exact bytes being committed

```
tests => tests: 29   passed: 29   failed: 0  RESULT: OK (compiler + shadow lint behave to contract)
tests/stage_control => stage-control deterministic matrix: 20 run, 0 failure(s), 0 error(s)
tests/harness => Ran 39 tests in 3.422s OK
tests/document_harness => Ran 169 tests in 35.069s OK
tests/document_harness_review => Ran 351 tests in 76.564s OK   (338 before this round; +13, all authored here)
repo-audit exit: 0
```

Frozen surface at the working tree: `git ls-tree HEAD .goals/plans/` carries
`8ad404b1…` at `document-work-assurance-harness-v3.plan.md`; `git rev-parse` gives
`b2dbdf75…` (contract) and `68031fa2…` (supersession-1); `git status --porcelain` over
`ResearchSystem/schema/document-assurance-v3` + `ResearchSystem/contract` is empty;
`git diff --stat HEAD` over the two user-locked oracles is empty.

## Mutation probes (`E4`) — neuter → value-level red → restore from sha256-checked copies

Reference snapshot (post-fix bytes), taken before any probe; the template was restored
from it after every probe and re-hashed each time:

```
61D591F0E00D1F33603ECE994B71F87173E81862EE35C22424FA20AC7F6E5BB6 *run_bind_v2.py (= .orig)
```

| probe | neutered thing (defect shape) | result |
|---|---|---|
| P1 | `round_documents` returns the FULL always — the reported M9 shape | **5 value FAILs** (branch test, refusal test, both recorder tests, real gate — the live `check_verify_outcome` rejects the FULL fed to a VERIFY slot with NOT-VERIFY) |
| P2 | `review_refs_of` binds only the last ref — "只绑一个 ref" | **2 value FAILs** (exact-list unit + real faithfulness gate: binding-incomplete) |
| P3 | `unresolved_ids` reads only the last review | **2 value FAILs** (unit + real gate: BLOCKER-DROPPED f1) |
| P4 | refusal block deleted | **2 value FAILs** (both refusal tests, None != 1); the complete-set negative control stays green |
| P5 | round≥1 verify-outcome gate deleted | **1 value FAIL** (its dedicated reachability-by-value test) |

5/5 probes bind, zero ERRORs, every failure on a value. After the final restore the file
hash equals the snapshot and the 13 tests are green again (`Ran 13 tests ... OK`).

## Disclosures for the FULL (read before crediting anything above)

1. **The `--emit` chain is unexercised by tests** (state advances REVIEWED →
   AWAITING_FINAL, the two `pointer_for` calls, `write_canonical`). Same coverage class
   as banked rider F-4 ("no template guard until the first real run"); the read-side
   gates remain the backstop for the protected `review_ref`.
2. **`check_review_result_v2` is a recorder stub in every main-path test** — its real
   behaviour needs a git repository behind the evidence commit and has its own suite
   (`test_review_v2_subject.py`, `test_review_cli_v2_subject.py`); what these tests pin
   is WHICH document the template hands it. The verify-outcome gate, the assembly and
   `check_assurance_candidate` are the real functions.
3. **One more F-5-shaped call site**: the emit chain adds
   `pointer_for("assurance_candidate_ref", …)` with the field name duplicated as the
   `advance()` keyword — the API-shape observation stays banked with its owner file.
4. **The template now absorbs the AssuranceCandidate assembly** the old docstring
   deferred to w1-r1 by reference; the six spec/evidence digestRefs are computed at bind
   time over the bytes in hand (`digest_ref_of`), because post-supersession-2 states
   carry path-only pointers on those fields while the frozen candidate schema requires
   `digest_sha256` — the same rule `check_result_refs` always used. Nothing cross-checks
   those six by design (C2 M6 = option B).

## Closeout (2026-07-30)

FULL record `v3-review-full-71d43be.md` committed `0576322`: **REVIEWED_NO_BLOCKER** —
zero blockers, repair and VERIFY budget untouched. Dispositions:

- **F-1** (bank-shaped) → HARNESS-RIDERS row `F-1c3`, redeem when a batch next touches
  `run_bind_v2.py` or the digest policy. The correction it names is recorded here rather
  than by editing the reviewed text above: disclosure 4's premise "post-supersession-2
  states carry path-only pointers on those fields" holds for **five of the six** refs —
  `work_spec_ref` is in `DIGEST_PROTECTED_FIELDS`, its pointer carries a binding bytes
  digest, and `digest_ref_of` currently discards that digest and recomputes over disk.
- **O-1** (round-1 `DISCLOSURES` empty default gives a round-1 author no cue that a
  repaired blocker listed under N2-A9 deserves an explanatory disclosure) — no action
  this round; same wait-for-the-first-real-run class as banked F-4.
- **O-2** (the faithfulness test fixture's path-only `work_spec_ref` papers over F-1's
  field) — rides with `F-1c3`.
- **F-a** redeemed in the closeout commit: its named ledger text was already removed by
  the 2026-07-29 restructure (the FULL re-verified the grep), so redemption is the row
  deletion plus this note.

Round chain: card OK → cold read by citation (zero budget) → candidate `71d43be` →
FULL `0576322` → closeout. Next: plan Step 8, Phase C4 (M11).

## Fix round (2026-07-30 — F-1's banking overturned by the user)

The user un-confirmed F-1's banking and activated this round's fix leg: `E9`'s test —
has a valid independent FULL occurred — is mechanical and does not expire at closeout,
so this is C3's one user-approved fix and it obliges the targeted VERIFY. Approved
scope: F-1 (the reviewer's verify-against option), O-2 (fixture), and landing the user's
three bank-tightening rules in the `HARNESS-RIDERS.md` header (that file is not an E10
member — the `ae4df09` read enumerated the layer at eight — so no read is owed).
`F-1c3` is deleted in this same commit: redeemed by this fix rather than by touch.

- **Fix shape**: `digest_ref_of` copies an authored `digest_sha256` when the pointer
  carries one; refuses with `AssuranceFault` when the bytes in hand contradict it (both
  sides are in hand — the M7 reconcile-at-generation shape); computes from disk only for
  digestless pointers. General over all six refs; in post-narrowing practice only
  `work_spec_ref` carries an authored digest.
- **RED**: `test_a_pointer_digest_contradicted_by_disk_refuses_the_assembly` fails
  against the pre-fix shape — `Ran 16 tests … FAILED (failures=1)`; the matching-digest
  and digestless tests are the negative controls, green before and after.
- **GREEN** on the exact bytes committed: bind tests 16/16 OK; suites
  29 / 20 / 39 / 169 / **354**; repo-audit exit 0; schema + contract porcelain empty;
  both user-locked oracles byte-identical to HEAD.
- **P6 probe (`E4`)**: guard neutered back to unconditional recompute (the F-1 defect
  shape) → the refusal test fails at value (assertRaises unmet), controls stay green;
  restored from the sha256-checked scratchpad copy, post-restore hash
  `1FAAE6F4B50DE51D35BFC21E903B9A02AA055A0498348DE23CD9004F16996FB6` equal to the
  snapshot, 16/16 green again.

The activation is itself the first application of the new closeout-balance rule (header
③), applied retroactively by the user's own challenge. Owed next: one targeted VERIFY
covering the accepted finding, the whole repair diff and the permanent boundaries.

**VERIFY returned** (`v3-review-verify-7e9c19b.md`, committed `f0e5d64`):
`REVIEWED_NO_BLOCKER`, 0 blockers, 0 findings; O-1v noted not banked (absent-file path
raises bare FileNotFoundError — pre-existing, loud), O-2v wording-level (ledger line
anticipates the dispatch by 13 s), O-3v routed to the ledger's open-question slot
(actor-binding conventions accumulating outside the `E10` layer — the user's call).
**C3's budget fully spent: one FULL, one user-approved fix, one targeted VERIFY. Round
closed for good; next is plan Step 8, Phase C4 (M11).**

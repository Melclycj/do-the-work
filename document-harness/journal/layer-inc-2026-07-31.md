# Layer-incorporation round journal — bank governance prose into the layer + the 11-rider layer batch

Construction log for the layer-incorporation round (durable plan
`.goals/plans/harness-layer-incorporation-round.plan.md`; ruled 2026-07-31, ordered before
Phase C4). Evidence lines are pasted tool output from the executor session of 2026-07-31,
kept here per `E3`. This is a true amendment round: it adds clauses to rules
(`E10` "an amendment adding a clause to any rule is design and opens a round"), so it runs
the full mechanics — E11 card → candidate → FULL → (fix+VERIFY if activated) → amendment
read of the final layer bytes (`rsc v3 dispatch --read`).

## Opening prechecks (2026-07-31, this session)

- Plan base `bf73536`; HEAD at opening `c61d82d`. Drift = 3 bookkeeping commits
  (`d6a477c` plan write, `ef46c31` ledger NEXT pointer, `c61d82d` preclear reconcile),
  touching only `.goals/**` and `ResearchSystem/HARNESS-LEDGER.md` — no layer file, no
  scope collision. They join the FULL's view via dispatch base `0224176` (C3
  verify-closeout), alongside the three bookkeeping commits the plan already named
  (`0a3d18d`, `4ad1184`, `bf73536`).
- `HARNESS-RIDERS.md`: 19 rows counted; header carries the three prose blocks the ruling
  incorporates (redemption convention + routing; 2026-07-30 three tightenings; 2026-07-30
  backfill note).
- `layer_path_check.py` read: scans staged **added** lines of LAYER files for backtick
  path tokens; `HARNESS-RIDERS.md` is not in its `LAYER` tuple and stays out (the
  incorporation moves rule text, not the file).
- Worktree clean at opening (`git status --porcelain` empty).

## Cold-read coverage derivation (`E10` citation clause, per member)

`git rev-parse ae4df09:<path>` vs `HEAD:<path>` (HEAD = `c61d82d`) for the eight members
`v3-cold-read-ae4df09.md` §1 tabulates:

```
SAME  ResearchSystem/document-harness/CONSTRUCTION-CHECKLIST.md  33126c19 -> 33126c19
SAME  ResearchSystem/document-harness/README.md  4daab565 -> 4daab565
SAME  ResearchSystem/document-harness/EXECUTION.md  bd490c8b -> bd490c8b
SAME  ResearchSystem/document-harness/REVIEW.md  70bc521e -> 70bc521e
SAME  ResearchSystem/migration/document-work-assurance-v3/v3-harness-operating-contract.md  0ae222fd -> 0ae222fd
SAME  ResearchSystem/migration/document-work-assurance-v3/v3-harness-review-contract.md  7dcdb817 -> 7dcdb817
SAME  ResearchSystem/contract/Document-Work-Assurance-Contract-v3-supersession-1.md  68031fa2 -> 68031fa2
SAME  ResearchSystem/contract/Document-Work-Assurance-Contract-v3-supersession-2.md  e1a2f26b -> e1a2f26b
```

Member-set staleness: `git diff --name-only ae4df09 HEAD -- ResearchSystem/schema/
ResearchSystem/contract/` returns 0 paths — no schema `description` amended, no new
contract text, so the set is still exactly eight. Every member is therefore covered by
citing `v3-cold-read-ae4df09.md` (itself an end-to-end read: member 2 in full, the other
seven by verified citation to the `d58969d` / `403fc9a` reads). Same shape as the C3
opening. Zero budget; no dispatch.

## Per-rider source return (plan Step 2) — finding text re-read, minimum fix transcribed

| rider | source re-read | minimum fix as the source states it |
|---|---|---|
| BC-1 | ledger ruled-line 2026-07-30 + `a6b87ad` commit body | Clause text of the (a) ruling: a low whose record supplies exact appliable bytes / named content is applied immediately, layer included, reported after the fact, reversible; no-bytes lows still bank; a layer application still owes the independent read, riding the next layer read at per-member digest cost. Land it in `E10`'s free channel. |
| E10-d | rider row born `717e547` (2026-07-29 reform); source "checklist read chain" — no single finding carries a verbatim minimum fix. **SPEC-gap noted**: fix designed this round (extend the deferral/design test to replacements and deletions), disclosed on the card. | — |
| F-1r | `v3-review-full-8ec4c60.md` F-1 (:223-241) | Verbatim: "move the new clause after '…once one has, changing it opens a round;' and say which of the two wins when both apply." |
| L-2r | `v3-checkpoint-read-d58969d.md` L-2 (:145-152) | Make the record property a rule: a read record states the blob ids it read end-to-end — citation depends on it (fail-safe today; usability degrades silently). |
| E2-t | `v3-checkpoint-read-dcced4e.md` L-1 (:198-212) + `v3-cold-read-451e8b0.md` O-3 | Pin the as-of moment of *existing*. The authoring commit `11d147e` (2026-07-29) states the authoring-time reading ("写「现有」故新增 schema 不受阻"); pack held 14 files then and now (`git ls-tree` counts; zero pack diff since). Pinning also makes `E10`'s schema-description clause reachable (O-3). |
| E2-s2 | signature batch 2026-07-30 + `v3-checkpoint-read-403fc9a.md` L-1 / §4 | **User ruling required**: does signed supersession-2 blob `e1a2f26b…` join the `E2` frozen list (symmetric with supersession-1)? If yes: the checklist's E2 list line gains a fourth blob; the carrier's bytes are never touched (§13). If no: rider closes on the recorded ruling. |
| O-4r | `v3-cold-read-451e8b0.md` O-4 (:204-209) | Qualify `E12`'s "never a written SHA" to ranges *recorded in a file* — "Read as governing a range recorded in a file, rule and tool agree; the sentence does not say so." Say so. |
| O-6 | `v3-review-full-af2905c.md` O-6 (:401-403); instances: af2905c O-6, 403fc9a O-2, ae4df09 O-2, C3's two record commits | **User ruling required (vocabulary)**: extend `E8`'s kind list. Proposed: + amendment / ruling / record (covers all four attested shapes). |
| V-c | `v3-review-verify-f054a08.md` V-c (:244-249) | Restore the contrastive punctuation the qualifier consumed (same seam as F-1r; fixed by the same re-arrangement). |
| O-1 | `v3-review-full-af2905c.md` O-1 (:364-369) | **User ruling required (split or not)**: R5 shape observation — `E10` at 7+ clauses, growth finding-driven; this round adds text to it, so the moment to take a position. Either answer closes the rider. |
| VB-1 | 2026-07-29 repair-batch ruling; deleted ledger line recovered from `git show 8ec4c60`: "review 判据 = repo 现实为判据、requirement 只当问题清单（`R3` 顺序当真，产品侧随 reform 轮明确）"; `v3-review-full-8ec4c60.md`:123 confirms "correctly banked as VB-1" | Land the product-side wording in `REVIEW.md` explicitly (repo reality is the verdict basis; the obligation list is the question list). |

## Drafted revision texts (plan Step 4) — one per edit site, awaiting card approval

### 1. `E10` mid-section re-arrangement (BC-1 + F-1r + V-c + E10-d, one seam)

Current (checklist :74-85, the free-channel-to-design stretch):

> a read's must-fix findings are answered by an amendment commit plus an independent
> re-read of the amended text, and that pair is not a round and spends no budget — it
> admits only deletions and the literal replacement the finding names; an amendment adding
> a clause to any rule is design and opens a round — for as long as no round has relied on
> the text — relied means an outcome would change if the text changed, which authoring,
> citing or recording it alone is not — once one has, changing it opens a round;

Replacement draft (each move traceable: F-1r moves the design clause after the qualifier
and adds the who-wins sentence; V-c restores the contrast by parenthesizing the relied
definition; BC-1 inserts the byte-supplied-low channel; E10-d extends the design test to
replacements/deletions):

> a read's must-fix findings are answered by an amendment commit plus an independent
> re-read of the amended text, and that pair is not a round and spends no budget — it
> admits only deletions and the literal replacement the finding names; a low finding whose
> record supplies the exact bytes or names the content takes the same free channel —
> applied immediately, instruction layer included, reported after the fact and reversible;
> a layer application still owes its independent read, riding the next read of this layer
> at per-member digest cost; a finding without appliable bytes banks; the free channel
> holds only for as long as no round has relied on the text (relied means an outcome would
> change if the text changed, which authoring, citing or recording it alone is not) — once
> one has, changing it opens a round; an amendment adding a clause to any rule, or
> replacing or deleting text so that what a rule requires changes, is design and opens a
> round; when the free channel and the design test both apply — the named literal
> replacement itself adds a clause or a bound — design wins and the round opens;

### 2. `E10` citation clause (L-2r, additive)

Current: "and a member whose blob is unchanged since a recorded end-to-end read of it is
covered by citing that record;"

Draft: "and a member whose blob is unchanged since a recorded end-to-end read of it is
covered by citing that record — a read's record states the blob id of each member it read,
because citation depends on it;"

### 3. `E2` *existing* pin (E2-t)

Current: "and every existing file in the `ResearchSystem/schema/document-assurance-v3/`
pack."

Draft: "and every file the `ResearchSystem/schema/document-assurance-v3/` pack held when
this item was written (2026-07-29, fourteen files); a pack file added later is not frozen
by this rule."

(Membership decidable by `git ls-tree` at the 2026-07-29 amendment; count re-derived this
session: pack holds 14 at HEAD and pack diff since `ae4df09` is empty.)

### 4. `E2` list line (E2-s2 — only if the user rules "in")

Current: "…the list is exactly this: signed plan blob `8ad404b1…`, contract `b2dbdf75…`,
supersession-1 `68031fa2…`, and every existing file…" / "Three blobs and one directory"
(blob re-derived this session: `git rev-parse HEAD:.goals/plans/document-work-assurance-harness-v3.plan.md`
→ `8ad404b12b3242e700d0ad215048dffccada7d9c`)

Draft if IN: add "supersession-2 `e1a2f26b…`" after supersession-1; "Three blobs" →
"Four blobs". Carrier bytes untouched either way (contract §13).

### 5. `E12` recorded-range qualifier (O-4r)

Current: "A range's base is written and its tip is `HEAD`, never a written SHA: recording
the range is itself a commit inside the round, so…"

Draft: "A range recorded in a file has its base written and its tip `HEAD`, never a
written SHA — the CLI printing a resolved full SHA is display, not a recorded range:
recording the range is itself a commit inside the round, so…"

### 6. `E8` kind vocabulary (O-6 — user rules the list)

Current: "Name the commit's kind — candidate / pre-submission correction / review fix /
closeout / errata — so the review side can attribute it without asking."

Proposed: "candidate / pre-submission correction / review fix / closeout / errata /
amendment / ruling / record" (three additions cover the four attested instances:
amendment batches `8ec4c60` `403fc9a` `a6b87ad`, ruling record `6618b84`, C3's two
review-record commits).

### 7. New rule R10 — the rider bank (incorporated from the riders header)

Placement: after R9's block (physically after R3/R9, before R4), numbered past the
current maximum. Text migrated from the riders header — deletions and seam-stitching
only, no re-type of kept content; the one substitution: the header's dead reference
"`C-3` takes must-fix" (a retired-contract number absent from this checklist) becomes
the live "`E10`'s must-fix channel", disclosed here.

Draft:

> - **R10** The rider bank (`ResearchSystem/HARNESS-RIDERS.md`) is the construction
>   side's internal debt ledger; product-run observations, schema governance and
>   post-CLOSED admission belong to HarnessIssue, never here. Routing for a read's
>   findings: `E10`'s must-fix channel takes must-fix, R9 takes wording-level, the bank
>   takes the middle. One row per rider: what · redeem-when · source; no narrative — the
>   source records hold it. Redemption = the fix rides a batch already touching that
>   surface, and the row is deleted in the same commit. A row names its target file(s) or
>   clause — "对应文件" alone is not a target. redeem-when is a touch condition or a
>   deadline, whichever arrives first; a finding whose value expires MUST carry that
>   moment as its deadline. A FULL returning REVIEWED_NO_BLOCKER with lows does not bank
>   them by default: before closeout the executor weighs each low's deadline against its
>   touch trigger and puts the spend-the-fix-leg / bank choice to the user (E9's test —
>   has a valid independent FULL occurred — does not expire at closeout, so a late
>   activation is still that round's one user-approved fix and still obliges the VERIFY).

### 8. `REVIEW.md` verdict-basis section (VB-1)

Insert after "The one sentence" section:

> ## The basis of judgment is the repository, not the requirement list
>
> You judge the work against the repository reality at the pinned revisions — what the
> code, documents and evidence actually do. The obligation list is your question list,
> never the verdict basis: it tells you where to look, and its omissions are findings
> about the map, but a candidate is never right because the list was satisfied nor wrong
> because the list was silent. Lead with the implementation; conformance of process and
> record is a boundary check, run second.

### 9. Riders header contraction

The header's three prose blocks reduce to a pointer (rules now in R10) plus a one-line
pointer to the backfill record (`0a3d18d` commit body). The 11 layer-family rows (BC-1 /
E10-d / F-1r / L-2r / E2-t / E2-s2 / O-4r / O-6 / V-c / O-1 / VB-1) delete in the same
commit their fixes land. 8 rows remain (F-4 / F-c / F-d / O-2b / SCC / CT / RA / F-3r).

## Test surface

Pure-text round: no new guard, no test to go RED — recorded as fact, not manufactured
(`E4` applies to new guards; none is added). Every commit runs the five suites
(29 / 20 / 39 / 169 / 354 at C3 close) + `repo-audit.py` exit 0 + the `E2` frozen-surface
re-check (four blobs after this round's E2-s2 ruling + schema pack porcelain + two
user-locked oracles).

## GREEN — on the exact bytes being committed (2026-07-31)

```
tests => tests: 29   passed: 29   failed: 0  RESULT: OK (compiler + shadow lint behave to contract)
tests/stage_control => stage-control deterministic matrix: 20 run, 0 failure(s), 0 error(s)
tests/harness => Ran 39 tests in 3.410s OK
tests/document_harness => Ran 169 tests in 25.689s OK
tests/document_harness_review => Ran 354 tests in 55.744s OK
repo-audit exit: 0
```

Frozen surface at the working tree (after the E2 amendment, so four blobs):

```
signed plan blob: 8ad404b1
contract: b2dbdf75
supersession-1: 68031fa2
supersession-2: e1a2f26b
schema+contract porcelain: []
oracle diff: []
```

## User rulings received (2026-07-31, card round)

The E11 card was rendered; the user asked what splitting `E10` would involve (answered:
the 8 content classes it carries, the split shape, the re-pointing work), then replied
**"同意不拆，ok开工"**.

1. **O-1 — explicitly ruled: NO SPLIT this round.** Rider closes on this ruling; a split
   remains available as its own amendment round after C4.
2. **E2-s2 — IN, per card recommendation under the user's card OK** (supersession-2 blob
   `e1a2f26b…` joins the `E2` list; carrier bytes untouched).
3. **O-6 — vocabulary "+ amendment / ruling / record", per card recommendation under the
   user's card OK.**
4. R10 placement (after R9's block) and the `C-3`→`E10` live-reference substitution stood
   disclosed on the card; no objection raised.

Recording note: ruling 1 was explicit in the user's words; rulings 2–3 are the card's
recommendations adopted via the card OK, not separately voiced — recorded as such.

## Execution deltas from the §Drafted texts above (wording only, applied 2026-07-31)

- Draft 1: "the free channel holds **only** for as long as" → "holds for as long as"
  (drops an emphasis word the original qualifier never had; the contrastive dash before
  "once one has" is kept, which is the V-c fix).
- Draft 3: "when this item was written" → "when the pack entry joined this list" (the
  fourth blob now precedes the pack item, so "this item" could misread as supersession-2).
- Draft 7 (R10): the header's rule-① transition tail ("rows predating this get their
  targets named at their next due-check") is dropped in migration — the backfill it
  scheduled was executed 2026-07-30 (`0a3d18d`, all seven rows named), so the tail
  governs an empty set. Deletion, disclosed here.

## Closeout (2026-07-31)

Round chain, complete: card OK + three rulings → cold read by citation (zero budget) →
candidate `feacb86` → FULL record `v3-review-full-feacb86.md` committed `6132828`,
**REVIEWED_NO_BLOCKER** (0 blockers, 2 lows, 3 observations) → L-1's supplied bytes
applied `7463229` through the byte channel (first use on layer text; fix leg NOT
consumed) → L-2 banked by user ruling as `L-2li`, committed `784e49b` → layer read
dispatched on the branch tip, record `v3-checkpoint-read-784e49b.md` committed `f196fb7`
(**0 must-fix**, 1 low banked as `L-1lr` — E9/R10 letters lag the byte channel — 4
observations; the `7463229` ride-along debt discharged in the same read) → this closeout.
Budget: FULL spent, fix + VERIFY legs untouched. Bank: the 11 layer-family rows redeemed
and deleted at `feacb86`; 8 legacy rows + 2 new (`L-2li`, `L-1lr`) = 10 rows standing.
Open questions left with the user (in records, not blocking): whether `E8` governs
bookkeeping-only commits (FULL O-1 / read O-4), and the E9/R10 letter-lag seam now banked
as `L-1lr`. Ledger pointer advanced: NEXT = Phase C4 (M11).

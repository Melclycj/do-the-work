# HARNESS RIDERS — banked findings, redeemed on touch

Pure data table. The governing rules — the bank/HarnessIssue split, routing, row format,
redemption, and the 2026-07-30 tightenings — live in
`document-harness/CONSTRUCTION-CHECKLIST.md` R10 (incorporated 2026-07-31; this header
carried them until then). The rule-① backfill of 2026-07-30 is recorded in commit
`0a3d18d`'s body.

| id | what | redeem when | source |
|---|---|---|---|
| wl-route | **供了字节的 wording-level finding 有两条路，三句规则二比一**：`E10` 自由通道枚举写的是「a finding below must-fix — **low or observation alike** — whose record supplies the exact bytes」（**没点名 wording-level**）· `R9` 抬头句说「A read's **wording-level** findings are **banked**, never rounds」，只把「no-decision-nameable」那一类写成搭下一批 · `R10` 路由句说自由通道收**any finding** whose record supplies the exact bytes 且「档位不改变路由」。两句说当场套、一句说入 bank。批 B R5 的 `W-1`（有名下游决定 + 有字节）**走了当场套**。改变的决定：一条 wording-level 的修**立刻落地并在层里待到下次 read**，还是等一个批——两读文本依据等强，两个 executor 必分歧（同形先例：`chk-thin`——已于 2026-08-22 轮 `PRERUN-RIDERS` 兑付删行，以及 `E1-suff`——已于 2026-08-18 轮 `ORCHESTRATOR-CHARTER` 兑付删行）。**读者刻意不供字节**：任何 tiebreak 都是给 `E10` 枚举或 `R9` 加 bound = design | 修法为 design 形状，故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `E10` 自由通道枚举句 / `R9` 抬头句 / `R10` 路由句的设计批；**deadline = 下一份对 wording-level finding 供字节的 read 记录**（分歧第一次真能咬人的时刻，按 `HD-37` ① 不在创造本行的 R5 之内） | v3-checkpoint-read-f61ce2c.md `L-2` |
| wspec-owner | WorkSpec author 重指到 executor 后，**两处 `E2` 冻结的 schema title 仍写着被取代的读法**：`document-work-spec.schema.json` 与 `document-work-spec.v2.schema.json` 的「sole owner: stage author / planning agent」（冻结 pack 内，字节欠 `E2` 裁决方可动）。**契约那一处已由轮 `CONTRACT-V4` 兑付**——v4 §3 表行改写为 the run's executor (its WorkSpec author)，宽容读法（「stage author」是角色标签、承载者由 `HD-35` 改派）自此有正文承载，评审员拿 run 对表误读违规的风险已消；剩余风险=只读 schema title 的读者 | `E2` 对该两 schema 的 recorded ruling，或下一批碰这两个 schema 文件，孰先；**原 deadline（第一个按新模型起草 WorkSpec 的 run）解除**——v4 正文已替表说话 | v3-checkpoint-read-be9878a.md L-3 · 轮 `CONTRACT-V4` 半兑（契约站点） |
| F-c | digest-narrowing plan:247-248 边界编号错（"边界 3 降级"实为 plan 表 #2，对两份枚举皆无解；:219 警示已加、247/248 未改——照抄会降错边界） | 碰 harness-digest-narrowing.plan.md | v3-review-full-f2507a5.md F-c |
| RA | `run_all` 现有**一个产品调用者**——run-v2 evidence 模板（`HD-25`，批 B R1 `e9166d2` 接线，`run_evidence_v2.py:200`）；**另有两处单元测试调用** `tooling/tests/document_harness/test_candidate_checks.py:874`/`:887`（自 `11ce5b4`（2026-07-28）起，即本行出处那个 FULL 的同一个 commit——一条钉住「gap 之后的 check 不跑」、另一条是其负对照，**它们是今天唯一钉住 `run_all` stop 契约的东西**）。故原文"全仓零调用者"指的一直是产品侧、从来不是字面全仓（VERIFY `dbbec28` `V-2`）。**剩下的是 CLI 那半边**：产品 run 机检引擎仍无 CLI 接线，且模板是**抄**进 run 的，八个已关闭 run 不继承（rider `delta-prose`）。（原文"全仓零调用者"自 `e9166d2` 起为假，FULL `e9166d2` `L-2` 指出；用户 2026-07-30 归口） | **2026-08-17 用户裁再次重定范围、非兑付**——本行至此只剩 **CLI 入口**一件事。产品调用者那半**已由 `HD-25` 关闭**（批 B R1 `e9166d2` 把引擎接进 run-v2 evidence 模板，条目已 implemented；R4 实测今日一个产品调用者 `run_evidence_v2.py:200` + 三处测试引用），剩下的是「跑这一轮的机检」仍只住在**抄**进 run 目录的模板脚本里、`dtw` 八命令无一是它（2026-08-19 前写「六命令」、2026-08-21 前写「七命令」，`init`/`preview` 加入后计数两度作废而本行实质不变——两者都不是机检入口；`HD-47`/`HD-51`）。`split-design` §1 已裁六命令原样，故加不加第七个命令按 `R5` 归用户；用户判定这是**便利性而非正确性**——接线已让产品 run 真的在用引擎，`HD-25` 接受的 delta 代价（模板是抄的、已关闭 run 不继承）加不加 CLI 都一样存在——**不为此推翻 §1**（**该判断限于本行这个案子**：2026-08-19 用户就 onboarding 另裁了一个第七命令`dtw init`，`HD-47`——§1 的「六命令原样」自此读作搬迁指令而非命令数上限，本行的答案不因此改变）。redeem-when = **第一次需要在 run 目录之外跑机检**的场合，或下一个碰 `run_all` 签名的批（FULL `e9166d2` `O-1`：docstring 承诺保留 `SPEC_GAP` 证据，但 `raise` 时 `results` 是局部变量，调用者拿不到——动签名时 `test_candidate_checks.py:874/887` 就是影响面），孰先 | v3-review-full-11ce5b4.md O-4 · v3-review-full-e9166d2.md L-2 |
| PD | `document_harness/__init__.py:238` `pack_digests()` 哈希整个 v3 pack（含成员 9）却全仓零调用者（两处活调用是 v2 `schemas.pack_digests()`，另一 pack）；`E2` 现零机械 enforcement，此为唯一现成挂点。与 `RA` 同形状 | **2026-08-17 用户裁重定范围、非兑付**——`HD-27` 写明的重开条件（拆分批）已到达，R4 逐条复核后认定其三条理由**并未同时变形**：产品 run 的 `_check_git_diff_boundary` 仍把 schema 与契约列进 `boundary.out`、构造批仍每轮独立评审，只有第三条（`HD-16` 使「证据离仓自证」落空）随 `HD-28` 的 supersede 而变。拆分带来的新事实是冻结面**同时存在于两个仓**且无维持机制，但那是 rider `pin-drift` 记的问题，而一道 pre-commit 守卫守的是「不得无裁决写入」、**守不到「两仓一致」**，解不了它。故 **`E2` 维持不接**（`E6`）。redeem-when 重定为****去前缀那一件之后**（原文写「重扎根轮之后」，而该轮 2026-08-18 拆成三件、名字不再指任何单一轮次；条件的实质——调用者副本已删、冻结面只剩一份——已由 `SPLIT-COPY-RETIRE` 满足）——那时冻结面只剩一份，形势才真的变，届时再问一次；或下一个碰 `E2` 冻结面的批，孰先。**2026-08-20 复问已发生**（`DE-PREFIX` 收口，冻结面已单份）：用户裁**仍不接**（第三次；判据未变——一道 pre-commit 守卫守的是「不得无裁决写入」、守不到「两仓一致」，`E6` 不为此造机器）。「届时再问」一臂就此消耗，redeem-when 余单臂＝下一个碰 `E2` 冻结面的批。**第二问随行不变**：`pack_digests` 零调用（R4 实测仍只有 `__init__.py:238` 定义与 `:266` 导出）的真因是 v3 证据从不记自己由哪个 interface 版本产出，与 `E2` 无关 **触碰记录（轮 `CONTRACT-V4`，2026-08-23）**：FULL `O-1` 实测 `CONTRACT_PATH` 重指 v4 后零绑定——指回已退役路径 792 全绿，`pack_digests` 仍零调用（定义 `:238` + 导出 `:266` 重证）；VERIFY 又测 `E2` 的 blob 字面量同样无任何机器引用。touch 到达、`E6` 判据未变、行实质不变 | v3-review-verify-c05d052.md V-4 · `HD-27` |
| E10-sync | `E10` 钉死九成员后，成员名单同时活在三处：`CONSTRUCTION-CHECKLIST.md` 的 `E10` 成员句、`tooling/hooks/layer_path_check.py` 的 `LAYER`、`tooling/tests/document_harness/test_precommit_checks.py:225-236` 的 `EXPECTED`。后两者由 `test_layer_equals_the_hand_written_membership` 互相断言；**散文那一腿无任何守卫**——mutation 实证：从 `E10` 句删掉一条路径后它列八条却仍宣称 "exactly these nine paths and nothing else"，三 guard + 20 测试全绿（`7a08265` VERIFY P3 于十成员复证；route-b 轮 P2 又证 **(b) 必读义务句同腿同暴露**——删句后三 guard + 632 测试仍全绿）。钉死前散文是开口的、不可能因遗漏而与 `LAYER` 矛盾，现在可以且静默 | **碰 `E10` 成员句的任何批：三处（成员句 / `LAYER` / `EXPECTED`）同改并在 commit 正文点名**——本行是每次触碰时的核对项，非一次性兑付；**deadline = 下一次碰成员句的批**（2026-08-08 裁 `HD-22`：不加机器（`E6`），原 deadline「v4 铸成」经 `V-4` 判不再 bound damage）——2026-08-18 轮 `ORCHESTRATOR-CHARTER` 触碰成员句并三处同改（九→十，新成员 `ORCHESTRATION.md`），deadline 顺延至下一次碰成员句的批。**另记：`HD-22` 三处之外的散文站点已增至五**（read `7701f03` `L-2` 实测，量程＝全仓 tracked 排除 migration 与 journal）：调用者仓的 `.githooks/pre-commit`（2026-08-19 已更正为 ten）＋本仓 `CALLER-ONBOARDING` 轮新增三处——`document-harness/ONBOARDING.md:130` · 本仓 `.githooks/pre-commit:14-15` · 根 `README.md:33/:50`（`document-harness/README.md:34` 原已在册）。这些散文腿无任何守卫，触碰成员句的批按本行核对时**连带这五处**；**再记（VERIFY 71e1f24 `O-2`）**：`tooling/tests/document_harness/test_precommit_hook.py:58` 的 `MEMBER` 常量是第四份**机器侧**成员路径拷贝（单成员路径而非名单，`E5` 手写正是其绑定力来源；fail-safe——该成员若迁出层，该测试响亮变红而非静默） | v3-review-full-838c413.md L-3 + v3-review-verify-7a08265.md V-4 |
| ctx-ground | 「7 份真实指令的 Context 小节标题皆恰为 `## Context (non-normative)`」是 `_is_context_title` 精确匹配**与其负对照**共同站的地基，而**没有任何测试绑定它**：负对照断言的是测试文件自己的 `CONFORMING` fixture，不是 `assurance/runs/*/instruction.md`。将来某份指令把该标题写歪，方向是 fail-safe（掉进 prose 态、欠回全部派生件），但**静默**，且 docstring 那句会悄悄停止为真。**用户裁决不做**：让 harness 测试去读 run artifact 是新机器（`E6` 反面），且泛化抽取器正是 `RA`/`PD` 已记两次的「无调用者常驻件」形状。**2026-08-22 复问，用户第二次维持**（轮 `PRERUN-RIDERS`，产品首跑前的清账）：判据一字未变——让 harness 测试去读 caller 的 run artifact 仍是 `E6` 的反面；失败方向 fail-safe（掉进 prose 态、欠回全部派生件），**残余风险＝静默，明示接受**。补偿是起草侧的：首个产品 run 的 Context 小节标题按谓词读法**逐字**写，该纪律记在 `document-harness/plans/prerun-riders.plan.md` 的裁决 6，不进指令层（进层即加义务＝design）。机器侧缺口按裁决保留，故本行**重写而非删除**，deadline 不变 | 第一份编号态指令（P5B 批次）落地时，或下一批碰 `_is_context_title` / `test_instruction_form.py` 的负对照，孰先 | v3-review-verify-c7fb720.md O-2 |
| freeze-audit | **两道守卫互锁**：`review_freeze_check.py` 规定冻结窗口内只有评审记录能落（`E9`），而 `repo-audit.py` 是同一道 pre-commit 上的硬 gate——**返回的记录一旦踩中 audit，就没有任何顺序能同时满足两者**。本轮实例：FULL 记录引用评审员自己的 `grep '[[:space:]]'`，被读成坏 wikilink 挡住；先修 audit 再落记录这条路经实测被冻结钩子拦下（`review_freeze exit=1`，见 journal §8），于是只剩「删 marker」或「`--no-verify`」，两者都违反 `E9`。本次靠根因修 audit 绕开（`32cf220`），**代价是 base commit 的树不能自过 gate**（VERIFY `O-3v`）。下次未必有这么干净的根因修法：记录是不可编辑的独立证据（`R6`），而 audit 的判据可能根本不该为它让路 | 下一批碰 `review_freeze_check.py` / `repo-audit.py` 的 pre-commit 判据，或下一次返回记录被任何 gate 挡住时（**那一刻就是 deadline**——届时必须当场裁「谁让路」而无法再推迟） | v3-review-verify-25511d9.md O-3v + journal v1-context-exact-2026-08-05.md §8 实测 |
| waiver-live | `E10` 开轮豁免够不够到 (b) 必读句的 `§live` read，两个载体都没写：checklist 豁免从句只挂在层 cold read 上（`:109-116`），README `:29` "every cold read MUST" 在 cold read 被豁免时悬空，决策簿头部同样沉默且按构造管不了自己。分歧改变动作——豁免轮里一个 session 照读、另一个引豁免跳过，跳过会漏 live 裁决（如 `HD-23` 的预算判据）。**读者刻意不供字节**：任何澄清都给 `E10` 义务加 bound=设计开轮 | 下一批碰 `E10` cold-read/`§live` 句；**deadline = 用户第一次真给开轮豁免**（分歧咬人的那一刻） **触碰记录（轮 `PUB-FACADE`，2026-08-23）**：第一次真豁免已发生，未咬人——orchestrator 在豁免给出前已整读 `§live`（`document-harness/plans/publicization-a.plan.md` 裁决 3 记录之）；修法仍是 design，touch 到达而不可兑，redeem-when 不变 | v3-checkpoint-read-bd77fd4.md L-1 |
| decl-dup | bind 的 declarations 路径两处拼写（`:70` 常量 `DECLARATIONS` 只用于 STOP 消息，`:276` 独立重建 `CONTROL / "bind-declarations.json"`）——copy 类分叉，今日由 missing/complete 两条测试可捕。**字节已供**：`declarations_path = run_dir / DECLARATIONS`。未入修腿系 executor 呈表时漏报此条、用户未裁，按未接受项 bank | 下一批碰 `run_bind_v2.py` 的 declarations 读取或其 STOP 消息 | v3-review-full-eec4171.md L-3 |
| argv-cap | R2 把 comparator 声明搬上 check-spec argv 后撞 schema 约束：argv token 禁反引号/美元符/管道/分号/与号/尖括号且单 token ≤500 字符，候选面 2313 个标题中 **329（14%，126 文件）无法写成一条 argv 声明**（p4-doc 自身 0 拒、四份 frozen spec 不受影响）。R2 与修腿均未披露此天花板；修法（声明改文件承载、或转义约定）是设计题，字节不可现套 | **deadline = 第一个要绑 comparator mode 的 run 起草时**（届时 329 里任一标题成为 site 即咬人）；或下一批碰 `compare_blocks.py` 参数面，孰先 | v3-review-verify-3b6267c.md V-1 |
| retire-suite | VERIFY de8f4ef 两条同套件项：**V-5** kept 计数守卫绑在来源未绑谓词——fixture 给每个 ordered id 都写了 out.txt，把 `.is_file()` 谓词整个删掉 12 测试仍全绿；p4-doc 上该 mutation 打印 23 而实存 13（比 L-3 原病还糟一档）。**字节已供**：fixture 去掉一个 ordered id 的 out.txt（kept 断言与 survivors 循环两处随调）。**V-3** 双前缀第四实例 `test_run_v2_template_retire.py:105`（fixture docstring 写 `chk-<id>.out.txt`）——`E7` 同类清扫漏网 | 下一批碰 retire 套件或其 fixture | v3-review-verify-de8f4ef.md V-5 · V-3 |
| readme-three | README:12 「the three step scripts additionally take the round's refs」悬在四条 invocation 上（第四条 retire 零 round flags）；FULL d52f41b L-1 明言不以 executor 披露作 discharge，修腿未动。句字面仍真（恰好那三个吃 refs）但按句数脚本的读者会漏数 retire。字节方向：限定成 the three round steps (evidence, bind, repair) 类 | 下一批碰 run-v2 README 实例化段 | v3-review-full-d52f41b.md L-1 后半 · v3-review-verify-de8f4ef.md V-4 |
| status-key | `check_subject` 的 CLOSED carve-out 钥匙是 run **自写**的 `status` 字段，本路径零交叉核对（其余四个身份字段都对 CandidateRecord 核；`flow.check_state_pointers` 会要求 CLOSED 携 `final_decision_ref`/`summary_ref` 但不在此路径——callers 仅 `rsclib/document_harness/cli.py:346`/`flow.py:307`（前者原 `rsc.py:309`，随 R2 的 CLI 摘出而改址））。实测同场景一词之差：EVIDENCED+缺一份 → review exit 1；CLOSED+缺同一份 → review exit 0。补偿控制真实但在另一模块且响不硬（dispatch 腿 `NOT-DISPATCHABLE` exit 1）；具体代价 = `dtw review --subject` 不再能抓「递错 commit」（递了 closing commit 得干净报告）。用户裁（2026-08-10）：不加机器（`E6`），bank。**2026-08-22 复问，用户第二次维持**（轮 `PRERUN-RIDERS`，产品首跑前的清账）：`check_subject` 的 carve-out 原样不动，机器侧缺口按裁决**保留**，故本行**重写而非删除**。补偿控制自此不再只是「在另一模块」的旁证，而是一条点名的操作纪律：**orchestrator 在派发 closeout subject 之前，先对该 run 的 state 跑 `dtw flow`**——递错 commit 由既有机器在既有接缝上抓住，不为此造第二台机器（`E6`）。纪律未进指令层（进层即加义务＝design），承载＝本行 + 该轮 plan 的裁决 4 | **deadline = 第一个真实 run 的 closeout 评审**（递错 commit 可能性首次真实存在的时刻），或下一批碰 `check_subject` 的 status/carve-out 面，孰先 | v3-review-full-d52f41b.md O-1 |
| delta-prose | 「A future run's directory carries its delta and not the shared core」（A2 plan acceptance）现仅**散文承载**（run-v2 README + 修后三 docstring 同向），零机械 enforcement：已批的 write_scope 设计判断挡的是「候选**改**模板」，而 run **抄**模板写在自己 control root 内完全 conformant，warrant 够不着；`check_template_instance.py`（唯一 pre-START gate）只查 spec 版本/preamble/paragraph-map，不看 run 目录携带什么脚本。要不要加守卫是设计题（`E6` 倾向不加——需要新机器的修是重新质疑被守之物的信号；`R5` 归用户裁） | 首个新 run 起草时（散文成唯一防线的第一次实测），或下一批碰 `check_template_instance.py` 的 gate 判据面，孰先 | v3-review-full-7dd027e.md L-2 |
| pin-drift | 两棵树在 pin 上逐一相同（**在 `eb6fbc2` 的 tip 上由评审员实测**：当时的九成员 + 三守卫 + `cli.py` 全 SAME；此后成员增至十、守卫减至二，该实测是那个 revision 的事实而非现状），但**没有任何东西维持它**：一个 `E10` amendment 落在调用者侧之后，到 resync commit 加 gitlink bump 之间，真正执行的守卫是 pin 里那份旧的、不是刚改的那份。`HD-34` 的纪律覆盖的是反方向（调用者不得改 harness 内容），这个方向无载体。**无字节**：加一致性检查是新机器（`E6`），该不该做归用户（`R5`）| 修法为 design 形状（加一致性检查是新机器，`E6`），故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `HD-34` 纪律面或 resync 流程的批；**deadline = 第一个在调用者侧落 `E10` amendment 而未同步 pin 的轮次** | v3-review-full-eb6fbc2.md `O-4` |
| decited-paths | 本仓被删副本后，调用者侧 markdown 链接的**文本**是坏路径（**十处**来自 `SPLIT-COPY-RETIRE` 的去反引号扫类，**另加至少一处**由该轮 `B-2` 修复自身引入——`assurance/README.md` 的 `templates/run-v2/`；VERIFY `V-1`(b) 指出十处这个数在写下时已经旧了，**兑付时按当时的 base 重数**），而它们**只是被去掉了反引号**，不是被修好：`candidate_path_check` 只判反引号 token，去了引号它就看不见，于是十处「指向本仓不存在的文件」的字样对检查隐形。写成挂载点内路径**当时**会被同一支守卫误拦（rider `submod-index`：`TrackedPaths.from_index` 走 `git ls-files`，submodule 内容不在 index 里——2026-08-23 轮 `STRANGER-GUARDS` 兑付删行），所以彼时无可用字节。**真修依赖 `submod-index` 先落地**——让 checker 认 submodule 内部路径之后，这十处改成真路径即可 | 随 `submod-index` 兑付的同一批，或下一个碰 `candidate_path_check` / `paths.py` 的批，孰先 **触碰记录（轮 `STRANGER-GUARDS`，2026-08-23）**：`submod-index` 已兑——checker 认 submodule 内部路径（`paths.py` 经 gitlink 读 submodule 自己的 index）；真修自此可写，但站点全在调用者仓，按 `mount-inert` 先例跨仓兑付：坏链接文本按兑付时的 base 重数、随调用者侧 gitlink-bump 批改成真路径，本行在**那个批**删除 | v3-review-full-2d148f3.md `L-2` **触碰记录（VERIFY 2538893 `O-4`）**：同 `submod-index`——`DE-PREFIX` 修腿碰了两文件的 docstring，touch 到达而不可兑（依赖 `submod-index` 先落地，其修法是 design）。redeem-when 不变 **触碰记录（INIT-SURFACE）**：同上，再次 touch 而不可兑 |
| charter-qualifiers | `ORCHESTRATION.md` 九条 cite-only 行里有**三行压掉了被引规则自带的限定**：`E10` 的开轮豁免（unless the user waives it）· `E11` 的 wait 条件（unless told otherwise）· `R10` 的前提（只在 FULL 返 `REVIEWED_NO_BLOCKER` 带 low 时才有那个选择）。三处方向**都是更严**，故不会放行不该放行的事，但与被引规则**不等价**——而该表自陈的契约恰是「read the rule」，即指向即全部。改变的决定：一个只读该表的 orchestrator 会以为豁免不存在。**无字节**：补限定＝把规则抄进第二个成员（正是本文件收窄形要避免的），删该行＝减义务，两个方向都是 design | 下一个有资格开轮且碰 `ORCHESTRATION.md` 义务表的批；**deadline = 用户第一次真给开轮豁免**（三处里第一处会当场咬人的那一刻，与 rider `waiver-live` 同一时刻、不同缺陷） | v3-cold-read-50016a8.md `O-2` |
| self-caller-guards | 仪器仓的 `.githooks/pre-commit` 只跑 `layer_path_check`，而该仓对自己的构造轮**也是调用者**：`dtw dispatch` 在此写 `.harness/review-pending.json`（`CALLER-ONBOARDING` 一轮四次），故 `E9` 的冻结窗口在该仓**零机械执行**，`candidate_path_check` 同理不跑。加不加按 `R5` 归用户，**用户 2026-08-19 裁不进 `HD-48` 那批** | 下一批碰仪器仓 `.githooks/pre-commit`，或 **deadline = 该仓第一次在冻结窗口内落下非记录 commit**（`E9` 违反第一次真发生的时刻；不在本轮内——本轮四次窗口全由 orchestrator 手工守住） | `CALLER-ONBOARDING` executor 报告第 2 条 · `.githooks/pre-commit` 自陈段 · 用户裁决 2026-08-19 · FULL `v3-review-full-7f6e7f0.md` `O-6`（实测：本仓两个记录 commit 各含 8/1 个 unresolved token，对 candidate lint 是 work product，未拦只因 hook 只跑 `layer_path_check`） |
| e1-table | `ORCHESTRATION.md` 的九义务表**没有 `E1` 披露义务的行**：`E1` 现要求轮记录 executor 四持有中持了哪几项（载体＝commit 正文或轮 journal，责任人＝orchestrator——`XREPO-REFS` 落座），但只读义务表的 orchestrator 找不到这条，承载在 *may never do* 节的指路句里。加表行会同时打开 rider `charter-qualifiers` 的表面（三行压限定），故两者该同批收 | 下一个有资格开轮且碰 `ORCHESTRATION.md` 义务表的批（与 `charter-qualifiers` 同表面同批）；**deadline = 第一个由非本机 orchestrator 冷启动跑的构造轮**（只读表的读者第一次真出现） | v3-review-full-dd18226.md `O-2` · v3-review-verify-2937bcd.md `O-2` |
| hd38-both-ways | `LEDGER-SPLIT` 一轮内把 `HD-38` 两个方向各用一次、相隔三个 commit：一条自由通道字节独立成 commit（`30b33a9`），另一条搭了九路径收批车并改称 `E10` deferral（`69fc082`）——两读文本依据等强（`HD-38`「不搭车」vs `E10` deferral「nil-effect 可搭」），同形先例＝rider `wl-route`。读者刻意不供字节：tiebreak 是 design | 下一个有资格开轮且碰 `HD-38` 条目或 `E10` deferral 从句的设计批（与 `wl-route` 同类，宜同批收）；**无 deadline**——两读都不放行错误写入，分歧只在落点 | v3-cold-read-69fc082.md `L-2` |
| onboarding-labels | 指令层 README `:30` ONBOARDING 表行的五个标签只盖住九条中的八条：条目 2（`.harness/` + gitignore 条目）无标签对应。纯导航行，不改任何规则 | 下一批碰该表行或 `ONBOARDING.md` 条目集；**无 deadline**——漏的是标签不是条目，正文九条俱在 | v3-cold-read-69fc082.md `O-2` |
| e9-pair-budget | `E9` 的例外清单只点名 free channel，而 `E10` 给 must-fix 的 amendment+复读结对同样的零预算地位——两处口径分歧，读 `E9` 的人会把结对误记成消耗修腿。**第三次被记录**（`v3-checkpoint-read-136f27f.md` `O-3` · `v3-cold-read-4410899.md` `L-3`，前两次 routed nowhere），本行即其归宿。修法是 design（改 `E9` 措辞） | 下一个有资格开轮且碰 `CONSTRUCTION-CHECKLIST.md` `E9`/`E10` 预算措辞的批 | v3-cold-read-4410899.md `L-3` · v3-checkpoint-read-136f27f.md `O-3` |
| e10-cannot-see | `E10` 条款「守卫看不见什么」的清单**补了一项后仍自称完整仍短**：path shape 盲区只列 placeholder 一种，漏「无扩展名」与「七扩展名之外」（配对实测 `` `tooling/no-such-script.sh` `` 与 `` `.githooks/no-such-hook` `` 静默放行，positive control 被拦证明守卫到达；成员内今有实例——README `Local enforcement` 行的 `.githooks/pre-commit` token）。字节在 VERIFY 记录里；修法可能触发 `E10` design 判据（动条款枚举），故不走免通道。**追注（2026-08-21 cold read `v3-cold-read-dd22789.md` `O-2`）**：盲区不止条款清单——其上一句的绝对量词「the class entire」被同一探针证伪（`` `tooling/hooks/no_such.sh` `` 形状静默放行），兑付时要改两句、不是一句 | 下一个碰 `E10` 该条款或 `layer_path_check` `PATHLIKE` 的有资格开轮的批 | v3-review-verify-2538893.md `V-3` |
| exec-mount-test | 产品侧 `--executor` 模式无挂载布局测试，而它正是将来从调用者（仪器为 submodule mount）派发的模式；构造侧模式反而有一条（`test_a_caller_that_mounts_…`）。行为本身被 FULL 实测为对——charter 经 `instrument_relative` 解析，该函数有既有类属测试钉住——缺的是模式专属回归钉，非缺陷 | 下一批碰 `test_dispatch.py` 执行者测试类；**无 deadline**——行为已有属测钉住 | v3-review-full-229f03f.md `O-1` |
| hi-schema-gloss | `harness-issue.schema.json:45` `observed_after` description 第二句仍是松读法（"an issue claiming to be **observed** mid-run is unrepresentable"），与其自己第一句（"was **recorded**"）、修后的 `REVIEW.md:47-48` 与 `issues.py:140-141` 三处严读法相抵——轮 `PRERUN-RIDERS` 修腿把 `L-3` 点名的 `REVIEW.md` 站点修严后，本轮之前两处同松的文本变成一严一松，且无任何东西追踪幸存者。机器不受影响（schema 只校验枚举、`issues.py` 只比终态）；咬的是 closeout 时按 `REVIEW.md` 新路由归口观察的 orchestrator——打开 schema 被第二句告知「表达不出」，弃填，观察随轮死，正是 `REVIEW.md` 「观察活过轮次」句要防的结果。**字节已供但现在不能写**：该 schema 是 `E2` 冻结 pack 十五件之一（VERIFY 实核 pack 恰十五件），按 `HD-20` 先欠 recorded ruling；替换字节=把 *claiming to be observed mid-run* 换成 *recorded while the run is still in flight*（`L-3` 同款替换，全文在 VERIFY 记录 `V-1` 节）。同形先例 `wspec-owner` | `E2` 对该 schema 的 recorded ruling，或下一批碰 `harness-issue.schema.json` / `REVIEW.md` observation 路由节，孰先；**deadline = 第一次真有观察按新路由在 closeout 归口的时刻**（读者第一次真撞上第二句的时刻） | v3-review-verify-860729f.md `V-1` |
| py-convention | 裸 `python` 面向读者的命令类在轮 `PUB-FACADE` 量程外仍存活（VERIFY 声明量程=全仓 tracked 排除该轮 11 件、历史记录/journal 按引文排除）：`document-harness/EXECUTION.md:364`（**`E10` 成员**——POSIX executor 照句执行即 command not found）+ `assurance/templates/run-v2/README.md:14,16,18,19`（caller-facing 四处）。修法=批 A 已立的约定句形（一句罩全文件）；成员件字节按 `E10` 免通道落后欠 read 搭车 | 下一批碰 `EXECUTION.md` 或 run-v2 README；**deadline = 第一个 POSIX executor / 调用者真按这些句子敲命令的时刻** | v3-review-verify-71e1f24.md `O-1` |
| read-name-split | `R6` 给 read 记录两个文件名（`v3-checkpoint-read-` / `v3-cold-read-`）而层内无一处定义各自何时适用；`E10` 定义了 cold read，checkpoint read 全层无定义。改变的决定：`E10` 引用从句只认「recorded **end-to-end** read」，amendment 复读不算，文件名是判断先前覆盖的廉价索引而它没规格。今日不咬（57 份已落盘记录按先例可判，且每份按 `R4` 自陈覆盖面）。**无字节**：修法加从句=design | 下一个有资格开轮且碰 `R6` 记录通道句或 `E10` citation 从句的批；**无 deadline** | v3-cold-read-b8df15a.md `L-1` |
| v4-verifmode | contract v4 §5 Verification-mode 行（`contract/Document-Work-Assurance-Contract-v4.md:133`）仍列 `local_check_and_review`，而其自称 single home `common.schema.json#/$defs/verificationMode` 自 SIMP-A1（2026-08-05 recorded ruling）起只有两值——冻结 v3 源字面继承（`b2dbdf75:121`，签署时为真、合并时已陈旧）。机器 fail closed（`document-work-spec.v2.schema.json:151` ref 两值 home，实测拒第三值）；站着的是操作契约与成员 3 在 closed enum 成员资格上互驳，恰在 §5 自称权威表的表面。**字节已供**（删该值，或按 §13.1 版本边界形注为 v1-history）但落 `E2` 冻结路径 + `HD-20` 交叉，先欠 recorded ruling。与 `wspec-owner`/`hi-schema-gloss` 同类同路由 | `E2` 对 v4 的 recorded ruling（五处同类——本行 + `v4-plan-digest` + `wspec-owner` 两 schema title + `hi-schema-gloss`——宜一次裁），或下一批碰 v4 §5，孰先 | v3-cold-read-cf54a79.md `L-1` |
| v4-plan-digest | v4 `:34-36` 绑 plan 权威的 SHA-256（`9B08CD00…F171F`）在本仓任何 plan blob 上均不匹配（现 blob `37EA94BD…`、历史唯一另一 blob `8ECA2A0E…`）——digest 绑的是 caller 时代 2026-07-20 签署时的 plan 字节，本仓史起于 2026-08-15 且 plan 已随拆分搬移重扎根，自此 `UNVERIFIABLE` 而旁文无一字说明；陌生读者按此核 contract→plan 绑定会得出 mismatch/tampering 的错误结论（正解只在它够不到的 provenance 史里）。零机器消费此 digest，故无 verdict path 受影响。**两条修路都记录**：冻结内字节（同 `v4-verifmode` 路由）或冻结外一句（dh-README 的 contract-v4 行/plan 行加「digest 绑 caller 时代签署字节」）——走哪条归用户 | `E2` recorded ruling（同 `v4-verifmode` 五处同类），或用户裁走冻结外路线后下一批碰 `document-harness/README.md`，孰先 | v3-cold-read-cf54a79.md `L-2` |

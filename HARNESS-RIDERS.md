# HARNESS RIDERS — banked findings, redeemed on touch

Pure data table. The governing rules — the bank/HarnessIssue split, routing, row format,
redemption, and the 2026-07-30 tightenings — live in
`document-harness/RULES.md` R10, which held them in `document-harness/CONSTRUCTION-CHECKLIST.md`
until round `CORE-ONLY-LAYER` split that file (incorporated 2026-07-31; this header
carried them until then). The rule-① backfill of 2026-07-30 is recorded in commit
`0a3d18d`'s body.

| id | what | redeem when | source |
|---|---|---|---|
| wl-route | **供了字节的 wording-level finding 有两条路，三句规则二比一**：`E10` 自由通道枚举写的是「a finding below must-fix — **low or observation alike** — whose record supplies the exact bytes」（**没点名 wording-level**）· `R9` 抬头句说「A read's **wording-level** findings are **banked**, never rounds」，只把「no-decision-nameable」那一类写成搭下一批 · `R10` 路由句说自由通道收**any finding** whose record supplies the exact bytes 且「档位不改变路由」。两句说当场套、一句说入 bank。批 B R5 的 `W-1`（有名下游决定 + 有字节）**走了当场套**。改变的决定：一条 wording-level 的修**立刻落地并在层里待到下次 read**，还是等一个批——两读文本依据等强，两个 executor 必分歧（同形先例：`chk-thin`——已于 2026-08-22 轮 `PRERUN-RIDERS` 兑付删行，以及 `E1-suff`——已于 2026-08-18 轮 `ORCHESTRATOR-CHARTER` 兑付删行）。**读者刻意不供字节**：任何 tiebreak 都是给 `E10` 枚举或 `R9` 加 bound = design | 修法为 design 形状，故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `E10` 自由通道枚举句 / `R9` 抬头句 / `R10` 路由句的设计批；**deadline = 下一份对 wording-level finding 供字节的 read 记录**（分歧第一次真能咬人的时刻，按 `HD-37` ① 不在创造本行的 R5 之内）  **触碰记录（轮 `CORE-ONLY-LAYER`，2026-08-30）：三处表面同时被触碰而不兑付。** `E10` 自由通道枚举句、`R9` 抬头句、`R10` 路由句随拆分一并迁入 `document-harness/RULES.md`，三句**字节全同**（本轮的移动经逐条比对，二十一条里只有 `E10` 与 `R6` 变字节，见该 commit 正文）。分歧原样存在，只是换了文件；修法仍是 design | v3-checkpoint-read-f61ce2c.md `L-2` |
| RA | `run_all` 现有**一个产品调用者**——run-v2 evidence 模板（`HD-25`，批 B R1 `e9166d2` 接线，`run_evidence_v2.py:200`）；**另有两处单元测试调用** `tooling/tests/document_harness/test_candidate_checks.py:874`/`:887`（自 `11ce5b4`（2026-07-28）起，即本行出处那个 FULL 的同一个 commit——一条钉住「gap 之后的 check 不跑」、另一条是其负对照，**它们是今天唯一钉住 `run_all` stop 契约的东西**）。故原文"全仓零调用者"指的一直是产品侧、从来不是字面全仓（VERIFY `dbbec28` `V-2`）。**剩下的是 CLI 那半边**：产品 run 机检引擎仍无 CLI 接线，且模板是**抄**进 run 的，八个已关闭 run 不继承（rider `delta-prose`）。（原文"全仓零调用者"自 `e9166d2` 起为假，FULL `e9166d2` `L-2` 指出；用户 2026-07-30 归口） | **2026-08-17 用户裁再次重定范围、非兑付**——本行至此只剩 **CLI 入口**一件事。产品调用者那半**已由 `HD-25` 关闭**（批 B R1 `e9166d2` 把引擎接进 run-v2 evidence 模板，条目已 implemented；R4 实测今日一个产品调用者 `run_evidence_v2.py:200` + 三处测试引用），剩下的是「跑这一轮的机检」仍只住在**抄**进 run 目录的模板脚本里、`dtw` 八命令无一是它（2026-08-19 前写「六命令」、2026-08-21 前写「七命令」，`init`/`preview` 加入后计数两度作废而本行实质不变——两者都不是机检入口；`HD-47`/`HD-51`）。`split-design` §1 已裁六命令原样，故加不加第七个命令按 `R5` 归用户；用户判定这是**便利性而非正确性**——接线已让产品 run 真的在用引擎，`HD-25` 接受的 delta 代价（模板是抄的、已关闭 run 不继承）加不加 CLI 都一样存在——**不为此推翻 §1**（**该判断限于本行这个案子**：2026-08-19 用户就 onboarding 另裁了一个第七命令`dtw init`，`HD-47`——§1 的「六命令原样」自此读作搬迁指令而非命令数上限，本行的答案不因此改变）。redeem-when = **第一次需要在 run 目录之外跑机检**的场合，或下一个碰 `run_all` 签名的批（FULL `e9166d2` `O-1`：docstring 承诺保留 `SPEC_GAP` 证据，但 `raise` 时 `results` 是局部变量，调用者拿不到——动签名时 `test_candidate_checks.py:874/887` 就是影响面），孰先 | v3-review-full-11ce5b4.md O-4 · v3-review-full-e9166d2.md L-2 |
| PD | `document_harness/__init__.py:238` `pack_digests()` 哈希整个 v3 pack（含成员 9）却全仓零调用者（两处活调用是 v2 `schemas.pack_digests()`，另一 pack）；`E2` 现零机械 enforcement，此为唯一现成挂点。与 `RA` 同形状 | **2026-08-17 用户裁重定范围、非兑付**——`HD-27` 写明的重开条件（拆分批）已到达，R4 逐条复核后认定其三条理由**并未同时变形**：产品 run 的 `_check_git_diff_boundary` 仍把 schema 与契约列进 `boundary.out`、构造批仍每轮独立评审，只有第三条（`HD-16` 使「证据离仓自证」落空）随 `HD-28` 的 supersede 而变。拆分带来的新事实是冻结面**同时存在于两个仓**且无维持机制，但那是 rider `pin-drift` 记的问题，而一道 pre-commit 守卫守的是「不得无裁决写入」、**守不到「两仓一致」**，解不了它。故 **`E2` 维持不接**（`E6`）。redeem-when 重定为****去前缀那一件之后**（原文写「重扎根轮之后」，而该轮 2026-08-18 拆成三件、名字不再指任何单一轮次；条件的实质——调用者副本已删、冻结面只剩一份——已由 `SPLIT-COPY-RETIRE` 满足）——那时冻结面只剩一份，形势才真的变，届时再问一次；或下一个碰 `E2` 冻结面的批，孰先。**2026-08-20 复问已发生**（`DE-PREFIX` 收口，冻结面已单份）：用户裁**仍不接**（第三次；判据未变——一道 pre-commit 守卫守的是「不得无裁决写入」、守不到「两仓一致」，`E6` 不为此造机器）。「届时再问」一臂就此消耗，redeem-when 余单臂＝下一个碰 `E2` 冻结面的批。**第二问随行不变**：`pack_digests` 零调用（R4 实测仍只有 `__init__.py:238` 定义与 `:266` 导出）的真因是 v3 证据从不记自己由哪个 interface 版本产出，与 `E2` 无关 **触碰记录（轮 `CONTRACT-V4`，2026-08-23）**：FULL `O-1` 实测 `CONTRACT_PATH` 重指 v4 后零绑定——指回已退役路径 792 全绿，`pack_digests` 仍零调用（定义 `:238` + 导出 `:266` 重证）；VERIFY 又测 `E2` 的 blob 字面量同样无任何机器引用。touch 到达、`E6` 判据未变、行实质不变 **触碰记录（批 `FREEZE-TO-ALARM`，2026-08-27/28）：第四次触碰到达，本行重定范围——既非兑付，也不是第四次拒绝。** 余下那条单臂「下一个碰 `E2` 冻结面的批」就此**消耗**。该批 item C 给 `E2` 装上了机械 enforcement：CI job `announced-path-disclosure`（`.github/workflows/ci.yml` + `tooling/announced_path_disclosure.py` + 其测试），谓词是**事后披露**（某 commit 改了 announced path，它自己的 commit message 有没有写出该文件的完整仓内相对路径），不是 `HD-27` 三次拒绝的那个**写前授权**谓词，也不挂 pre-commit。**本行第一半的前提因此被实测证伪**：`pack_digests()` 不是「唯一现成挂点」，它对这个问题根本不是挂点——它返回整包一个聚合 digest 且读工作树，而报警必须点名**哪个 commit 改了哪个文件**，聚合值点不出文件、读工作树也评不了一个 range，`git diff-tree` 才是答这个问题的命令（论证见 item C commit `1d4d9aa` 正文与该脚本 docstring）。故「`E2` 现零机械 enforcement，此为唯一现成挂点」这半边**就此关闭**：守卫已加，且没有用它——`HD-27` 点名拒绝的那件事至今未做。**余下的实质只剩「`pack_digests()` 零调用」一件**（于本批 tip 重测：`git grep -n 'pack_digests' -- '*.py'` 只返定义 `tooling/rsclib/document_harness/__init__.py:238`、导出 `:266`，以及 `tooling/announced_path_disclosure.py:24` 一处散文说明它为何不被使用——不是调用），其处置是 `document-harness/split-design.md` §5 的**删除提议**，与 `E2` 无关。redeem-when 因此重定为：**下一批碰 `tooling/rsclib/document_harness/__init__.py` 导出面、或碰 `document-harness/split-design.md` §5 那条删除提议的批**——删一个零调用函数是代码而非指令层，`E10` 的 design test 够不着，故按 `R10` 不必点名「有资格开轮」的表面，任何碰到该面的批都可兑；**无 deadline**——死代码没有开始咬人的时刻，与本行原状一致 | v3-review-verify-c05d052.md V-4 · `HD-27` |
| E10-sync | `E10` 钉死九成员后，成员名单同时活在三处：`CONSTRUCTION-CHECKLIST.md` 的 `E10` 成员句、`tooling/hooks/layer_path_check.py` 的 `LAYER`、`tooling/tests/document_harness/test_precommit_checks.py:225-236` 的 `EXPECTED`。后两者由 `test_layer_equals_the_hand_written_membership` 互相断言；**散文那一腿无任何守卫**——mutation 实证：从 `E10` 句删掉一条路径后它列八条却仍宣称 "exactly these nine paths and nothing else"，三 guard + 20 测试全绿（`7a08265` VERIFY P3 于十成员复证；route-b 轮 P2 又证 **(b) 必读义务句同腿同暴露**——删句后三 guard + 632 测试仍全绿）。钉死前散文是开口的、不可能因遗漏而与 `LAYER` 矛盾，现在可以且静默 | **碰 `E10` 成员句的任何批：三处（成员句 / `LAYER` / `EXPECTED`）同改并在 commit 正文点名**——本行是每次触碰时的核对项，非一次性兑付；**deadline = 下一次碰成员句的批**（2026-08-08 裁 `HD-22`：不加机器（`E6`），原 deadline「v4 铸成」经 `V-4` 判不再 bound damage）——2026-08-18 轮 `ORCHESTRATOR-CHARTER` 触碰成员句并三处同改（九→十，新成员 `ORCHESTRATION.md`），deadline 顺延至下一次碰成员句的批。**另记：`HD-22` 三处之外的散文站点已增至五**（read `7701f03` `L-2` 实测，量程＝全仓 tracked 排除 migration 与 journal）：调用者仓的 `.githooks/pre-commit`（2026-08-19 已更正为 ten）＋本仓 `CALLER-ONBOARDING` 轮新增三处——`document-harness/ONBOARDING.md:130` · 本仓 `.githooks/pre-commit:14-15` · 根 `README.md:33/:50`（`document-harness/README.md:34` 原已在册）。这些散文腿无任何守卫，触碰成员句的批按本行核对时**连带这五处**；**再记（VERIFY 71e1f24 `O-2`）**：`tooling/tests/document_harness/test_precommit_hook.py:58` 的 `MEMBER` 常量是第四份**机器侧**成员路径拷贝（单成员路径而非名单，`E5` 手写正是其绑定力来源；fail-safe——该成员若迁出层，该测试响亮变红而非静默）  **触碰记录兼核对（轮 `CORE-ONLY-LAYER`，2026-08-30）：成员句被触碰，三处同改，本行不兑付（按本行自陈：每次触碰的核对项）。**① 成员句随规则拆分**换了家**——自本轮起住 `document-harness/RULES.md`（本行上格写的 `CONSTRUCTION-CHECKLIST.md` 就此为旧址，原句留着不改，`HD-59`），且句子本身由一句变两句：第一句只点 harness 自己的九成员（`CONSTRUCTION-CHECKLIST.md` 出、`RULES.md` 入，**计数仍为九**），第二句说一个仓在 `harness.json` 里声明自己的规则、被声明的规则只绑声明它的那个仓。② `LAYER`（`tooling/hooks/layer_path_check.py`）与 ③ `EXPECTED`（`tooling/tests/document_harness/test_precommit_checks.py`）同 commit 改同一条路径；第四份机器侧拷贝 `test_precommit_hook.py` 的 `MEMBER` 亦同改。**五处散文站点逐处核过，无一需改**：计数全是「九」而九未变——`.githooks/pre-commit:14-15` 两处、根 `README.md` 的 Layout 段与状态表那一行、`document-harness/README.md` 的 Local enforcement 行；`document-harness/ONBOARDING.md` 已无成员计数句。**另有两处点名旧址的散文被改**：根 `README.md` 与 `README.zh-CN.md` 的 `E10-sync` 段各把「成员句在 `CONSTRUCTION-CHECKLIST.md`」改指 `RULES.md`——本行上格的五处清单未含它们，故一并记：**散文站点自此为七**。deadline 顺延至下一次碰成员句的批  **触碰记录兼核对（轮 `CORE-ONLY-CODE`，2026-08-30）：成员句再次被触碰，三处同改，本行不兑付。**① 成员句由九路径改为**七**（`document-harness/RULES.md`，item D 删掉两份 retired contract stub；同段末尾另起一句写明它们何时、因何离开，`HD-59` 向前更正）。② `LAYER`（`tooling/hooks/layer_path_check.py`）删同两条，其 docstring 的「nine members」改「seven members」。③ `EXPECTED`（`tooling/tests/document_harness/test_precommit_checks.py`）删同两条。④ 第四份机器侧拷贝 `test_precommit_hook.py` 的 `MEMBER` **本轮不改，理由是它不需要改**——其值自轮 `CORE-ONLY-LAYER` 起即 `document-harness/RULES.md`，删掉的两条都不是它，且它仍是成员；该值的绑定力由 mutation 实证：把 `document-harness/RULES.md` 从 `LAYER` 删掉，`test_precommit_hook.py` 3 passed → 1 failed（`test_hook_blocks_when_the_layer_check_fails`），`test_precommit_checks.py` 55 passed → 8 failed，即本行所记的 fail-safe 成立。**散文站点自七改记为十一行、六个文件**，逐处实测后全部同改（量程＝全仓 tracked 排除 `migration/`、`document-harness/journal/`、`document-harness/plans/` 与 ledger archive，键为大小写不敏感的 `nine` 与「九」）：`.githooks/pre-commit:14`、`:15`；根 `README.md` 的 Layout 段、状态表那一行、`E10-sync` 段；`README.zh-CN.md` 对应三处；`document-harness/README.md:22` 与 `:26`（`:26` 一行内两处计数）；`document-harness/ONBOARDING.md:150`。上一次触碰记的七处未含 `document-harness/README.md:22`、`:26` 与 `ONBOARDING.md:150`——那三处当日成立（`:22`/`:26` 当时写「nine」而九为真），本次是**范围更正**而非推翻。同一扫描下剩余的 `nine`/「九」全部**不是成员计数**，逐条列出以免下一轮重扫：`ONBOARDING.md` 的九个 onboarding 项（`:3`、`:210`、`:219`、`:236`）、`ORCHESTRATION.md:48` 的九条义务、`document-harness/README.md:22` 的「nine of its twelve obligations」、`:24` 的「nine rows」、`flow.py:99` 的九个未守卫状态。deadline 顺延至下一次碰成员句的批  **残余枚举的范围更正（轮 `CORE-ONLY-CODE` 修腿，2026-08-30；`HD-59` 向前更正，上一段逐字留着）：上面那份「剩余的全部不是成员计数」漏了三处，它们是成员计数且已被本轮改假。** FULL `v3-review-full-70c82b4.md` `B-1` 在同一量程下实测出 `CONTRACT-V4-SIGNATURE.md:197`、`document-harness/io-design.md:8` 与 `:42` 三处仍写九。第一处已于本修腿 commit 改为不写计数；后两处**不改**——`document-harness/io-design.md` 的 blob `a1594eb2…` 正是 `HD-35` 第三次签字所绑的那一份，改它欠一次重签，故按 plan 裁决 34 的另一臂「在 commit 正文里点名并写明为何不改」处理，理由载于本修腿 commit 正文。同段另一处随 plan 裁决 37 (a) 的替换漂了一行：`ORCHESTRATION.md` 的九条义务今在 `:49`（原记 `:48`）。**本行的残余枚举自此读作**：`ONBOARDING.md` 的九个 onboarding 项（`:3`、`:210`、`:219`、`:236`）、`ORCHESTRATION.md:49` 的九条义务、`document-harness/README.md:22` 的「nine of its twelve obligations」、`:24` 的「nine rows」、`flow.py:99` 的九个未守卫状态，加上 `document-harness/io-design.md:8` 与 `:42` 两处**待重签才动得了的成员计数**。本次写入不是触碰记录、也不是兑付：本 commit 不碰成员句，故本行的触碰条件未到达，deadline 不动 | v3-review-full-838c413.md L-3 + v3-review-verify-7a08265.md V-4 |
| freeze-audit | **两道守卫互锁**：`review_freeze_check.py` 规定冻结窗口内只有评审记录能落（`E9`），而 `repo-audit.py` 是同一道 pre-commit 上的硬 gate——**返回的记录一旦踩中 audit，就没有任何顺序能同时满足两者**。本轮实例：FULL 记录引用评审员自己的 `grep '[[:space:]]'`，被读成坏 wikilink 挡住；先修 audit 再落记录这条路经实测被冻结钩子拦下（`review_freeze exit=1`，见 journal §8），于是只剩「删 marker」或「`--no-verify`」，两者都违反 `E9`。本次靠根因修 audit 绕开（`32cf220`），**代价是 base commit 的树不能自过 gate**（VERIFY `O-3v`）。下次未必有这么干净的根因修法：记录是不可编辑的独立证据（`R6`），而 audit 的判据可能根本不该为它让路 | 下一批碰 `review_freeze_check.py` / `repo-audit.py` 的 pre-commit 判据，或下一次返回记录被任何 gate 挡住时（**那一刻就是 deadline**——届时必须当场裁「谁让路」而无法再推迟） | v3-review-verify-25511d9.md O-3v + journal v1-context-exact-2026-08-05.md §8 实测 |
| argv-cap | R2 把 comparator 声明搬上 check-spec argv 后撞 schema 约束：argv token 禁反引号/美元符/管道/分号/与号/尖括号且单 token ≤500 字符，候选面 2313 个标题中 **329（14%，126 文件）无法写成一条 argv 声明**（p4-doc 自身 0 拒、四份 frozen spec 不受影响）。R2 与修腿均未披露此天花板；修法（声明改文件承载、或转义约定）是设计题，字节不可现套 | **deadline = 第一个要绑 comparator mode 的 run 起草时**（届时 329 里任一标题成为 site 即咬人）；或下一批碰 `compare_blocks.py` 参数面，孰先 | v3-review-verify-3b6267c.md V-1 |
| delta-prose | 「A future run's directory carries its delta and not the shared core」（A2 plan acceptance）现仅**散文承载**（run-v2 README + 修后三 docstring 同向），零机械 enforcement：已批的 write_scope 设计判断挡的是「候选**改**模板」，而 run **抄**模板写在自己 control root 内完全 conformant，warrant 够不着；`check_template_instance.py`（唯一 pre-START gate）只查 spec 版本/preamble/paragraph-map，不看 run 目录携带什么脚本。要不要加守卫是设计题（`E6` 倾向不加——需要新机器的修是重新质疑被守之物的信号；`R5` 归用户裁） | 首个新 run 起草时（散文成唯一防线的第一次实测），或下一批碰 `check_template_instance.py` 的 gate 判据面，孰先 | v3-review-full-7dd027e.md L-2 |
| pin-drift | 两棵树在 pin 上逐一相同（**在 `eb6fbc2` 的 tip 上由评审员实测**：当时的九成员 + 三守卫 + `cli.py` 全 SAME；此后成员增至十、守卫减至二，该实测是那个 revision 的事实而非现状），但**没有任何东西维持它**：一个 `E10` amendment 落在调用者侧之后，到 resync commit 加 gitlink bump 之间，真正执行的守卫是 pin 里那份旧的、不是刚改的那份。`HD-34` 的纪律覆盖的是反方向（调用者不得改 harness 内容），这个方向无载体。**无字节**：加一致性检查是新机器（`E6`），该不该做归用户（`R5`）| 修法为 design 形状（加一致性检查是新机器，`E6`），故按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `HD-34` 纪律面或 resync 流程的批；**deadline = 第一个在调用者侧落 `E10` amendment 而未同步 pin 的轮次** | v3-review-full-eb6fbc2.md `O-4` |
| charter-qualifiers | `ORCHESTRATION.md` 九条 cite-only 行里有**三行压掉了被引规则自带的限定**：`E10` 的开轮豁免（unless the user waives it）· `E11` 的 wait 条件（unless told otherwise）· `R10` 的前提（只在 FULL 返 `REVIEWED_NO_BLOCKER` 带 low 时才有那个选择）。三处方向**都是更严**，故不会放行不该放行的事，但与被引规则**不等价**——而该表自陈的契约恰是「read the rule」，即指向即全部。改变的决定：一个只读该表的 orchestrator 会以为豁免不存在。**无字节**：补限定＝把规则抄进第二个成员（正是本文件收窄形要避免的），删该行＝减义务，两个方向都是 design | 下一个有资格开轮且碰 `ORCHESTRATION.md` 义务表的批；**deadline = 用户第一次真给开轮豁免**（三处里第一处会当场咬人的那一刻，与 rider `waiver-live` 同一时刻、不同缺陷） **触碰记录（轮 `CORE-SET-LAYER`，2026-08-26）**：本轮改了该表的 `E10` 行（删掉进 mount 的链接、只留裸名），touch 到达而**不兑付**——用户 2026-08-25 裁决 14：九条设计行由 dispatch-economy 批一次收，本行随该批。三处压掉的限定一字未动，`E10` 行仍不带豁免限定。另记：同一时刻的 `waiver-live` 本轮已兑付删行（`E10` 从句现明写 `§live` 不随冷读豁免而免），deadline 指的那一刻不变，本行的缺陷——只读该表的 orchestrator 以为豁免不存在——原样存续 | v3-cold-read-50016a8.md `O-2` |
| e1-table | `ORCHESTRATION.md` 的九义务表**没有 `E1` 披露义务的行**：`E1` 现要求轮记录 executor 四持有中持了哪几项（载体＝commit 正文或轮 journal，责任人＝orchestrator——`XREPO-REFS` 落座），但只读义务表的 orchestrator 找不到这条，承载在 *may never do* 节的指路句里。加表行会同时打开 rider `charter-qualifiers` 的表面（三行压限定），故两者该同批收 | 下一个有资格开轮且碰 `ORCHESTRATION.md` 义务表的批（与 `charter-qualifiers` 同表面同批）；**deadline = 第一个由非本机 orchestrator 冷启动跑的构造轮**（只读表的读者第一次真出现） **触碰记录（轮 `CORE-SET-LAYER`，2026-08-26）**：本轮改了该表的 `E10` 行，touch 到达而**不兑付**——同用户 2026-08-25 裁决 14，与 `charter-qualifiers` 同表面同批。表里仍无 `E1` 披露义务的行 | v3-review-full-dd18226.md `O-2` · v3-review-verify-2937bcd.md `O-2` |
| hd38-both-ways | `LEDGER-SPLIT` 一轮内把 `HD-38` 两个方向各用一次、相隔三个 commit：一条自由通道字节独立成 commit（`30b33a9`），另一条搭了九路径收批车并改称 `E10` deferral（`69fc082`）——两读文本依据等强（`HD-38`「不搭车」vs `E10` deferral「nil-effect 可搭」），同形先例＝rider `wl-route`。读者刻意不供字节：tiebreak 是 design | 下一个有资格开轮且碰 `HD-38` 条目或 `E10` deferral 从句的设计批（与 `wl-route` 同类，宜同批收）；**无 deadline**——两读都不放行错误写入，分歧只在落点  **触碰记录（轮 `CORE-ONLY-LAYER`，2026-08-30）：touch 到达而不兑付。** `E10` 的 deferral 从句随拆分迁入`document-harness/RULES.md`，字节未动；`HD-38` 条目本轮未碰。修法为 design，不在本轮授权内 | v3-cold-read-69fc082.md `L-2` |
| e9-pair-budget | `E9` 的例外清单只点名 free channel，而 `E10` 给 must-fix 的 amendment+复读结对同样的零预算地位——两处口径分歧，读 `E9` 的人会把结对误记成消耗修腿。**第三次被记录**（`v3-checkpoint-read-136f27f.md` `O-3` · `v3-cold-read-4410899.md` `L-3`，前两次 routed nowhere），本行即其归宿。修法是 design（改 `E9` 措辞） | 下一个有资格开轮且碰 `CONSTRUCTION-CHECKLIST.md` `E9`/`E10` 预算措辞的批  **触碰记录（轮 `CORE-ONLY-LAYER`，2026-08-30）：touch 到达而不兑付。** `E9` 与 `E10` 的字节随规则拆分整体迁入 `document-harness/RULES.md`，两条的预算措辞**一字未动**，故本行所记的分歧原样存在，只是换了文件。不兑付的理由是边界：修法是给 `E9` 的例外清单加一项或给 `E10` 的结对句加限定，都是 design，而本轮的授权（plan 裁决 2/8/9/10/14 与 `HD-67`）只覆盖规则拆分、成员句、`harness.json`、契约与 `CLAUDE.md` | v3-cold-read-4410899.md `L-3` · v3-checkpoint-read-136f27f.md `O-3` |
| e10-cannot-see | `E10` 条款「守卫看不见什么」的清单**补了一项后仍自称完整仍短**：path shape 盲区只列 placeholder 一种，漏「无扩展名」与「七扩展名之外」（配对实测 `` `tooling/no-such-script.sh` `` 与 `` `.githooks/no-such-hook` `` 静默放行，positive control 被拦证明守卫到达；成员内今有实例——README `Local enforcement` 行的 `.githooks/pre-commit` token）。字节在 VERIFY 记录里；修法可能触发 `E10` design 判据（动条款枚举），故不走免通道。**追注（2026-08-21 cold read `v3-cold-read-dd22789.md` `O-2`）**：盲区不止条款清单——其上一句的绝对量词「the class entire」被同一探针证伪（`` `tooling/hooks/no_such.sh` `` 形状静默放行），兑付时要改两句、不是一句 | 下一个碰 `E10` 该条款或 `layer_path_check` `PATHLIKE` 的有资格开轮的批  **触碰记录（轮 `CORE-ONLY-LAYER`，2026-08-30）：touch 到达而不兑付。** `E10` 该条款随拆分迁入`document-harness/RULES.md`，清单句一字未动；`layer_path_check` 的 `PATHLIKE` 本轮未碰（改的是 `LAYER` 与新增的 `scanned_paths`）。不兑付同 `e9-pair-budget`：给清单补项是给规则加从句＝design，不在本轮授权内  **触碰记录（轮 `CORE-ONLY-CODE`，2026-08-30）：touch 到达而不兑付，但本行点名的那个**活实例已消失**。**本行上格写「成员内今有实例——README `Local enforcement` 行的 `.githooks/pre-commit` token」；该 token 于本轮 item K 按裁决 24 的删除原则删掉（`document-harness/README.md:26`，句子改写为「as a tracked hook」，不再写路径），故**成员内该盲区自此零实例**——量程＝`E10` 七成员加本仓声明的规则文件，键为反引号里含 `.githooks` 的 token，实测返 0。**盲区本身一字未改**：`E10` 该条款的清单句与其上一句的「the class entire」都在 `document-harness/RULES.md` 里原样，`layer_path_check` 的 `PATHLIKE` 本轮未碰。**故本行不因此删除**：它记的是守卫看不见什么，不是某一个实例；下一次有人往成员里写一个无扩展名或七扩展名之外的token，守卫照旧静默放行。**另记，供兑付时用**：同类 token 在 `document-harness/ONBOARDING.md` 尚有五处（`:32`、`:52`、`:63`、`:148`、`:150`），其中 `:150` 的「the hook is `.githooks/pre-commit` at this repository's root」是本仪器持有物；ONBOARDING 是产品层第 4 行但**不是成员**，sweep 与守卫都扫不到它，且裁决 24 当日实测的九处不含它，故本轮不动、只记  **触碰记录（轮 `CORE-ONLY-CODE` 的修正 pass，2026-08-30）：上一段点名的 `:150` 已降名，本行仍不删除。** plan 裁决 37 (c) 按裁决 24 的原则把该句改为「the hook that calls it is the instrument's own, at the instrument's own root」——不写路径、不写文件名，故 `document-harness/ONBOARDING.md` 内**本仪器持有物**的同形 token 归零；量程＝该文件内反引号里含 `.githooks` 的 token，实测五处降为四处，余 `:32`、`:52`、`:63`、`:148` 全是叫调用者自建 hook 的指示、属调用者持有，按本轮指令原样留下。**盲区本身仍一字未改**：`E10` 的 what-the-guard-cannot-see 清单句与其上一句的 the class entire 都在 `document-harness/RULES.md` 里原样，`layer_path_check` 的 `PATHLIKE` 未碰；本行记的是守卫看不见什么、不是某一个实例，故 touch 到达而不兑付 | v3-review-verify-2538893.md `V-3` |
| e1-reader | `E1` 修正句（`1a0a200`）点名的派发对象是「reviewer or executor」，**漏了 reader**（`dtw dispatch --read` 的第三个派发家族）；其理由——subagent 不加载 system config——对 reader 一字不差地同样成立，但字面读该句管不到 read 派发。今日不咬：本轮开轮 commit 手写了「全部派发走 `claude -p`」，且实际 read 已按此跑。**无字节**：补名单＝给规则加 bound＝design | 下一个有资格开轮且碰 `E1` form 从句或 `ORCHESTRATION.md` 三角色表的批（与 `e1-table`/`charter-qualifiers` 同表面，宜同批收；M-1 amendment 碰了该表但按 `R10` must-fix 通道 meet touch 不兑 design 行）；**deadline = 第一个不由本机 orchestrator 冷启动跑的构造轮**（字面读者第一次真出现） | v3-cold-read-21dad76.md `L-2` |
| read-name-split | `R6` 给 read 记录两个文件名（`v3-checkpoint-read-` / `v3-cold-read-`）而层内无一处定义各自何时适用；`E10` 定义了 cold read，checkpoint read 全层无定义。改变的决定：`E10` 引用从句只认「recorded **end-to-end** read」，amendment 复读不算，文件名是判断先前覆盖的廉价索引而它没规格。今日不咬（57 份已落盘记录按先例可判，且每份按 `R4` 自陈覆盖面）。**无字节**：修法加从句=design | 下一个有资格开轮且碰 `R6` 记录通道句或 `E10` citation 从句的批；**无 deadline**  **触碰记录（轮 `CORE-ONLY-LAYER`，2026-08-30）：`R6` 记录通道句被**改字节**而本行仍不兑付，照记。** 本轮把该句里的 `migration/document-work-assurance-v3/` 降为「该仓自己声明的 review-records 目录」——那是一个只有本仪器有的目录，写在产品档文件里就是调用者解不出的 path token（`E10` 自己的「name and its holder」条款），本仓的那个值改由 `CONSTRUCTION-CHECKLIST.md` 承载。**两个读记文件名之间仍无判据，本行所治的正是这件事，一字未动。** 不兑付：加判据是给 `R6` 加 bound＝design | v3-cold-read-b8df15a.md `L-1` |
| figure-units | 三处字节/字符figure 的**口径不决定其值**(VERIFY `V-1`,类而非例):(a) 原 `CORE-SET.md:34-36` 声明的量程是「`git ls-files` 的树 + `stat -c%s` 工作树字节 @ `core.autocrlf=true`」,而工作树字节在本仓**不是树的函数**(389 件全 `i/lf`,工作树 315 `w/crlf` / 73 `w/lf` / 1 mixed)——同一棵树在新克隆上是 6.39 MiB、此 checkout 上 6.38 MiB、executor 记的 6.37 MiB,产品档同理 0.730 vs 0.732。**没人量错,是配方不钉值**。修法:字节改用 **blob 字节**(`git cat-file`,树决定其值)或去掉第三位有效数字。**不改变任何行动**——每个 figure 支撑的结论(调用者只背走一小部分;行数上限看不见行内增长)都原样成立,且清单头部已写「re-run rather than cite」。**触碰记录（轮 `CORE-SET-SIGNATURE`，2026-08-26）**：item N 把 `CORE-SET.md` 并入 `CONSTRUCTION-INDEX.md`，站点 (a) **就此关闭**——合并后的清单只留 file count（树决定其值），并在脚注写明为什么不写 worktree byte figure，即本行开的那个药方；(b)(c) 两处 journal 数字仍开、按 `HD-23` 路由，故本行改指不删 | 下一个碰 `CONSTRUCTION-INDEX.md` figure 段或写新 byte figure 的批;**deadline = 第一次有人拿这些 byte figure 做决定**(口径差第一次真咬人的时刻)  **触碰记录（轮 `CORE-ONLY-LAYER`，2026-08-30）：touch 到达而不兑付。** 本轮改了 `CONSTRUCTION-INDEX.md` 的 figure 段（产品档件数 15→14、树件数 391→415、并写明本轮自己的记录会再次作废它），但本行现存的两处站点 (b)(c) 是 journal 里的 byte figure，按 `HD-23` 路由、不在本轮改动边界内。**同轮第二次触碰（修腿，FULL `v3-review-full-8f6b3ef.md` `L-4`）**：该段的测量锚点 `cbaee8e` 改为 `4b81dd9`——两个 figure 只在后者上重现；口径问题不是本行所治的锚点问题，(b)(c) 原样开着，理由同上一条，是边界而非判断  **同段第三次触碰（轮 `CORE-ONLY-CODE`，2026-08-30）：touch 到达而不兑付。**本轮又改了该 figure 段：产品层仍 **59**、全仓 415→**421**，锚点由 `4b81dd9` 改为 `8ce93f7`，并加一句写明本轮为何只动分母（加一个构造侧 dispatch 生成器与两个测试文件、删两个 stub，四件都在产品层各行之外）。**仍未写入任何 byte figure**，站位 (a) 依旧关闭、(b)(c) 依旧原样；本行的兑付条件是「写 byte figure」，未到达 | v3-review-verify-0f0498f.md `V-1` |
| template-clause-unguarded | `templates/decision-log.md` 新增的开轮必读从句**零机器守卫**:突变实测删掉整个从句后 `test_init_command` 23 + `test_caller_surfaces`/`test_precommit_checks` 84 = **107 全绿**,而 107 正是修腿正文当作证据的那个数;正对照(删被钉的第 11 行)1 failed。钉子(`DECISION_LOG_HEADER_LINES`,`E5` 手写字面量)够得到**那个块**、够不到追加在其后的从句;全仓对该模板的机器引用只有该常量与一个 byte-identity 测试(比对拷贝与模板,故钉不住内容)。同形先例:`v3-review-full-2026a14.md` `B-2`——inheritance 那句缺席数周而全绿。**读者刻意不开药方**:扩字面量是在 `E6` 说该重新质疑的面上加机器 | 下一个碰 `test_init_command.py` 的 `DECISION_LOG_HEADER_LINES` 或该模板头部规矩的**有资格开轮**的批;**deadline = 该从句第一次被误删或改写而无人发现**——即下一次有人编辑该模板头部 | v3-review-verify-0f0498f.md `V-3` |
| onboarding-carries-construction | `document-harness/ONBOARDING.md` **依赖调用者不携带的构造侧材料**，两个病灶一个病：**(a) 四个 Owner 单元格仍以 `io-design.md` 为唯一归属**——item 4「rider bank 以空实例发货」· item 5「journal 刻意不预建」· item 6「harness 不提供 ledger 模板」＋「由 policy file 声明 ledger 参数」· item 7「policy file 独立成件」的载体决定；`io-design.md` 自陈是批 B R2 交付物、不是成员、对任何规则无权威，核心集清单把它归构造档，于是调用者照 ONBOARDING 走时六项里的答案指向一份它没有、且自称什么都不管的文件。**(b) `ONBOARDING.md:12/:196/:206` 三处引本仪器的构造 journal**（`caller-onboarding-2026-08-19.md` ×2 · `stranger-proof-walk-2026-08-24.md`），journal 同样归构造档。(b) 被裁决 12 的**判据**判死、被它的**枚举**漏掉——枚举点的是十一处**成员**站点而 `ONBOARDING.md` 不是成员，`sweep_refs.py` 不扫它、验收①的 grep 只量三个 register；三处**早于本轮**（`cc3b3ab`/`92cc514`/tip 均为 3）。**本行由两行合并而来（用户裁 2026-08-26）**：同一文件同一病，分两行记就是这条观察本身在说的「靠加部件」。**触碰记录（轮 `CORE-SET-LAYER`）**：(a) 的 item 5 Owner 格已被修腿改过一次（FULL `L-4`，删去误挂 `HD-1` 的 one-file-per-round 归属），touch 到达但**只改了半个**——改掉的是 `HD-1` 那半，`io-design.md` §6 承载的「刻意不预建」原因仍在该格；裁决 15 的硬界（**无别家者不得现造 owner**，那是给规则加从句＝design）未被突破 | **两条兑付臂，形状不同、不得互相绑住**：**(a) 需开轮**——修法是给某条规则加从句以承接那四个决定，按 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `ONBOARDING.md` 的 Owner 列、`E10`/`R10` 的调用者持有物条款、或 `templates/` 头部规矩的批。**(b) 不需开轮**——修法是路径降名（item J/M 同形），下一批碰 `ONBOARDING.md` 引用面或 `CONSTRUCTION-INDEX.md` 产品档定义的即可兑，先兑 (b) 不影响 (a) 仍挂。**deadline（两臂共用）= 第一个只带产品档冷启动、照 ONBOARDING 走的调用者**——四个 Owner 格与三处 journal 引用同时指向它手上没有的文件的那一刻；与 `checklist-cited-not-carried` 同一时刻、不同缺陷。按 `HD-37` ① 不在创造本行的轮次之内 **触碰记录（轮 `CORE-SET-SIGNATURE`，2026-08-26）**：item N 把 `CORE-SET.md` 并入 `CONSTRUCTION-INDEX.md`，本行两处锚点随之改指；两条兑付臂一条都未走，(a) 四个 Owner 格与 (b) 三处 journal 引用原样。**(b) 兑付（轮 `CORE-ONLY-LAYER`，2026-08-30）：三处路径降名已落，本行因 (a) 仍挂而不删。**触碰条件由本轮给 `ONBOARDING.md` 加第十项（`harness.json`）而到达；三处 `journal/…` path token 改为「裸名 + holder 句」——`:12` 与 `:221` 的 `caller-onboarding-2026-08-19.md`、`:231` 的 `stranger-proof-walk-2026-08-24.md`，各自写明 held by this instrument's own construction history。**(a) 四个 Owner 格一字未动**：其修法是给某条规则加从句以承接那四个载体决定，是 design 且不在本轮授权内（本轮的 plan 裁决只授权规则拆分、成员句、`harness.json`、契约、`CLAUDE.md`），故仍挂在 (a) 那条臂上  **触碰记录（轮 `CORE-ONLY-CODE`，2026-08-30）：(a) 臂的 touch 再次到达而不兑付。** `65ecdac` 按 plan 裁决 37 (c) 改写 item 9 的 Owner 格（`ONBOARDING.md:150`，本仪器持有的 hook 路径 token 按裁决 24 原则降名）；items 4–7 的四个 Owner 格一字未动，(a) 的修法仍是 design、不在裁决 38 的授权内，行照留 | 用户裁决 15 · plan `document-harness/plans/core-set.plan.md` item L · v3-review-verify-0f0498f.md `V-2` |
| archive-header-selfcount | `HARNESS-DECISIONS-archive.md:7`（由 `a554c0b` 写入）记「移入后本档 **404** 行」，而该文件在 `a554c0b` 上实测 **411** 行：新增的抬头块自身 7 行，404 + 7 = 411——数字测于它所量之物**最后一次改动之前**，`E3` 的形状自指地发生在写下它的那个块上。**不改变任何行动**：`HD-6` 的门槛是 100 行，404 与 411 都远过线，触发与「不清」的答案都不受影响，故按 `R9` 属 wording-level。留档是因为反差就在同一个抬头块内——第五次询问的行数由冷 executor 早一个 commit（`07ef526`）写下，**345 且准确**，同一形状的两个 figure 一对一错。修法＝把 404 改成写入后的真值，或改成不自指的量程说法 | 下一批碰 `HARNESS-DECISIONS-archive.md` 抬头块或其行数 figure 的批；**无 deadline**（`R9`：无可点名的下游决定，故搭下一批，不开轮、不开 read） **触碰记录（批 `FREEZE-TO-ALARM` 修腿，2026-08-28）：touch 到达而不兑付，照记。** 本次 `HD-44` 移档在抬头块加了第七次询问块，故本行的触碰条件成立；**不兑付的理由是边界而非判断**——本修腿的边界是用户 2026-08-28 批准的三条 finding（`HD-44` 后继 · announced 集合无本仓枚举 · `split-design.md` §5 被证伪），把一个无关的 figure 更正折进任一条都是超出已批准的修腿边界（`E9`：超边界须明说），故按边界留行、在 `HD-44` 移档 commit 正文里点名。**新块不复制该缺陷**：第七次询问块刻意不写移入后的行数，改写成「要数就现跑 `wc -l`」，即本行开的两个药方里的第二个——但只作用于新写的字节，`404` 那处原样（它住在已提交的记录里，改法本身还牵到 `HD-59`/`HD-23` 的分界，一并留给兑付它的那一批） | v3-review-full-a554c0b.md `L-3` |
| r9-terminal-no-carrier | **`R9` 的终端路由「rides the next batch」是层里唯一不写下任何承载物的路由分支,且实测已经丢过一次**。上一份冷读的 `O-3`(`README.md:22` 的 "tenth member" 序数陈旧)按 `R9` 明确路由到「下一批碰这个成员的批」;轮 2 **确实**碰了那个成员(`07ef526` 改 `:16`),而该句字节一字未动,且 `HARNESS-RIDERS.md` / `CONSTRUCTION-LEDGER.md` / plan / 轮 2 的 FULL 与 VERIFY **全部无记载**——一个周期、一个实例、零承载。**缺陷在路由不在那句话**(该句作为带日期的历史成立,`E10` 成员句才是成员数的唯一权威)。对照:`E10` 两条通道当场落字节;`R10` 的 bank 写一行带 redeem-when 与 deadline;只有 `R9` 这一支什么都不写,于是**「已路由」与「已丢」是同一个状态**,收轮的 orchestrator 与下一轮都分辨不出。**无字节**:给该分支加承载物是给规则加从句＝design,`E10` 的设计测试当场开轮、自由通道被其优先句关闭 | 修法为 design 形状,故按 `HD-37` ② 只点名**有资格开轮**的表面:下一个有资格开轮且碰 `R9` 抬头句 / `R10` 路由句 / `E10` 自由通道枚举句的批——**与 rider `wl-route` 同一表面、不同问题**(那行治「**供了字节的** wording-level finding 走哪条」,本行治「**没有字节的** finding 被送去「下一批」之后住在哪」),宜同批一并裁;**deadline = 下一份把 finding 按 `R9` 终端支路由出去的 read 记录**——第二次丢失真能被指认的时刻,按 `HD-37` ① 不在创造本行的这次开轮读之内  **触碰记录（轮 `CORE-ONLY-LAYER`，2026-08-30）：本行等的那个测量到了，且这一次那句话被修了。** 开轮冷读 `v3-cold-read-a542c6d.md` `O-5` 实测 `document-harness/README.md` 的「added as the tenth member 2026-08-18」第五次陈旧地留在原处，并**刻意不再走 `R9` 终端支**（那正是四次把它丢掉的那条路），改为记在本行上。本轮触碰该文件，故顺手改了那半句：日期留着，序数「tenth」删去，改写为「成员有几个是 `E10` 成员句的事、不是这一行的事，而它读九」。**本行不兑付**：它治的是「`R9` 终端支路由出去的 finding 没有载体」这个结构问题，一个实例被修不等于分支有了载体；修法（给 `R9` / `R10` / `E10` 三句之一加载体从句）是 design，不在本轮授权内。丢失次数就此可点名为五次，第五次由本轮结束 | v3-cold-read-b737742.md `L-2` |
| itemh-sweep-count | **`5a39945` 正文的两个计数各短一,而它自陈的站点清单本身就证伪它。** 正文写「`git grep -n` 全树(排除 history)返 **23** 行;其中恰**六**处是 DEFAULT 并随之搬迁」。实测于 `fccadfb`:`git diff 5a39945~1 5a39945 -U0` 的删除行里含旧目录名者 **7** 行——`caller.py:50` · `test_precommit_checks.py` 31/32/419/430 · `test_init_command.py:140` · `test_caller_surfaces.py:206`,正是正文随后逐一点名的那一组(`caller.py:47` 也被点名,但它是「the migration directory」的注释、不带 token,故不在 grep 行内)。留下的**十七**处则正确:于 tip `1db5155` 按正文点名的六个文件逐一 `git grep -c`,5+2+1+2+3+4＝17,分毫不差。故 7 ＋ 17 ＝ **24**,不是 23。**类已关闭**——每处站点都被点名、该搬的都搬了,故要改的是那两个数字而不是扫类本身。改变的决定:`HD-41` ①③ 存在的理由就是让带计数的断言可被重跑,而下一个重跑此扫类以确认类已关闭的轮次会拿到不同的数,且分不清是漏了站点还是算错了数。按 `R9` 属 wording-level(准确事实可由相邻的逐处枚举与 `git diff` 复得)。**成行而非编辑**:该数字住在已提交的 commit 正文里,`HD-59` 禁止就地改写已提交的结论 | 下一批重跑该目录名扫类、或碰 item H 站点划分的批;**deadline = 下一次有人重跑这个扫类并拿它与 `5a39945` 的旧数对账的时刻**(计数第一次真咬人的时刻,按 `HD-37` ① 与 `R10` 不在创造本行的本轮之内) | v3-review-full-1db5155.md `L-1` |
| itemg-linecount-file | **`56d1b17` 正文的行数 figure 点错了文件。** 正文写「In `flow.py` the `review_binding` indirection, which had no caller anywhere. **That file** drops from 782 lines to 184」,指代对象取前一句点名的 `flow.py`,而那是错的一个。实测 `git show <rev>` 逐个 `wc -l`:`flow.py` 在 base `d3cda1a` 与 tip `1db5155` **两端都是 770 行**;真正 782 → 184 的是 `review.py`。可由同一段落随后的 `review.py` 删除清单与该 commit 自己的 diffstat 复得,故按 `R9` 属 wording-level。**成行而非编辑**:同 `itemh-sweep-count`,figure 住在已提交的 commit 正文里,`HD-59` 禁止就地改写 | 下一批碰 `flow.py` / `review.py` 行数 figure 的批;**无 deadline**(`R9`:两个文件的真实行数 `wc -l` 当场可得,无可点名的下游决定会因这句指代而走错——与 `archive-header-selfcount` 同形) | v3-review-full-1db5155.md `L-3` |
| e10-freeze-exception | **`E10`「what the guard still cannot see」清单的末句例外挂在一个不再存在的状态上。** `document-harness/CONSTRUCTION-CHECKLIST.md:185` 仍写 "the bytes `E2` freezes are excepted while they are frozen"，其前提是那被点名的十六件根本写不进去、故其存量文本不会长出守卫从不重扫的 token（该前提可读于 `39a21a8` 正文，它正是据此留下五个 frozen-supersession token 残留）。本批 item A（`184387c`，2026-08-27）把 `E2` 从写前 gate 改成事后逐点披露的报警，冻结状态就此不存在，例外的条件今后永不成立。**它不给出错误答案**：条件为假时例外静默落空、回到「存量文本不重扫」的通则，而那正是今天的实情；缺陷是同一个成员里两句对 `E2` 说不同的话——只读 `E10` 尾句的读者可据以认为 `E2` 仍在冻字节，而 `E2` 自己已明写 *frozen* 自此只是「哪些路径」的旧名、不是禁令。**刻意不供字节**：删这个例外或改写它的条件，改的是 `E10` 条款所例外的东西＝改变规则所要求的东西，按 `E10` 自己的 design test 是设计、要开轮 | 修法为 design 形状，故按 `R10` 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `E10`「what the guard still cannot see」清单句的批——**与 rider `e10-cannot-see` 同表面、不同缺陷**（那行治清单**漏项**：path shape 盲区少列两类，另有一份读记 heading 指针为第五类；本行治清单末句**例外的前提已消失**），宜同批一并收；**无 deadline**——例外的条件今后永不成立，故它静默失效而非给出错误答案，没有可点名的、缺陷开始咬人的时刻（与 `hd38-both-ways` 同形）  **触碰记录（轮 `CORE-ONLY-LAYER`，2026-08-30）：touch 到达而不兑付，并改指新址。** 该清单末句随 `E10` 整条迁入 `document-harness/RULES.md`（本行上格写的 `document-harness/CONSTRUCTION-CHECKLIST.md` 就此为旧址，原句留着不改，`HD-59`），例外从句一字未动。不兑付的理由是边界而非判断：删掉一个已无前提的例外是删 bound＝design，本轮虽是设计轮但其授权不含这一条，且本轮的改动边界已由 plan 裁决逐条钉死 | 轮 `FREEZE-TO-ALARM` item A commit `184387c` 正文——该 commit 自己把本站点点名为「本轮证伪、但既非 item A 的站点也不在本轮改动边界内」而不改（`E8`）· 用户裁决 2026-08-28（入 bank，不开轮） |
| announced-set-anchor | **`E2` 点名的十六条 announced 路径，在本仓没有任何可解析的枚举。** 条款用两个本仓够不到的指称定义该集合——「the fourteen of the 2026-07-29 entry」（本仓历史始于 `345acdd`，2026-08-15；那条登记住在被抽取的源仓）与「the pack held at the 2026-08-03 re-baseline」（同样早于本仓的全部历史）；`HD-62`（原 `HD-44`）只带件数「十五件」、不带件名；item A（`184387c`）又按 open question 3 的答案删掉了 contract v4 的 blob 字面量——本仓最后一个当场可核的锚点，`CONTRACT-V4-SIGNATURE.md` 记的是**被签**的 blob 而非当前 blob。今日该集合仍解得出，唯一的理由是 pack 恰好正好装着十五件〔实测于本行写入的 tip，量程＝全仓 tracked：`git ls-files schema/document-assurance-v3/ \| wc -l` → **15**，且这十五件与守卫 `ANNOUNCED` 的十五条一一对上〕。于是**该集合在本仓的唯一枚举是守卫自己的 `ANNOUNCED`（`tooling/announced_path_disclosure.py:56-73`）与其手写测试孪生（`tooling/tests/document_harness/test_announced_path_disclosure.py:53-70`）**，两者互相断言、不断言别的任何东西——`E10-sync` 的形状挪了一个面：三份拷贝、散文那一腿零守卫，而本仓对「散文腿坏掉照样全绿」有 mutation 实证（见 rider `E10-sync`）。item C 的正文点名援引了 `test_precommit_checks.py` 的 `EXPECTED` 先例，把它已知的这个缺口一并继承而未记。**改变的决定，两个方向都开着**：pack 第一次增件或减件时，一个问「这件是不是 announced」的 executor 从 `E2` 拿不到本仓可解析的答案，只能去读守卫——**规则反过来依赖守卫**，正是 `E5` 要防的倒置，也正是本轮在测试层刻意避开的那个；反方向是某次再基线把一件新 schema 写进 `E2` 的散文而 `ANNOUNCED` 未同步，报警从此静默地不看它，无一处变红。**读者刻意不供字节**（FULL `L-1` 明写），本行的判断随之：三条候选修法**都是 design**——给 `E2` 补一条本仓可解析的锚点句是**给规则加从句**（`E10` 的 design test：加从句即设计、当场开轮）；把两个指称换成十五条字面枚举是**替换文本使规则所要求之物改变**（集合的权威从一件历史事实挪到本条款的字节，且新造第四份无守卫拷贝，正是 `E10-sync` 记的那个形状）；把 `ANNOUNCED` 断言等于 `git ls-files schema/document-assurance-v3/` 则根本不修散文那一腿，反而把守卫的期望做成被守之物的函数，并对 `E2` 明拒的自动入册施压  **触碰记录（轮 `CORE-ONLY-LAYER`，2026-08-30）：touch 到达而不兑付。** `E2` 整条随规则拆分**留在**`document-harness/CONSTRUCTION-CHECKLIST.md`（拆分的判据是「产品 run 遵不遵守」，而 `E2` 点的是本仪器自己的字节），其 announced 清单句与再基线判据**字节未动**；本轮唯一相关的改动是 `CONSTRUCTION-INDEX.md` 产品档第 2 行的件数由 15 改为实测的 14，并在该行写明 `E2` 冻的是 2026-08-03 再基线那天的十五件、是一个 pack 已不再等于的快照——那正是本行所记的分歧，第一次写进了产品档清单本身。**不兑付**：给 `E2` 加一份可解析的枚举或改再基线判据都是 design，不在本轮授权内；用户 2026-08-28 的「维持」裁决未被推翻 | 修法为 design 形状，故按 `R10` 与 `HD-37` ② 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `document-harness/CONSTRUCTION-CHECKLIST.md` 里 `E2` 的 announced 清单句、或碰其再基线判据的批；**deadline = `schema/document-assurance-v3/` 第一次增件或减件的那一刻**——集合停止「恰好等于十五」、那两个指称第一次真的解不出来的时刻，按 `HD-37` ① 与 `R10` 不在创造本行的本轮之内（本批不碰 announced 字节，plan 的 *Constraints* 明写） **触碰记录（轮 `V1-RESULT-RETIRE`，2026-08-28）：deadline 到达；用户裁「维持」，redeem-when 重定，不兑付。** 本行的 deadline 由该轮 item C 删除 `schema/document-assurance-v3/review.schema.json` 而到达——pack 第一次减件。**上格「今日该集合仍解得出，唯一的理由是 pack 恰好正好装着十五件」自此为假**，原文逐字留着（`HD-59`）：实测于该轮 item C 落盘之后，量程＝全仓 tracked，`git ls-files schema/document-assurance-v3/ \| wc -l` → **14**，而 `announced_path_disclosure.py` 的 `ANNOUNCED` 仍列 15 条 schema（连同 contract v4 共 16），两者第一次对不上。**这个差是刻意的、不是漏改**（用户裁决 2026-08-28 ruling 8，plan `document-harness/plans/v1-result-retire.plan.md` item E）：`E2` 的清单点的是 2026-08-03 再基线时 pack 装着的那十五件——一段历史，今天删一件不改变当时装了什么——而守卫的清单按 `E5` 手写、故意不回读目录。**本行记的缺陷因此从「今天恰好还解得出」变成「今天已经解不出」**：一个问「这件是不是 announced」的 executor 现在必须先知道该集合是历史快照，才读得懂 15 与 14 的差，而 `E2` 在本仓可解析的锚点仍是零，三条候选修法一条未变、仍全是 design。故按用户裁决 2026-08-28 ruling 9 **维持不修**，redeem-when 由上文那句**重定为：下一个以 design 开的轮**（比 plan 作者所提的「等 pack 第一次增件」更严，且不再等一个「碰到该条款」的批）。先例＝rider `PD`，三次维持而非兑付 | v3-review-full-ad0663d.md `L-1` · 用户裁决 2026-08-28（本轮修腿：判为 design、入 bank，不开轮）· 用户裁决 2026-08-28 ruling 9（轮 `V1-RESULT-RETIRE`：deadline 到达时维持并重定，载体 `document-harness/plans/v1-result-retire.plan.md`） |
| alarm-mutation-gaps | **报警脚本有两处改坏了测试不会红。** FULL 自己跑的六组 mutation 里四组被钉住、两组没有：`alarm_floor` 的返回与另一处（详见记录 §1.1）。FULL 判两者都构造不出现实触发路径，故为 observation 而非 finding——但「今天构造不出」不等于「改了不会咬」，测试矩阵没绑住的行就是下一次改动的盲区 | 下一个碰 `tooling/announced_path_disclosure.py` 或其测试的批 | FULL `v3-review-full-ad0663d.md` `O-6` · 用户裁决 2026-08-28（preclear 路由入 bank） |
| alarm-yaml-range-untested | **报警 job 的「范围取哪一段」那几行仍然零测试覆盖——这是兑付 `alarm-yaml-untested` 时刻意留下的那一半，不是同一条重开。** 前身的兑付条件（本仓第一条 PR 走完，`pull_request` 分支首次真实求值）已满足，其所问的「那一次取的范围对不对」已答；**它的标题句所记的缺陷没有跟着关闭**。`.github/workflows/ci.yml` 里决定脚本收到什么范围的三处——`:45` 的 `fetch-depth: 0`、`:53` 的 `BEFORE`、`:54` 的 `AFTER`（两个 env 各含一个 push-事件 / pull_request-事件回退表达式）——仍无任何测试碰得到。实测于本轮 tip，量程＝该守卫的整个测试文件：`tooling/tests/document_harness/test_announced_path_disclosure.py` 的 **18** 条测试全部经同一个 `run()` 直接调 `alarm.check(repo.root, before, after)`，before/after 是参数，故判据被逐条钉死而**取值这一段整层裸露**。判据再对，范围取错就整条失效。**今天不咬人**：两个事件分支各已实跑一次绿——但「跑过一次绿」不是测试，下一次改这三行不会有任何东西变红。**无字节**：要测 YAML 表达式得新造一层（渲染 workflow，或起 `act`），值不值得按 `E6` 该先重新质疑被守之物，归用户（`R5`） | 下一批碰 `.github/workflows/ci.yml` 里 `announced-path-disclosure` job 的批；**deadline = 第一次出现那两次实跑都没覆盖到的事件形状**——新建分支时 `github.event.before` 的全零值、fork 来的 pull_request、或该 job 被第三种事件触发——按 `HD-37` ① 与 `R10` 不在创造本行的本轮之内 | 轮 `V1-RESULT-RETIRE` item H：plan `document-harness/plans/v1-result-retire.plan.md` 的 scope note 明写「是否另立一行由 executor 判」，本行即那个判断 · 前身 `alarm-yaml-untested`（FULL `v3-review-full-ad0663d.md` `O-2`，同轮兑付删行） |
| caller-cannot-resolve-ids | **九个反引号 hex commit id 住在 travel 的成员里，对调用者一律解析不到，而说明它们在哪解析的规则住在调用者拿不到的文件里**。站点（于本轮 tip `8ce93f7` 重测，量程＝七成员里所有 7–40 位的反引号 hex token，逐个 `git cat-file -t`）：`document-harness/README.md:26` `820b287` · `document-harness/EXECUTION.md:259` `a22cca0` · `:358` `838c413` · `:407` `ddd773a` · `:409` `a8af54c` · `:412` `6fd0ae3` · `:435` `418b89c` · `:494` `9ba9bbc` · `contract/Document-Work-Assurance-Contract-v4.md:246` `7db177d`——九个全 MISSING；同量程里另有三个**解析得到**的（`EXECUTION.md:411` `0d73a5f`、contract `:281` `56d1b17`、contract `:284` 的 blob），contract `:284` 那句自陈「in no working tree and in no repository that runs against this contract」，即它自己告诉读者别去找，九个没有这句。**解析规则在哪**：`document-harness/CONSTRUCTION-CHECKLIST.md` 的 *Where a cited commit id resolves*——本仓自己声明的规则，`E10` 明写「只绑声明它的那个仓」，且 `CONSTRUCTION-INDEX.md` 把它留在构造侧。**为什么专猎该类的那一轮没抓到**：`E10` 的 caller-held 条款是为**路径**写的，两个执行它的仪器也都只认路径形状——`layer_path_check.PATHLIKE` 要求已收录扩展名或结尾斜杠，`sweep_refs` 的 `NAMETOK` 匹配 basename——七位 hex 两者都不是，故 acceptance 1 在 harness-only 树上的零对这九条**是沉默的**。**会走错的下游决定**：`EXECUTION.md:358` 告诉一个正在判「这批改动算 doc-only 还是碰了 tooling」的读者（同节把该分层判定写成每一趟都绑），说已有两轮验收如此读过该条款，并点名 `838c413` 为其一；在调用者那里 `git show 838c413` 失败，而调用者手上没有任何东西说这个 id 是过期的、还是它的 clone 短了、还是它属于另一个仓。同形状另有两处：`:435` 的 revert 锚点代价（`418b89c`）与 `:494` 的 checker 撰写规则出处（`9ba9bbc`）。**读者刻意不供字节**：能让该类可判的修法是给 `E10` 加一条把 holder 约定从路径延伸到被引 id 的从句——加 bound＝design；没有该从句而手改九句话，是一个没有规则要求的编辑 | 修法为 design 形状，故按 `R10` 只点名**有资格开轮**的表面：下一个有资格开轮且碰 `E10` caller-held 引用条款、或碰 `sweep_refs` 解析规则的批；**deadline = 第一个既 mount 本 harness、又不持有本仓历史的仓库**（九条第一次对一个别无他路的读者真的失效的时刻，按 `HD-37` ① 不在创造本行的那一轮之内） | v3-cold-read-e88094c.md `L-1` |
| dispatch-exec-perms | **派出去的 cold executor 在本机跑不了 `python`、也跑不了 `git` write**，故电池 / 守卫 / mutation / commit 全由 orchestrator 代跑代提——`HD-55` 的角色分离在**字节**上成立（每个工作 commit 都写明 orchestrator 未编辑 executor 一个字节），但 `E1` 执行侧的**验证**那一半事实上由 orchestrator 替 executor 承担。轮 `V1-RESULT-RETIRE` 内实测**四次**（两份 amendment + 工作项 + 其修正派发），非偶发。目标 = `document-harness/ORCHESTRATION.md` 三角色表 / `HD-55` | 下一个可开轮的批（其修属 design，按 `R10` 不得指任何批）  **触碰记录（轮 `CORE-ONLY-CODE`，2026-08-30）：不兑付，但本行的前提在本轮被实测证伪，范围因此收窄。**本轮的 cold executor **`python` 与 `git` write 都跑得了**：电池（`python -m pytest tooling/tests -q`）、三个守卫、`ledger_cap_check`、`announced_path_disclosure`、四次 mutation、`git archive` 建 harness-only 树、以及本轮全部工作 commit（`7bcdace`、`08d3137`、`8ce93f7` 及本条所在的这个）**全部由 executor 自己跑、自己提**，orchestrator 未代跑代提任何一件。故本行「`E1` 执行侧的验证那一半事实上由 orchestrator 承担」在本轮**不成立**。**不因此删行**：本行记的是 `HD-55` 角色分离在验证这一半上**可能**塌陷，其载体（`ORCHESTRATION.md` 三角色表 / `HD-55`）一字未改，且权限是每台机器、每次派发的偶然事实——轮 `V1-RESULT-RETIRE` 内四次为真，本轮四次为假，两者都不是文本保证。**本行的实质因此重述为**：三角色表与 `HD-55` 都没说被派出的 executor 必须能自己跑验证，所以两种情形都合规而后果差别很大；要让它不再靠运气，得给那两处加一句＝design | 轮 `V1-RESULT-RETIRE` 的 plan Notes 与 `006138e` 正文；FULL `v3-review-full-a518888.md` §4 尾段确认 |
| caller-rule-read-no-generator | **调用者修改自己声明的规则文件时欠 `E10` 的独立 read，而生成那次 read 派发的模式已不在它手上**。`E10` 写明：一个仓在 `harness.json` 里声明的规则「amended under this rule's own discipline — including the independent read」；而轮 `CORE-ONLY-CODE` 的 item C（`7bcdace`）把 `--range` / `--read` / `--construction-executor` 三个只有构造轮用的模式移出产品层、落到本仓自己的 `tooling/construction_dispatch.py`（构造侧、不 travel），产品层剩下的 `dtw dispatch` 只有 `--subject` 与 `--executor`。故对一个 mount 本 harness 的调用者：义务照旧在，命令没有了。**目标条款/文件**：`document-harness/RULES.md` 的 `E10`（声明规则那两句 + 独立 read 那句）· 产品层仍带的派发生成器 `tooling/rsclib/document_harness/dispatch.py` 与 `tooling/rsclib/document_harness/cli.py` 的 `dispatch` 分支。**为什么 item C 不因此算错**：acceptance 6 的界是「harness 树上不得有构造专用代码路径」，而 read 模式今天只有全层形态，那正是构造侧的形态；原样留在产品层等于把构造轮的形态卖给调用者。**修法是 design**：要么给产品层加一个窄 subject 的 read 模式（命令面改动按 `HD-47` 逐案归用户），要么给 `E10` 加一句说明调用者怎么履行该 read——两条都是加从句或加机器，`E10` 为之开轮；按 `R5`，「那个模式该不该在那里」不是 executor 的结论。**本行刻意不供字节**：供了就是替用户做那个设计决定 | **plan 裁决 36 明裁 banked**：redeem-when ＝ 批 `dispatch-economy` 的**第一件**——must-fix 结对复读的窄 subject 出单形态（账本 `CONSTRUCTION-LEDGER.md` 的「dispatch-economy 构造批候选」条 ①）；该件本身就是要开轮的命令面设计，故符合 `R10`「design 形状的行只点名有资格开轮的表面、不点任何批」。**deadline = 第一个修改自己声明的规则文件的调用者**（义务与「没有命令」第一次同时落到一个别无他路的人手上的时刻，按 `HD-37` ① 不在创造本行的本轮之内） | plan `document-harness/plans/core-only.plan.md` 裁决 36 · journal `document-harness/journal/core-only-code-2026-08-30.md` §7.2 @ `e8b120c` |
| e10-fifth-reader | **`E10` 第二句的「Four readers」枚举在本仓短一个**：`tooling/construction_dispatch.py`（轮 `CORE-ONLY-CODE` item C）也读 `harness.json` 的 `rules`，且是唯一在声明缺席时**拒绝派发**（`V3-DISPATCH-NO-DECLARED-RULES`）而非降级的读者——恰是该句「文件缺席时会改变的决定」里最强的一种。不能把它的路径写进会 travel 的成员（裁决 24 删的正是这一类）；修法＝给枚举加不带路径的第五项（「以及一个仓自己的构造侧派发，若它有」之类）＝给规则加从句＝design | 下一个有资格开轮且碰 `E10` 第二句的批（`dispatch-economy` 要碰 `E10` 措辞面，宜同批）；无 deadline——陈旧枚举不会在某一刻开始咬人 | FULL `v3-review-full-70c82b4.md` `O-1` · 用户裁决 2026-08-30（「直接进 rider，等下一次可以走的时候加第五个」） |
| orch-caller-rule-counterpart | **`document-harness/ORCHESTRATION.md:41-44` asserts of any caller's declared rule file that it names `RULES.md` as its counterpart** — true of this repository's checklist, required of no caller by any rule; a travelling member describing a caller's own file by a property the caller was never asked to give it. Either generalising the clause (a change with no decision behind it) or requiring the property of a declared rule file (a bound, `E10`'s design test) needs a ruling; no bytes. | the next round touching `ORCHESTRATION.md`'s charter paragraph, or a design round on what a declared rule file must carry | `v3-cold-read-006d0d5.md` O-1 |
| e10-its-referent | **`document-harness/RULES.md:116` "Its edits are additive or subtractive" — the pronoun's antecedent, the instruction layer, sits three sentences back behind the two deletion-history sentences.** Wording-level under `R9`: no check outcome, binding, permission, obligation or verdict path changes, and the clause can only be about the layer. | the next batch touching `E10`'s deletion-history sentences or its amendment clause | `v3-cold-read-006d0d5.md` O-2 |
| summary-reads-unvalidated | **`summary.check_assurance_candidate` reads the round-0 FULL (and every review it reads) without validating any of it** — the one live member of item 7's class, "a function that decides from a ReviewResult it never validated", outside the module round `PROMISE-PATH-ENGINE` fixed; named in `09ed9ed`'s own scope statement as reported-not-fixed. `E7` says the class is what gets closed. | the next batch or round touching `tooling/rsclib/document_harness/summary.py` or closing item 7's class; **deadline = before the next product run's bind** (the moment an unvalidated result next feeds a candidate check) | `v3-review-full-38038ec.md` O-2, user-ruled to bank 2026-09-03 |
| no-repair-unbound | **The `NO_REPAIR` decision that licenses the candidate bind after a clean FULL is bound by nothing**: no digest of it reaches the state or the candidate, and `check_state_pointers` forbids a `repair_decision_ref` at repair round 0, so a later reader of an `AWAITING_FINAL` state cannot verify which decision unlocked it — in a batch whose item 5 exists because a digest nobody checks certifies nothing. Each half of the choice is argued in `387edc2`'s body and individually right; the gap is what falls between them. Fix is design (a binding adds machinery or a clause). | the next design round touching `run_bind_v2.py`'s decision-gating surface — round 2 `PROMISE-PATH-VOCAB` qualifies; **deadline = the first product run that reaches `AWAITING_FINAL` via `NO_REPAIR`** | `v3-review-full-38038ec.md` O-4, user-ruled to bank 2026-09-03 |
| emit-reviewed-legality | **`emit_reviewed` guards on one status, not on transition legality**: reached with an `AWAITING_FINAL` state it writes `REVIEWED` — a step `flow._SUCCESSORS["AWAITING_FINAL"]` does not admit — and returns `True`; a bind re-run walks back out so nothing is lost, but the illegal step reaches disk on the way. Predates round `PROMISE-PATH-ENGINE` and is untouched by its repair; the repair's docstring claim about when a transition is owed is answered by a status test rather than a legality test. Widen the guard / legality check / leave alone is design. | the next round touching `run_bind_v2.py`'s emit path — round 2 `PROMISE-PATH-VOCAB` qualifies; **deadline = the first product run that re-runs a completed bind** | `v3-review-verify-1f6a5a5.md` V-O-1, user-ruled to bank 2026-09-03 |
| record-commit-owner | **`RULES.md` `R6` "the orchestrator commits it" and `REVIEW.md`'s *Where the result lives* "You persist, and commit" command opposite acts of the same product reviewer session** (`ORCHESTRATION.md:56` on `R6`'s side): a product reviewer obeying `R6` leaves its record uncommitted — by `REVIEW.md`'s own definition an unreturned review, the freeze guard holding the caller's branch. Origin measured: `4b81dd9` moved `R6` from the construction checklist into `RULES.md`, widening its scope without asking whether the clause is true on the product side. The read tiered it must-fix; **user ruled (c) 2026-09-02: downgraded to low and banked** — no product run begins before this batch's rounds close. Fix = one owner, named once (qualify `R6`'s clause to the construction side, or change `REVIEW.md` to match `R6`); which side owns is the user's, and the choice is design — it decides who performs an act in every future run — so no bytes. | the next design round touching `R6` or `REVIEW.md`'s *Where the result lives* — round 2 `PROMISE-PATH-VOCAB` qualifies; **deadline = before the next product-run review dispatch** (the moment the defect bites) | `v3-cold-read-51bd4f6.md` M-1 |
